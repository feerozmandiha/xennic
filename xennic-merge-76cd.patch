diff --git a/docs/audit/executive-summary.md b/docs/audit/executive-summary.md
index b5f0a9f..c05534c 100644
--- a/docs/audit/executive-summary.md
+++ b/docs/audit/executive-summary.md
@@ -1,233 +1,52 @@
-# Executive Summary — Xennic Platform Enterprise Audit
+# خلاصه مدیریتی ممیزی و طراحی معماری پلتفرم ذخیره‌سازی (Executive Summary)
+========================================================================
 
-**Audit Date:** 1405/04/11 (July 2, 2026)
-**Project:** Xennic Enterprise v1.0 — Phase 8.5 Enterprise Hardening
-**Audit Scope:** 10 Work Packages covering Architecture, Production Readiness, Performance, Security, AI, Code Quality, Tests, Technical Debt, Refactoring Roadmap, Release Checklist
+**شناسه سند:** XENNIC-STORAGE-EXEC-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول ممیزی:** دستیار ممیزی و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**مخاطب:** Chief Executive AI (CEAI)  
+**وضعیت:** تکمیل شده (COMPLETED)  
 
 ---
 
-## Overall Scores
+## ۱. یافته‌های کلیدی ممیزی فاز صفر (Audit Findings)
 
-| Dimension                |   Score    | Status                                                  |
-| ------------------------ | :--------: | ------------------------------------------------------- |
-| **Architecture**         | **55/100** | ⚠️ Needs improvement — stub modules, DDD leaks          |
-| **Production Readiness** | **40/100** | 🔴 Not production-ready — major gaps                    |
-| **AI Readiness**         | **25/100** | 🔴 Critical — agents never call LLM, fake streaming     |
-| **Security**             | **35/100** | 🔴 Critical — secrets committed, missing guards         |
-| **Performance**          | **30/100** | 🔴 Critical — N+1, fake streaming, SELECT \* everywhere |
-| **Maintainability**      | **43/100** | ⚠️ Needs improvement — 95 bare catches, 50+ `as any`    |
-| **Test Coverage**        | **9/100**  | 🔴 Only 5/27 modules have tests (8.72%)                 |
-| **Overall Platform**     | **34/100** | 🔴 Not production-ready — 28-week plan to RC1           |
+ممیزی جامع پلتفرم ذخیره‌سازی پروژه زنیک (Xennic) در فاز صفر، سه گسست یا ناهماهنگی اصلی (Structural Discrepancies) را مابین طراحی تئوریک و پیاده‌سازی واقعی کد شناسایی کرده است:
 
----
-
-## Critical Issues (Must Fix Before RC1)
-
-### Security — 7 Critical
-
-1. **JWT private key + GROQ_API_KEY committed to git** — `infrastructure/docker/secrets/jwtRS256.key`, `apps/api/.env`
-2. **UserController has NO guards** — anyone can create/list/delete users (`user.controller.ts:92-181`)
-3. **SSRF via webhooks** — `fetch(webhook.url)` without IP validation (`webhook.service.ts:133`)
-4. **Hard delete endpoints public** — UserController + WorkspaceController allow hard deletes
-5. **Encryption master key hardcoded** in `.env`
-6. **No Helmet/security headers** anywhere (`main.ts`)
-7. **Prompt injection** — user input directly embedded in LLM prompts (`ai.service.ts:169-197`)
-
-### Production — 10 Critical
-
-1. **No graceful shutdown** — SIGTERM drops active connections (`main.ts:14`)
-2. **No env validation** — `@nestjs/config` installed but unused; all modules read `process.env` directly
-3. **Unbounded in-memory stores** in `ai-runtime/` — OOM risk
-4. **Zero Prisma `$transaction` usage** — data inconsistency on failures
-5. **No idempotency on POST endpoints** — double-billing risk
-6. **LlmProvider falls back to mock** on AI failure (`llm.provider.ts:121`) — silent data corruption
-7. **DB errors silently swallowed** — `catch { return null; }` throughout `ai.repository.ts`
-8. **Timer leaks** in EngineeringClientService and VisionClientService
-9. **No readiness/liveness probes** — can't deploy on Kubernetes
-10. **No Prisma transactions** — multi-step operations not atomic
-
-### AI — 5 Critical
-
-1. **Agent never calls LLM** — `ElectricalEngineerAgent.run()` returns hardcoded response
-2. **Execution pipeline echoes input** — `execute()` returns input content directly
-3. **Dummy embeddings identical** — same random seed produces identical vectors
-4. **`req.workspaceId` typo** in ai-runtime controller
-5. **Duplicate method** `analyze_document()` overrides itself
-
-### Code Quality — 6 Critical
-
-1. **Prisma client imported in application layer** — infrastructure leak
-2. **95 bare catch blocks** (`catch { }`) — errors silently swallowed
-3. **54 `console.log` calls** — not using structured Logger
-4. **6 classes >300 lines** — SRP violations
-5. **50+ `as any` casts** — type safety bypassed
-6. **CORS `["*"]` in Python services**
-
----
-
-## Score Breakdown
-
-### Architecture (55/100)
-
-- ✅ 21/25 active modules follow DDD Clean Architecture
-- ✅ Clean dependency injection with interface tokens
-- ✅ Multi-tenant workspace isolation consistently applied
-- ⚠️ 4 enterprise stub modules (empty directories)
-- ⚠️ Health module is flat (no DDD layers)
-- ⚠️ Prisma client leaks into application layer in some modules
-- ❌ No ADR (Architecture Decision Records) found
-- ❌ No centralized error handling boundary
-
-### Production Readiness (40/100)
-
-| Area                    | Score  | Key Issue                         |
-| ----------------------- | :----: | --------------------------------- |
-| Logging                 | 60/100 | Logger mixed with console.log     |
-| Exception Handling      | 75/100 | Global filter exists              |
-| Timeouts                | 70/100 | Most external calls have timeouts |
-| Retry Policy            | 45/100 | Only LlmProvider has retry        |
-| Resource Cleanup        | 50/100 | No OnModuleDestroy                |
-| Memory Leaks            | 30/100 | Unbounded in-memory stores        |
-| Configuration           | 40/100 | @nestjs/config never initialized  |
-| Env Validation          | 35/100 | No Joi validation                 |
-| Graceful Shutdown       | 10/100 | No SIGTERM/SIGINT handlers        |
-| Health Checks           | 40/100 | Only basic health endpoint        |
-| Readiness Checks        | 25/100 | No readiness probe                |
-| Liveness Checks         | 20/100 | No liveness probe                 |
-| Backpressure            | 15/100 | No backpressure handling          |
-| Rate Limiting           | 80/100 | ThrottlerModule configured        |
-| Idempotency             | 10/100 | No idempotency keys               |
-| Transaction Consistency | 20/100 | No Prisma transactions            |
-
-### AI Readiness (25/100)
-
-- ✅ Prompt engineering (system prompts) — well-structured
-- ✅ Agent framework architecture — modular design
-- ✅ RAG pipeline structure — correct components
-- ✅ AI Runtime module — session/state/memory abstractions
-- ❌ Agent never reaches LLM (hardcoded responses)
-- ❌ Execution pipeline echoes input
-- ❌ Dummy embeddings (all identical vectors)
-- ❌ No Citation Engine
-- ❌ No Evidence Chain
-- ❌ No Hallucination Guardrails
-- ❌ No Conflict Resolution
-- ❌ Fake streaming (word-by-word simulation, not real SSE)
-- ❌ Python tools are dead code (not registered in agent)
-
-### Security (35/100)
-
-- ✅ JWT authentication implemented (RS256)
-- ✅ RBAC system with roles and permissions
-- ✅ Rate limiting configured (ThrottlerModule)
-- ✅ Workspace isolation enforced in most queries
-- ❌ **Secrets committed to git** (JWT keys, API keys, passwords)
-- ❌ UserController has no guards (anyone can manage users)
-- ❌ No Helmet middleware (no security headers)
-- ❌ SSRF vulnerability in webhooks
-- ❌ Hard delete endpoints accessible without confirmation
-- ❌ Prompt injection vulnerability
-- ❌ Consultations module missing workspace isolation
-- ❌ PermissionsGuard fail-open (returns true on error)
-
-### Performance (30/100)
-
-- ✅ Prisma schema has 128 indexes — good coverage
-- ✅ UUID primary keys — consistent pattern
-- ❌ 30+ instances of SELECT \* in repositories
-- ❌ 8 missing foreign key indexes
-- ❌ Fake streaming (no real SSE — simulated word delays)
-- ❌ No Redis caching for frequently accessed data
-- ❌ N+1 patterns in knowledge_taxonomy (polymorphic)
-- ❌ Sequential API calls in RAG pipeline
-- ❌ Pretty-print JSON in prompts (unnecessary overhead)
-- ❌ OpenAPI regenerated on every build (slow)
-
-### Maintainability (43/100)
-
-- ✅ Clean DDD structure in 21/25 modules
-- ✅ Interface-based dependency injection
-- ✅ Consistent naming (snake_case models, camelCase fields)
-- ❌ 95 bare catch blocks
-- ❌ 54 console.log calls (not Logger)
-- ❌ 50+ `as any` casts
-- ❌ 6 classes >300 lines
-- ❌ Pagination duplicated ~25 times across repositories
-- ❌ Prisma client imported in application layer (infrastructure leak)
-- ❌ Stub modules (4 empty enterprise modules)
-
-### Test Coverage (9/100)
-
-- ✅ ai-runtime module: 12 spec files, 143 tests (best coverage)
-- ✅ Health module: 2 spec files
-- ⚠️ Knowledge module: 3 spec files
-- ❌ 21/27 modules have ZERO tests
-- ❌ No integration tests (real DB)
-- ❌ No e2e tests for API endpoints
-- ❌ No concurrency or load tests
-- ❌ 15 engineering-service Python tests fail
-- ❌ ai-service agent tests crash on import
-- ❌ Frontend (web) has zero tests
+1. **باگ بحرانی پردازش اسناد (Critical Intake Crash):** ارجاع نادرست اینترفیس `'IStorageService'` در ماژول کارخانه دانش به سرویس هسته ذخیره‌سازی منجر به بروز ناسازگاری شدید در امضای متدها شده و فرآیند پردازش RAG اسناد را در زمان اجرا به طور ۱۰۰٪ با کرش مواجه می‌سازد.
+2. **شکل‌گیری جزایر موازی ذخیره‌سازی:** میکروسرویس پایتون (`ai-service`) اقدام به ایجاد باکت‌های کاملاً مجزا برای هر مستأجر به فرمت `{workspace_id}-documents` کرده که با معماری ۶ باکت سراسری هسته زنیک در تضاد فاحش است. این تضاد مانع از تعامل پایدار هوش مصنوعی با فایل‌های آپلود شده کاربران در لایه وب می‌گردد.
+3. **عدم دسترسی به فایل‌های عمومی (Public Access Block):** گارد امنیتی مستأجر و کاربر به طور سراسری روی تمام متدهای کنترلر استوریج کشیده شده است. این امر دسترسی بدون لاگین به تصاویر پروفایل کاربران، لوگوهای سازمان و دارایی‌های عمومی برندینگ را ناممکن ساخته است.
+4. **رها شدن جداول نسخه‌گذاری:** فرآیند ثبت و بازیابی نسخه‌های مختلف سند در دیتابیس بدون هیچ کد عملیاتی در بک‌اند رها شده است.
 
 ---
 
-## Technical Debt Overview
-
-| Severity      | Count  |  Total Effort  |
-| ------------- | :----: | :------------: |
-| P0 (Critical) |   8    |   ~80 hours    |
-| P1 (High)     |   14   |   ~160 hours   |
-| P2 (Medium)   |   18   |   ~120 hours   |
-| P3 (Low)      |   8    |   ~40 hours    |
-| **Total**     | **48** | **~400 hours** |
+## ۲. توصیف معماری هدف پیشنهادی (Proposed Target Design)
 
-**Top 5 Technical Debt Items:**
+معماری هدف تدوین‌شده در سند `docs/architecture/storage-platform-architecture.md` راهکارهای پایدار زیر را برای حل قطعی چالش‌های فوق اتخاذ کرده است:
 
-1. Missing cascade deletes in Prisma schema (data integrity risk)
-2. Missing `password_reset_tokens` → `users` relation
-3. No graceful shutdown (SIGTERM/SIGINT)
-4. 49+ String fields should be Prisma enums
-5. UUIDs stored as TEXT instead of native `@db.Uuid`
+- **تمرکزگرایی تعامل با آبجکت استوریج:** تمام بخش‌های برنامه (شامل هوش مصنوعی و پایتون) ملزم به هدایت فایل‌ها به سمت ۶ باکت رسمی هسته (`public`, `private`, `reports`, `documents`, `engineering`, `ai`) با پیشوند مسیر استاندارد مستأجر هستند.
+- **اینترفیس یکتای تعاملی (`IUnifiedStorageService`):** رفع ناسازگاری امضای متدها و اصلاح کامل فرآیند ثبت سند کارخانه دانش بر روی فایل‌های ثبت شده در سیستم مرکزی.
+- **اندپوینت عمومی امن (Public Safe Path):** تعریف آدرس `/storage/public/:id` بدون نیاز به احراز هویت کاربری که منحصراً برای لود بایت‌ها از باکت عمومی `public` بهینه‌سازی شده است.
+- **برقراری تمامیت ارجاعی پایگاه داده:** اصلاح مدل Prisma جهت پیوند ساختاری مستقیم بین فایل‌ها و سایر نهادهای بیزینسی (مانند محاسبات مهندسی، لوگوی فضاها، آواتارها و اسناد چت).
 
 ---
 
-## Recommended Execution Order
+## ۳. اسناد و مراجع ثبت شده (Deliverables Checklist)
 
-```
-Phase 0 — Stop the Bleeding     (Weeks 1-2)   🔴 Security + Critical fixes
-Phase 1 — Foundation             (Weeks 3-5)   🟠 Production readiness
-Phase 2 — Security Hardening    (Weeks 6-8)   🟠 Security completion
-Phase 3 — Data Layer            (Weeks 9-11)  🟡 Schema + indexes
-Phase 4 — Code Quality          (Weeks 12-14) 🟡 Cleanup + standards
-Phase 5 — AI Quality            (Weeks 15-17) 🟣 Real AI pipeline
-Phase 6 — Testing               (Weeks 18-22) 🔵 Coverage + integration
-Phase 7 — DevOps                (Weeks 23-25) 🟢 CI/CD + K8s + monitoring
-Phase 8 — Polish                (Weeks 26-28) 🟢 Final hardening
-```
+در چارچوب مأموریت فاز صفر، اسناد کلیدی زیر در بستر مخزن مستندات زنیک مستقر و ایجاد شدند:
 
-**Estimated time to Release Candidate 1:** **28 weeks** (~7 months)
-**Estimated total effort:** **~1,140 hours**
-**Team recommendation:** 3-4 engineers (2 backend, 1 AI/ML, 1 DevOps)
+1. **گزارش ممیزی وضعیت فعلی:** `docs/audit/storage-current-state-audit.md` (بررسی جامع مشکلات و بدهی‌های فنی).
+2. **سند معماری هدف پلتفرم:** `docs/architecture/storage-platform-architecture.md` (تبیین چارچوب‌ها و دیاگرام‌های جدید).
+3. **ثبت تصمیم معماری رسمی (ADR):** `docs/adr/ADR-021-canonical-file-document-asset-platform.md` (ثبت حاکمیتی تصمیم و اثرات معماری).
+4. **فهرست شکاف‌ها و ریسک‌ها:** `docs/implementation/storage-gap-registry.md` و `docs/generated/storage-risk-register.md`.
+5. **مستند اتصالات API و مدل دیتابیس:** `docs/generated/storage-api-inventory.md` و `docs/generated/storage-database-inventory.md`.
+6. **ماتریس‌های تست، دسترسی و چرخه حیات:** `docs/generated/storage-permission-matrix.md`، `docs/generated/storage-lifecycle-state-machine.md` و `docs/generated/storage-test-gap-matrix.md`.
+7. **دستور مهندسی فاز یک:** `docs/implementation/phase-one-engineering-order.md` (ابلاغیه رسمی کارهای توسعه‌دهندگان).
+8. **سند راهبرد مهاجرت داده‌ها:** `docs/generated/storage-migration-strategy.md` (فرمول انتقال بدون قطعی).
 
 ---
 
-## Deliverables Produced
-
-|      #      | Document                             |      Lines       |     Status      |
-| :---------: | ------------------------------------ | :--------------: | :-------------: |
-|    WP-1     | `docs/audit/architecture-audit.md`   |        —         |  ✅ Generated   |
-|    WP-2     | `docs/audit/production-readiness.md` |       554        |  ✅ Generated   |
-|    WP-3     | `docs/audit/performance.md`          |       639        |  ✅ Generated   |
-|    WP-4     | `docs/audit/security.md`             |       487        |  ✅ Generated   |
-|    WP-5     | `docs/audit/ai-audit.md`             |       672        |  ✅ Generated   |
-|    WP-6     | `docs/audit/code-quality.md`         |       419        |  ✅ Generated   |
-|    WP-7     | `docs/audit/test-gap-analysis.md`    |       418        |  ✅ Generated   |
-|    WP-8     | `docs/audit/technical-debt.md`       |       760        |  ✅ Generated   |
-|    WP-9     | `docs/audit/refactoring-roadmap.md`  |       468        |  ✅ Generated   |
-|    WP-10    | `docs/audit/release-candidate.md`    |       147        |  ✅ Generated   |
-| **Summary** | `docs/audit/executive-summary.md`    |        —         |  ✅ Generated   |
-|  **Total**  | **11 documents**                     | **~4,564 lines** | **✅ Complete** |
-
----
+## ۴. برنامه و گام‌های آتی (Next Steps)
 
-_End of Executive Summary — Xennic Platform Enterprise Audit v1.0_
+پس از تایید این سند توسط Chief Executive AI، دستور مهندسی صادر شده به فاز اجرا (Phase 1 — Implementation) هدایت شده و مراحل مهاجرت دیتابیس و همسان‌سازی کدهای پایتون و بک‌اند آغاز خواهد شد. پلتفرم با استقرار این معماری نوین صاحب یک لایه ذخیره‌سازی مقتدر، ایمن و بهینه برای محافظت از Single Source of Truth خواهد شد.
diff --git a/docs/generated/storage-api-inventory.md b/docs/generated/storage-api-inventory.md
new file mode 100644
index 0000000..1c5d57c
--- /dev/null
+++ b/docs/generated/storage-api-inventory.md
@@ -0,0 +1,167 @@
+# مستند رسمی واسط‌های برنامه‌نویسی پلتفرم ذخیره‌سازی (Storage API Inventory)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-API-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه هدف:** 1.1.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت واسط‌ها:** پیاده‌سازی شده / در حال ارتقا  
+
+این سند فهرست جامعی از تمام اندپوینت‌های فعال و پیشنهادی پلتفرم ذخیره‌سازی مرکزی زنیک را شامل متدهای احراز هویت، بدنه ورودی و فرمت خروجی توصیف می‌کند.
+
+---
+
+## ۱. فهرست اندپوینت‌های هسته ذخیره‌سازی (Core Storage APIs)
+
+پایه آدرس روت تمام اندپوینت‌ها: `/api/v1/storage`
+
+### ۱.۱. آپلود فایل جدید (Upload File)
+- **آدرس:** `POST /upload`
+- **فرمت ارسال:** `multipart/form-data`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`, `PermissionsGuard`
+- **مجوز دسترسی (Permission):** `files.upload`
+- **هدرهای اجباری:**
+  - `Authorization: Bearer <token>`
+  - `x-workspace-id: <workspace_uuid>`
+- **بدنه درخواست (Request Body):**
+  - فیلد باینری با نام `file` (حداکثر حجم ۱۰۰ مگابایت)
+- **پاسخ موفق (HTTP 201 Created):**
+  ```json
+  {
+    "success": true,
+    "data": {
+      "id": "1d8487b4-3580-4965-a827-6f77e68db35b",
+      "workspaceId": "019f75f0-xennic-45be-9ab6-267035eb4450",
+      "bucket": "documents",
+      "filename": "73bb1fc7-cf25-46f9-bb83-cb5b3d7c390a.pdf",
+      "originalName": "electrical_standards.pdf",
+      "extension": "pdf",
+      "mimeType": "application/pdf",
+      "size": 1548292,
+      "sizeHuman": "1.5 MB",
+      "checksum": "a5e954c278de...",
+      "uploadedBy": "user-uuid",
+      "createdAt": "2026-07-18T10:14:00.000Z",
+      "downloadUrl": "http://localhost:9000/documents/..."
+    }
+  }
+  ```
+
+### ۱.۲. لیست فایل‌های مستأجر (List Files)
+- **آدرس:** `GET /files`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`, `PermissionsGuard`
+- **مجوز دسترسی (Permission):** `files.read`
+- **پارامترهای جست‌وجو (Query Params):**
+  - `page`: شماره صفحه (پیش‌فرض: 1)
+  - `limit`: تعداد در هر صفحه (پیش‌فرض: 20)
+  - `bucket`: فیلتر بر اساس نام باکت (اختیاری)
+- **پاسخ موفق (HTTP 200 OK):**
+  ```json
+  {
+    "success": true,
+    "data": [
+      {
+        "id": "1d8487b4-...",
+        "workspaceId": "...",
+        "bucket": "documents",
+        "originalName": "electrical_standards.pdf",
+        "sizeHuman": "1.5 MB",
+        "createdAt": "2026-07-18T10:14:00.000Z"
+      }
+    ],
+    "meta": {
+      "page": 1,
+      "limit": 20,
+      "total": 1,
+      "totalPages": 1
+    }
+  }
+  ```
+
+### ۱.۳. دریافت جزئیات فایل و آدرس دانلود موقت (Get File Details & URL)
+- **آدرس:** `GET /files/:id`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`, `PermissionsGuard`
+- **مجوز دسترسی (Permission):** `files.read`
+- **پارامتر مسیر (Param):** `:id` شناسه فایل (UUID)
+- **پاسخ موفق (HTTP 200 OK):** بازگشت جزئیات کامل فایل مشابه آپلود به همراه لینک دانلود مستقیم یک‌ساعته (`downloadUrl`).
+
+### ۱.۴. دانلود مستقیم فایل (Direct Binary Stream Download)
+- **آدرس:** `GET /files/:id/download`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`
+- **مجوز دسترسی (Permission):** `files.read`
+- **پارامتر مسیر (Param):** `:id` شناسه فایل (UUID)
+- **نوع پاسخ:** جریان باینری (Buffer Stream) همراه با هدرهای `Content-Type` و `Content-Disposition`.
+
+### ۱.۵. حذف نرم فایل (Soft Delete)
+- **آدرس:** `DELETE /files/:id`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`, `PermissionsGuard`
+- **مجوز دسترسی (Permission):** `files.delete`
+- **پاسخ موفق:** `HTTP 204 No Content` (تغییر وضعیت رکورد دیتابیس به حذف شده).
+
+### ۱.۶. آمار فضای دیسک مصرفی (Storage Statistics)
+- **آدرس:** `GET /stats`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`
+- **مجوز دسترسی (Permission):** `files.read`
+- **پاسخ موفق (HTTP 200 OK):**
+  ```json
+  {
+    "success": true,
+    "data": {
+      "totalFiles": 142,
+      "totalSizeBytes": 538291048,
+      "totalSizeHuman": "513.3 MB"
+    }
+  }
+  ```
+
+### ۱.۷. بررسی وضعیت اتصال به آبجکت استوریج (Health Check)
+- **آدرس:** `GET /health`
+- **محافظت امنیتی (Guards):** فاقد گارد (عمومی)
+- **پاسخ موفق (HTTP 200 OK):**
+  ```json
+  {
+    "success": true,
+    "data": {
+      "status": "ok",
+      "buckets": ["public", "private", "reports", "documents", "engineering", "ai"]
+    }
+  }
+  ```
+
+---
+
+## ۲. اندپوینت‌های پیشنهادی فاز یک هدف (Proposed APIs)
+
+جهت رفع نواقص کشف‌شده در بخش ممیزی، اندپوینت‌های زیر در فاز یک اضافه خواهند شد:
+
+### ۲.۱. دسترسی بدون فیلتر به دارایی‌های عمومی (Public Asset safe Route)
+- **آدرس:** `GET /public/:id`
+- **محافظت امنیتی (Guards):** **فاقد هرگونه گارد احراز هویت (Unauthenticated)**
+- **پارامتر مسیر (Param):** `:id` شناسه فایل عمومی
+- **شرح عملکرد:** این متد فورا فایل را از باکت عمومی `public` فراخوانی کرده و به مرورگر استریم می‌کند. در صورتی که فایل ارجاع داده شده در باکتی غیر از باکت `public` ذخیره شده باشد، سرویس موظف به بازگرداندن خطای `403 Forbidden` است تا از نشت اطلاعات حساس مستأجرین به خارج جلوگیری شود.
+
+### ۲.۲. آپلود نسخه جدید فایل (Upload New Version)
+- **آدرس:** `POST /files/:id/versions`
+- **فرمت ارسال:** `multipart/form-data`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`, `PermissionsGuard`
+- **مجوز دسترسی (Permission):** `files.upload`
+- **بدنه درخواست (Request Body):** فیلد باینری `file` با سایز مجاز.
+- **پاسخ موفق (HTTP 201 Created):**
+  ```json
+  {
+    "success": true,
+    "data": {
+      "fileId": "1d8487b4-...",
+      "version": 2,
+      "path": "2026/07/version_file_name.pdf",
+      "checksum": "checksum_v2...",
+      "createdAt": "2026-07-18T11:00:00.000Z"
+    }
+  }
+  ```
+
+### ۲.۳. مشاهده تاریخچه نسخه‌ها (List Versions)
+- **آدرس:** `GET /files/:id/versions`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`
+- **مجوز دسترسی (Permission):** `files.read`
+- **پاسخ موفق (HTTP 200 OK):** لیست تمام نسخه‌های آرشیو شده به همراه شماره نسخه و تاریخ ایجاد جهت بازیابی یا لود نسخه‌های قدیمی‌تر.
diff --git a/docs/generated/storage-database-inventory.md b/docs/generated/storage-database-inventory.md
new file mode 100644
index 0000000..12ee826
--- /dev/null
+++ b/docs/generated/storage-database-inventory.md
@@ -0,0 +1,127 @@
+# مستند ساختار پایگاه داده پلتفرم ذخیره‌سازی (Storage Database Inventory)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-DB-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت پایگاه داده:** آماده توسعه فاز یک  
+
+این مستند حاوی شرح تفصیلی جداول موجود و اصلاحات ساختاری لازم در سطح لایه داده پروژه زنیک (Prisma ORM) به همراه تحلیل کلیدها، نمایه‌ها و گاردها می‌باشد.
+
+---
+
+## ۱. ساختار جداول موجود در فایل شمای پریزما (`prisma/schema.prisma`)
+
+در حال حاضر دو جدول مستقل برای نگهداری فایل‌ها و نسخه‌ها تعریف شده است:
+
+### ۱.۱. جدول فایل‌ها (`files`)
+- **نام مدل:** `files`
+- **هدف:** نگهداری ردیف‌های فراداده فایل‌های آپلود شده در سیستم.
+
+| نام ستون در دیتابیس | نوع داده در Prisma | نوع داده در PostgreSQL | وضعیت Nullable | توضیحات بیزینسی |
+| :--- | :--- | :--- | :--- | :--- |
+| **`id`** | `String` | `uuid` | خیر (PK) | شناسه منحصربه‌فرد کلید اصلی فایل |
+| **`workspace_id`** | `String` | `text` | خیر (FK) | ارجاع به جدول فضاهای کاری (`workspaces`) |
+| **`bucket`** | `String` | `text` | خیر | نام باکت هدف در MinIO (محدود به ۶ نام استاندارد) |
+| **`path`** | `String` | `text` | خیر | مسیر نسبی فایل در باکت پس از شناسه مستأجر |
+| **`filename`** | `String` | `text` | خیر | نام فیزیکی فایل تولیدشده توسط سیستم (UUID) |
+| **`original_name`**| `String` | `text` | خیر | نام اصلی فایل که توسط کاربر ارسال شده است |
+| **`extension`** | `String` | `text` | خیر | پسوند فایل (مانند pdf, png) به صورت حروف کوچک |
+| **`mime_type`** | `String` | `text` | خیر | نوع استاندارد رسانه‌ای فایل (Mime-Type) |
+| **`size`** | `BigInt` | `bigint` | خیر | سایز فایل بر حسب بایت |
+| **`checksum`** | `String?` | `text` | **بله** | هش تولید شده از روی فایل (مبتنی بر SHA-256) |
+| **`uploaded_by`** | `String` | `text` | خیر (FK) | شناسه کاربر آپلودکننده فایل (`users`) |
+| **`created_at`** | `DateTime` | `timestamp` | خیر | زمان بارگذاری فایل (پیش‌فرض: `now()`) |
+| **`deleted_at`** | `DateTime?` | `timestamp` | **بله** | زمان حذف نرم فایل در صورت مخفی‌سازی |
+
+- **نمایه‌ها (Indexes):**
+  - `@@index([workspace_id])` جهت افزایش سرعت جست‌وجوی فایل‌های متعلق به یک مستأجر.
+  - `@@index([uploaded_by])` جهت فیلتر و گزارش‌گیری فایل‌های آپلودی هر کاربر.
+  - `@@index([mime_type])` برای بهینه‌سازی فیلتر نوع فایل‌ها.
+
+### ۱.۲. جدول نسخه‌ها (`file_versions`)
+- **نام مدل:** `file_versions`
+- **هدف:** ثبت ارجاع‌های نسخه‌های تاریخی تغییریافته فایل‌ها.
+
+| نام ستون در دیتابیس | نوع داده در Prisma | نوع داده در PostgreSQL | وضعیت Nullable | توضیحات بیزینسی |
+| :--- | :--- | :--- | :--- | :--- |
+| **`id`** | `String` | `uuid` | خیر (PK) | کلید اصلی شناسه نسخه |
+| **`file_id`** | `String` | `text` | خیر (FK) | کلید خارجی متصل به شناسه فایل پدر (`files.id`) |
+| **`version`** | `Int` | `integer` | خیر | شماره افزایشی نسخه فایل (شروع از ۱) |
+| **`path`** | `String` | `text` | خیر | مسیر باینری فایل نسخه در باکت |
+| **`checksum`** | `String?` | `text` | **بله** | هش شناسه یکپارچگی فایل نسخه جدید |
+| **`created_at`** | `DateTime` | `timestamp` | خیر | زمان آپلود شدن نسخه جدید |
+
+- **قوانین حذف ردیف (Cascade Delete):**
+  - ارتباط با `files` به صورت `onDelete: Cascade` است؛ به این معنی که با حذف سخت یک فایل اصلی، تمام ردیف‌های مربوط به نسخه‌های قدیمی‌تر آن نیز به صورت فیزیکی از جدول دیتابیس حذف خواهند شد.
+
+---
+
+## ۲. گسست دیتابیسی و نیازهای اصلاح شمای ارتباطی (Database Gap Registry)
+
+برای جاری ساختن معماری جامع پلتفرم، تغییرات ساختاری زیر روی شمای پایگاه داده طراحی و پیشنهاد می‌شود:
+
+```
+  ┌───────────────────────────────────────────────────────────┐
+  │                        workspaces                         │
+  └─────────────────────────────┬─────────────────────────────┘
+                                │ (logo_file_id ──► files.id)
+                                ▼
+  ┌───────────────────────────────────────────────────────────┐
+  │                           files                           │
+  └─────────────────────────────▲─────────────────────────────┘
+                                │ (file_id ──► files.id)
+         ┌──────────────────────┼──────────────────────┐
+         ▼                      ▼                      ▼
+┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
+│   file_versions  │  │knowledge_document│  │   calculations   │
+└──────────────────┘  └──────────────────┘  └──────────────────┘
+```
+
+### الف) اتصال مستقیم کارخانه دانش به جدول فایل‌ها
+در مدل `knowledge_documents` ستون `storage_path` باید به عنوان فیلد فرعی در نظر گرفته شده و فیلد اصلی ارتباطی به صورت زیر تعریف شود:
+```prisma
+model knowledge_documents {
+  // ...
+  file_id      String?           @unique
+  file         files?            @relation(fields: [file_id], references: [id])
+  // ...
+}
+```
+
+### ب) اتصال پیوست‌های محاسبات مهندسی
+در مدل `calculations` ورودی‌ها و خروجی‌های محاسباتی به جدول فایل‌ها لینک داده می‌شوند:
+```prisma
+model calculations {
+  // ...
+  input_file_id   String?
+  output_file_id  String?
+  
+  input_file      files?   @relation("CalculationInputs", fields: [input_file_id], references: [id])
+  output_file     files?   @relation("CalculationOutputs", fields: [output_file_id], references: [id])
+  // ...
+}
+```
+
+### ج) تصویر لوگوی فضاهای کاری و آواتار کاربران
+- **در مدل `workspaces`:**
+  ```prisma
+  logo_file_id    String?   @unique
+  logo_file       files?    @relation("WorkspaceLogo", fields: [logo_file_id], references: [id])
+  ```
+- **در مدل `users`:**
+  ```prisma
+  avatar_file_id  String?   @unique
+  avatar_file     files?    @relation("UserAvatar", fields: [avatar_file_id], references: [id])
+  ```
+
+---
+
+## ۳. سناریوی اجرای پچ مهاجرت دیتابیس (DB Migration Plan)
+
+از آنجایی که سیستم در حال توسعه است، برای اعمال تغییرات بدون گسست یا خرابی داده‌های موجود، مهاجرت‌ها به صورت زیر تایید صلاحیت می‌شوند:
+1. **گام اول:** تولید پچ پریزما جهت ایجاد ارتباطات جدید به شکل فیلدهای اختیاری (`Optional / Nullable`). این اقدام جلوی بروز هرگونه عدم انطباق روی رکوردهای قبلی دیتابیس را می‌گیرد.
+2. **گام دوم:** اجرای اسکریپت بک‌آپ‌گیری از داده‌ها به صورت خودکار قبل از اعمال مهاجرت به وسیله اسکریپت‌های زیرساختی ریکاوری.
+3. **گام سوم:** اعمال مهاجرت ساختاری پایگاه داده از طریق اجرای دستور زیر به کمک فرآیندهای امن توسعه:
+   `pnpm prisma migrate dev --name add_unified_storage_relations`
diff --git a/docs/generated/storage-lifecycle-state-machine.md b/docs/generated/storage-lifecycle-state-machine.md
new file mode 100644
index 0000000..8f15925
--- /dev/null
+++ b/docs/generated/storage-lifecycle-state-machine.md
@@ -0,0 +1,86 @@
+# مستند ماشین وضعیت چرخه حیات فایل و سند (Storage Lifecycle State Machines)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-LIFE-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت:** تایید شده (APPROVED)  
+
+این مستند مدل چرخه حیات (Lifecycle) و نمودار ماشین وضعیت رکوردهای فیزیکی و متادیتای موجودیت‌های پلتفرم استوریج را مشخص می‌کند.
+
+---
+
+## ۱. چرخه حیات فایل و نسخه‌ها (File and Version Lifecycle)
+
+پایه‌ای‌ترین چرخه حیات متعلق به موجودیت فایل خام (`FileEntity`) است. این ماشین وضعیت وظیفه هدایت بایت‌های باینری از زمان ورود به دیسک استوریج تا نابودی کامل را عهده‌دار است.
+
+```mermaid
+stateDiagram-v2
+    [*] --> Uploaded : بارگذاری جریان بایت‌ها
+    Uploaded --> Active : تایید متادیتای دیتابیس
+    Active --> Active : آپلود نسخه جدید (FileVersion)
+    Active --> Archived : انتقال به Cold Storage (پس از ۶ ماه)
+    Active --> SoftDeleted : درخواست حذف نرم توسط کاربر
+    Archived --> SoftDeleted : درخواست حذف نرم توسط کاربر
+    SoftDeleted --> Active : بازیابی فایل (Restore)
+    SoftDeleted --> Purged : گذشت ۳۰ روز (Scheduler)
+    Purged --> [*] : حذف فیزیکی از MinIO و دیتابیس
+```
+
+### شرح جزئیات وضعیت‌ها و انتقال‌ها (Transitions):
+- **Uploaded:** بایت‌ها با موفقیت در باکت استاندارد MinIO قرار گرفته‌اند اما تراکنش دیتابیس هنوز تایید نشده است.
+- **Active:** تراکنش دیتابیس با موفقیت ثبت شده و فایل به عنوان منبع زنده برای سیستم شناخته می‌شود.
+- **Archived:** فایل به کلاس ذخیره‌سازی ابری ارزان‌تر انتقال یافته است. لینک‌های دانلود همچنان معتبر هستند اما لود اولیه ممکن است با کمی تاخیر انجام شود.
+- **SoftDeleted:** فیلد `deleted_at` با تاریخ زمان فعلی مقداردهی می‌شود. فایل از جست‌وجوهای کلاینت حذف گردیده ولی بایت‌های آن در MinIO محفوظ می‌ماند.
+- **Purged (Hard-Deleted):** فرآیند نهایی نابودی فیزیکی. در این مرحله بایت‌های شیء از MinIO حذف سخت شده و رکوردهای دیتابیس به صورت Cascade پاکسازی می‌گردند.
+
+---
+
+## ۲. چرخه حیات اسناد کارخانه دانش (Knowledge Document Lifecycle)
+
+موجودیت سند هوش مصنوعی (`KnowledgeDocument`) دارای چرخه‌ای پیچیده و غنی‌تر است که وضعیت پردازش اسناد متنی را مشخص می‌سازد.
+
+```mermaid
+stateDiagram-v2
+    [*] --> Registered : بارگذاری متادیتای فایل در دیتابیس
+    Registered --> Processing : آغاز فرآیند لوله پردازش اسناد
+    Processing --> Parsed : استخراج محتوای متنی (Text Extraction)
+    Parsed --> Classified : تشخیص دپارتمان و استانداردهای سند
+    Classified --> Chunked : تقسیم محتوا به تکه‌های دانش (Chunking)
+    Chunked --> Embedded : تحلیل برداری و تولید Embeddings
+    Embedded --> Published : انتشار در مخزن دانش زنیک (RAG Ready)
+    
+    Processing --> Failed : خطای پردازش فایل / ناسازگاری پکیج
+    Parsed --> Failed : خطای استخراج متن
+    Classified --> Failed : خطای هوش مصنوعی و عدم تحلیل سند
+    Embedded --> Failed : خطای سرویس تولید بردار
+    
+    Failed --> Processing : تلاش مجدد (Retry)
+    Published --> Deleted : حذف سند توسط مدیر
+    Deleted --> [*] : پاکسازی
+```
+
+### جزئیات گام‌های شکست پردازش (Failed State Handling):
+- در صورت وقوع هرگونه خطای سیستمی (مثلاً قطعی کلاینت پایتون یا اورفلو شدن توکن‌های ال‌ال‌ام)، سند در وضعیت **Failed** قرار می‌گیرد و پیغام خطا در ستون `error_message` ثبت می‌شود.
+- سیستم هوشمند کارخانه دانش دارای مکانیزم تلاش مجدد خودکار (Auto-Retry) تا سقف ۳ مرتبه در پس‌زمینه (توسط صف‌های BullMQ) است.
+
+---
+
+## ۳. چرخه حیات دارایی‌های عمومی برندینگ (Brand Asset Lifecycle)
+
+تصاویری که به عنوان لوگو، آیکون و آواتارها بارگذاری می‌شوند، مستقیماً با لایه نمایش فرانت‌اند در ارتباطند و چرخه‌ای کاملاً ایمن و سبک دارند.
+
+```mermaid
+stateDiagram-v2
+    [*] --> UploadedPublic : آپلود در باکت public
+    UploadedPublic --> VerifiedUI : بهینه‌سازی فرمت تصویر (فرمت WebP)
+    VerifiedUI --> ActiveAsset : تخصیص به عنوان لوگو/آواتار کاربر
+    ActiveAsset --> Replaced : بارگذاری لوگوی جدید توسط مستأجر
+    Replaced --> PurgedAsset : حذف فیزیکی بایت‌ها از باکت عمومی
+    PurgedAsset --> [*]
+```
+
+### جزئیات بهینه‌سازی تصاویر (VerifiedUI):
+- در زمان ورود تصاویر عمومی، سیستم بک‌اند موظف به اجرای یک پردازشگر تصویر سبک جهت بهینه‌سازی حجم و فرمت تصاویر به فرمت‌های وب‌پسند مانند `image/webp` یا `image/png` است تا پهنای باند شبکه بهینه‌سازی شود.
+- با آپلود لوگوی جدید، لوگوی قبلی مستقیما به وضعیت **Replaced** وارد شده و بایت‌های فیزیکی آن پس از ۷ روز برای جلوگیری از گسست کش مرورگرها، به شکل سخت از باکت عمومی پاکسازی می‌گردند.
diff --git a/docs/generated/storage-migration-strategy.md b/docs/generated/storage-migration-strategy.md
new file mode 100644
index 0000000..4ea8c1f
--- /dev/null
+++ b/docs/generated/storage-migration-strategy.md
@@ -0,0 +1,77 @@
+# راهبرد مهاجرت داده‌ها و همگرایی پلتفرم ذخیره‌سازی (Storage Migration Strategy)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-MIGRATE-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت راهبرد:** آماده تایید (IN_REVIEW)  
+
+این مستند راهبرد گام‌به‌گام و همه‌جانبه مهاجرت کدهای قدیمی و انتقال بایت‌های آبجکت استوریج به بستر معماری همگرای هدف را به همراه مکانیزم‌های بازگشت به عقب (Rollback Strategy) تشریح می‌کند.
+
+---
+
+## ۱. مراحل مهاجرت و همگرایی (Migration Stages)
+
+مهاجرت سیستم ذخیره‌سازی زنیک طی ۵ فاز سازمان‌یافته صورت می‌گیرد تا از هرگونه قطعی سرویس یا از دست رفتن اسناد حساس مهندسی پیشگیری شود:
+
+```
+[فاز ۱: بازبینی و طراحی] ──► [فاز ۲: مهاجرت پایگاه داده] ──► [فاز ۳: بازنویسی کدها] ──► [فاز ۴: انتقال فیزیکی بایت‌ها] ──► [فاز ۵: تست نهایی و Cutover]
+```
+
+### فاز ۱: تایید معماری و زیرساخت (Audit & Infrastructure Alignment)
+- **اقدامات:**
+  - تایید و تصویب سند معماری هدف توسط رهبری هوش مصنوعی (CEAI).
+  - افزودن کانتینر MinIO به فایل Docker Compose لوکال جهت هماهنگی محیط‌های لوکال توسعه‌دهندگان.
+  - حذف متغیرهای محیطی ناامن هاردکدشده پیش‌فرض در تمام سرویس‌ها.
+
+### فاز ۲: مهاجرت پایگاه داده (Database Migration)
+- **اقدامات:**
+  - اعمال شمای اصلاح‌شده Prisma و ایجاد فیلدهای ارتباطی کلید خارجی جدید به عنوان ویژگی‌های اختیاری (`Nullable`).
+  - اجرای مهاجرت پایگاه داده بدون تغییرات تخریبی روی فیلدهای قبلی:
+    `pnpm prisma migrate dev --name unified_storage_links`
+  - حفظ ستون قدیمی `storage_path` در جداول هوش مصنوعی تا پایان انتقال کامل داده‌ها.
+
+### فاز ۳: بازنویسی کدهای بک‌اند و پایتون (Code Refactoring)
+- **اقدامات:**
+  - تصحیح امضای تابع آپلود در کلاس `StorageService` و پیاده‌سازی اینترفیس یکتای `'IStorageService'`.
+  - رفکتور در فرآیند ثبت اسناد کارخانه دانش (`DocumentIntakeService.registerDocument`) جهت اتصال به استوریج جدید و برطرف کردن باگ کرش.
+  - بازنویسی کلاینت پایتونی `MinIOClient` در میکروسرویس پایتون جهت خواندن فایل‌ها از باکت مشترک `documents` با پیشوند مستأجر به جای باکت مستقل در هم شکسته قبلی.
+
+### فاز ۴: انتقال فیزیکی بایت‌ها و داده‌های قدیمی (Data Migration Script)
+- **اقدامات:**
+  - طراحی یک اسکریپت سبک مهاجرت داده فیزیکی (مثلاً اسکریپت Node.js یا پایتون مبتنی بر `mc` کلاینت).
+  - **وظیفه اسکریپت:** خواندن فایل‌های فیزیکی ذخیره شده در باکت‌های قدیمی به شکل `{workspace_id}-documents` و بارگذاری مجدد آن‌ها در باکت واحد مرکزی `documents` با الگوی جدید پیشوند مسیر:
+    `${workspaceId}/documents/${fileId}.${ext}`
+  - درج رکوردهای متناظر در جدول `files` پایگاه داده به صورت همزمان برای تضمین تمامیت ارجاعی.
+
+### فاز ۵: آزمون نهایی و تغییر وضعیت (Verification & Cutover)
+- **اقدامات:**
+  - اجرای تست‌های تضمین پایداری و ماتریس تست‌های ST-01 و ST-02.
+  - هدایت ترافیک نهایی فرانت‌اند به کنترلرهای جدید.
+  - پاکسازی فیزیکی کدهای مرده قدیمی و باکت‌های مستأجر منفرد قبلی از روی سرورهای MinIO.
+
+---
+
+## ۲. برنامه بازگشت به عقب در صورت بروز خطا (Rollback Strategy)
+
+در صورتی که در هر یک از مراحل مهاجرت با اختلال بحرانی یا از دست رفتن داده‌ها مواجه شویم، پروتکل بازگشت به عقب (Rollback) به صورت زیر به سرعت فعال می‌گردد:
+
+```mermaid
+graph TD
+    A[وقوع خطا در فرآیند مهاجرت] --> B{آیا خطا در لایه داده فیزیکی است؟}
+    B -- بله --> C[بازیابی بک‌آپ دایرکتوری MinIO با ابزار mc mirror]
+    B -- خیر --> D{آیا خطا در لایه دیتابیس است؟}
+    D -- بله --> E[اجرای مهاجرت معکوس دیتابیس با Rollback Migration]
+    D -- خیر --> F[برگشت کدهای برنامه به تگ گیت قبلی Git Revert]
+```
+
+### ۲.۱. بازیابی بایت‌های پایگاه داده و آبجکت استوریج
+- **دیتابیس:** در صورت بروز مشکل در ساختار دیتابیس، با استفاده از فایلهای پشتیبان تهیه شده قبل از فرآیند مهاجرت (`.sql`)، پایگاه داده به وضعیت پایدار قبلی بازگردانی می‌شود.
+- **استوریج شیء (MinIO):** قبل از شروع فرآیند انتقال فاز ۴، یک همگام‌سازی و کپی پشتیبان کامل از باکت‌های موجود تهیه می‌شود:
+  `mc mirror local/ remote/backups/storage_pre_migration`
+  در صورت خرابی، بایت‌های پشتیبان فورا ری‌استور می‌شوند.
+
+### ۲.۲. بازگشت کدهای برنامه (Application Code Rollback)
+- کدهای محیط عملیاتی بر روی بستر مخزن گیت به آخرین کامیت تگ پایدار قبلی بازمی‌ندازند تا سیستم به صورت فوری لود شده و خطاها در محیط محلی بازسازی و برطرف شوند.
+- این سازوکار تداوم کارکرد ۱۰۰ درصدی بیزینس را بدون ایجاد وقفه طولانی تضمین می‌کند.
diff --git a/docs/generated/storage-permission-matrix.md b/docs/generated/storage-permission-matrix.md
new file mode 100644
index 0000000..a70a0ee
--- /dev/null
+++ b/docs/generated/storage-permission-matrix.md
@@ -0,0 +1,65 @@
+# ماتریس امنیتی و دسترسی‌های پلتفرم ذخیره‌سازی (Storage Permission Matrix)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-PERM-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت:** مصوب (APPROVED)  
+
+این مستند ماتریس دقیق دسترسی‌ها، قوانین امنیتی، نقش‌ها و مرزهای ایزوله‌سازی را در سطح پلتفرم ذخیره‌سازی مرکزی زنیک مشخص می‌کند.
+
+---
+
+## ۱. ماتریس دسترسی به تفکیک نقش‌ها (Role-Based Access Control)
+
+ماتریس زیر نشان‌دهنده دسترسی نقش‌های مختلف سازمان به امکانات و باکت‌های گوناگون پلتفرم است. گارد کنترل دسترسی سازمان (`RbacModule` و `PermissionsGuard`) موظف به کنترل و اعمال دقیق این مجوزهاست.
+
+| قابلیت / عملیات روی فایل | مالک فضای کاری (Owner) | مدیر (Admin) | عضو (Member) | مهمان (Guest) | بازدیدکننده عمومی (Anonymous) | باکت هدف | مجوز مرتبط در RBAC |
+| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :--- |
+| **بارگذاری فایل عمومی** | ✅ | ✅ | ✅ | ❌ | ❌ | `public` | `files.upload` |
+| **بارگذاری فایل خصوصی** | ✅ | ✅ | ✅ | ❌ | ❌ | `private`, `engineering` | `files.upload` |
+| **بارگذاری اسناد متنی** | ✅ | ✅ | ✅ | ❌ | ❌ | `documents` | `files.upload` |
+| **مشاهده دارایی‌های عمومی**| ✅ | ✅ | ✅ | ✅ | ✅ (بدون لاگین) | `public` | فاقد مجوز (سراسری) |
+| **دانلود فایل‌های محرمانه**| ✅ | ✅ | ✅ (فقط دپارتمان) | ❌ | ❌ | `private`, `reports` | `files.read` |
+| **ایجاد نسخه جدید فایل** | ✅ | ✅ | ✅ | ❌ | ❌ | همه باکت‌ها | `files.upload` |
+| **حذف نرم فایل (Soft Delete)**| ✅ | ✅ | ❌ | ❌ | ❌ | همه باکت‌ها | `files.delete` |
+| **حذف سخت فایل (Hard Delete)**| ✅ | ❌ | ❌ | ❌ | ❌ | همه باکت‌ها | `files.admin` |
+| **مشاهده آمار مصرف فضا** | ✅ | ✅ | ✅ | ❌ | ❌ | — | `files.read` |
+| **اجرای دستی پاک‌سازی** | ✅ | ❌ | ❌ | ❌ | ❌ | — | `files.admin` |
+
+*توضیح علامت‌ها:*  
+- ✅ مجاز  
+- ❌ غیرمجاز / مسدود  
+
+---
+
+## ۲. قوانین ساختاری ایزوله‌سازی چندمستأجری (Tenant Isolation Rules)
+
+### ۲.۱. ایزوله‌سازی در دیتابیس (Logical DB Isolation)
+- تمام رکوردهای جدول `files` باید با فیلد `workspace_id` فیلتر شوند.
+- متد‌های مخزن داده (Storage Repository) حق بازگرداندن ردیفی را ندارند که مقدار `workspace_id` آن با شناسه فضای کاری جاری ثبت‌شده در هدر درخواست (`req.workspaceId` استخراج‌شده از گارد Workspace) تطابق ندارد.
+
+### ۲.۲. ایزوله‌سازی در لایه باینری (Physical S3 Isolation)
+- برای تمام فایل‌های محرمانه (باکت‌های `private`، `reports`، `documents`، `engineering`، `ai`)، پیشوند مسیر کلید آبجکت در MinIO الزاما به صورت زیر خواهد بود:
+  `{workspaceId}/{bucketName}/{fileId}.{extension}`
+- این قانون فیزیکی، مانع از تداخل دسترسی و لود فایل‌ها بین مستأجرین می‌شود.
+
+---
+
+## ۳. فرآیند فنی امنیت و اعتبارسنجی (Security Validation Process)
+
+```
+[درخواست کاربر] ──► [تایید احراز هویت توکن] ──► [بررسی شناسه Workspace] ──► [بررسی فیلتر باکت عمومی/خصوصی] ──► [استریم فایل]
+```
+
+1. **شناسایی باکت مقصد در زمان دانلود:**
+   - سرویس اصلی ابتدا رکوردهای پایگاه داده فایل را با فیلتر شناسه مستأجر فرامی‌خواند.
+   - اگر باکت فایل مرجع برابر با `public` باشد، بلافاصله اجازه دسترسی و بازگرداندن لینک صادر می‌شود.
+   - اگر باکت فایل غیرعمومی باشد، گارد بررسی عضویت کاربر در آن مستأجر (`workspace_id` فایل با `req.workspaceId` هماهنگ باشد) فعال شده و پس از اعتبارسنجی سطح دسترسی و مجوز، کلید آبجکت برای تولید آدرس دانلود یا دریافت مستقیم باینری فراخوانی می‌شود.
+2. **بررسی سهمیه‌های امنیتی در زمان آپلود:**
+   - پیش از درج بایت‌ها در MinIO، گارد سیستم حجم کل باکت مستأجر را بررسی می‌کند.
+   - اگر حجم آپلود جدید باعث تجاوز از سقف مجاز مستأجر شود، سیستم با ارسال پیغام خطای ۴۰۳ درخواست را متوقف می‌کند تا از حملات مسدودسازی حافظه (Denial of Storage) پیشگیری نماید.
+3. **محدودسازی دسترسی به متدهای حذف:**
+   - حذف نرم فایل‌ها فقط به نقش‌های سطح بالا (Owners and Admins) محدود می‌شود.
+   - فرآیند حذف فیزیکی و سخت فایل‌ها منحصرا به مالک سازمان (Owner) تعلق داشته و در لایه کنترلر با گارد مجوز `files.admin` محافظت می‌گردد.
diff --git a/docs/generated/storage-risk-register.md b/docs/generated/storage-risk-register.md
new file mode 100644
index 0000000..5a08336
--- /dev/null
+++ b/docs/generated/storage-risk-register.md
@@ -0,0 +1,80 @@
+# دفتر ثبت مخاطرات پلتفرم ذخیره‌سازی زنیک (Storage Risk Register)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-RISK-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت:** مصوب (APPROVED)  
+
+این مستند فهرست کاملی از مخاطرات امنیتی، زیرساختی و عملیاتی مرتبط با پلتفرم ذخیره‌سازی مرکزی زنیک را به همراه راهکارهای پیشگیری و برنامه‌های مقابله ثبت کرده است.
+
+---
+
+## ۱. جدول ماتریس ارزیابی مخاطرات (Risk Assessment Matrix)
+
+سطح مخاطره بر اساس فرمول: **شدت (Impact) × احتمال (Probability)** محاسبه می‌شود (بازه ۱ تا ۲۵):
+- ۱ تا ۵: ریسک پایین
+- ۶ to ۱۲: ریسک متوسط
+- ۱۵ to ۲۵: ریسک بحرانی و بالا
+
+| شناسه | عنوان مخاطره | احتمال (1-5) | شدت (1-5) | نمره ریسک (1-25) | سطح ریسک | طبقه‌بندی موضوعی |
+| :--- | :--- | :---: | :---: | :---: | :---: | :--- |
+| **RISK-01** | نشت اطلاعات و نقض ایزوله‌سازی مستأجرین | ۲ | ۵ | **۱۰** | متوسط | امنیت داده‌ها |
+| **RISK-02** | کرش قطعی لوله پردازش به علت تضاد امضای متدها| ۵ | ۵ | **25** | **بحرانی** | پایداری نرم‌افزار |
+| **RISK-03** | هرج‌ومرج و انحراف کلاینت‌ها در ساخت باکت‌های موازی| ۴ | ۴ | **۱۶** | **بحرانی** | انحراف معماری |
+| **RISK-04** | حملات پر کردن دیسک سرور (Denial of Storage) | ۳ | ۴ | **۱۲** | متوسط | پایداری زیرساخت |
+| **RISK-05** | تجمع فایل‌های یتیم فیزیکی (Byte Leak) در MinIO | ۴ | ۳ | **۱۲** | متوسط | هزینه‌های عملیاتی |
+| **RISK-06** | بهره‌برداری نفوذگران از پسوردهای پیش‌فرض کد | ۲ | ۵ | **۱۰** | متوسط | امنیت زیرساخت |
+| **RISK-07** | از دسترس خارج شدن بستر ذخیره‌سازی شیء (MinIO) | ۲ | ۵ | **۱۰** | متوسط | پایداری عملیاتی |
+
+---
+
+## ۲. مشخصات تفصیلی مخاطرات و راهکارهای کاهش (Risk Specifications & Mitigations)
+
+### RISK-01: نشت اطلاعات و نقض ایزوله‌سازی مستأجرین
+- **تاثیر نشت:** دسترسی غیرمجاز کاربر فضای کاری الف به اسناد فنی محرمانه فضای کاری ب.
+- **راهکار پیشگیری (Mitigation):**
+  - اعمال فیلترینگ منطقی در سطح کوئری‌های دیتابیس دسامبر با کمک گارد Workspace.
+  - پیاده‌سازی متد اعتبارسنجی همپوشانی Workspace در تمام سطوح دانلود باینری.
+- **برنامه مقابله (Contingency):** تعلیق دسترسی موقت به اندپوینت‌های دانلود عمومی و بررسی تمام رکوردهای لاگ دسترسی در سیستم مانیتورینگ.
+
+### RISK-02: کرش قطعی لوله پردازش به علت تضاد امضای متدها
+- **تاثیر نشت:** توقف صد درصدی فرآیند بارگذاری اسناد و فیل شدن تراکنش‌های هوش مصنوعی.
+- **راهکار پیشگیری (Mitigation):**
+  - تصحیح امضای متدهای کلاس سرویس ذخیره‌سازی بر اساس اینترفیس یکتا در پچ فاز یک.
+  - ممنوع کردن ثبت‌های مستقیمی که فاقد تست‌های ارزیابی کیفی هستند.
+- **برنامه مقابله (Contingency):** غیرفعال‌سازی موقت ماژول Intake دانش و نمایش پیغام در حال به‌روزرسانی به کاربران.
+
+### RISK-03: هرج‌ومرج و انحراف کلاینت‌ها در ساخت باکت‌های موازی
+- **تاثیر نشت:** پراکندگی اسناد فیزیکی، عدم دسترسی پایتون به بایت‌های آپلودی وب و فیل شدن فرآیندهای RAG.
+- **راهکار پیشگیری (Mitigation):**
+  - لغو کامل کدهای مستقل کلاینت MinIO در سرویس پایتون.
+  - تحمیل کدهای استانداردی آدرس‌دهی و باکت‌های هسته زنیک.
+- **برنامه مقابله (Contingency):** اجرای دستی اسکریپت جابجایی فایل‌ها و همگام‌سازی باکت‌های متداخل.
+
+### RISK-04: حملات پر کردن دیسک سرور (Denial of Storage)
+- **تاثیر نشت:** آپلود فایل‌های پرحجم توسط کاربران متخاصم و تمام شدن فضای کل هاست استوریج.
+- **راهکار پیشگیری (Mitigation):**
+  - تعریف سهمیه (Quota) سخت‌گیرانه برای هر Workspace متناسب با پکیج اشتراک.
+  - اعتبارسنجی حجم قبل از پذیرش و ذخیره‌سازی جریان فایل.
+- **برنامه مقابله (Contingency):** مسدودسازی آنی دسترسی آپلود فضای کاری خاطی و ارتقای فضای هارددیسک به صورت داینامیک.
+
+### RISK-05: تجمع فایل‌های یتیم فیزیکی (Byte Leak) در MinIO
+- **تاثیر نشت:** انباشت ترابایت‌ها فایل بلااستفاده و حذف نرم شده در MinIO بدون پاکسازی فیزیکی.
+- **راهکار پیشگیری (Mitigation):**
+  - توسعه و اجرای منظم زمان‌بندی روزانه (Cron Job) جهت پاکسازی فیزیکی رکوردهایی که بیش از ۳۰ روز از حذف نرم آن‌ها گذشته است.
+- **برنامه مقابله (Contingency):** اجرای دستی دستور پاکسازی با استفاده از ابزار کلاینت MinIO (`mc`).
+
+### RISK-06: بهره‌برداری نفوذگران از پسوردهای پیش‌فرض کد
+- **تاثیر نشت:** دسترسی نفوذگران به اطلاعات حیاتی به دلیل استفاده برنامه از کدهای پیش‌فرض هاردکدشده امنیتی در صورت نبود متغیرهای محیطی.
+- **راهکار پیشگیری (Mitigation):**
+  - حذف کامل فالبک‌های پیش‌فرض امنیتی در کدهای بک‌اند و پایتون.
+  - پیاده‌سازی الگوهای بررسی الزامی متغیرهای محیطی در استارت‌آپ برنامه و Fail-Fast.
+- **برنامه مقابله (Contingency):** تغییر اضطراری کلیدها و پسوردهای MinIO و راه‌اندازی مجدد تمام ران‌تایم‌های پروژه.
+
+### RISK-07: از دسترس خارج شدن بستر ذخیره‌سازی شیء (MinIO)
+- **تاثیر نشت:** اختلال همه‌جانبه در لود تصاویر، آواتارها، خروجی‌های محاسباتی و توقف کارهای پردازش اسناد.
+- **راهکار پیشگیری (Mitigation):**
+  - پیاده‌سازی کلاستر توزیع‌شده MinIO به همراه مکانیزم همگام‌سازی چندمرکزه (Replication).
+- **برنامه مقابله (Contingency):** استفاده از ویژگی ذخیره‌سازی محلی موقت (Local Disk Cache Backing) تا زمان اتصال مجدد آبجکت استوریج.
diff --git a/docs/generated/storage-test-gap-matrix.md b/docs/generated/storage-test-gap-matrix.md
new file mode 100644
index 0000000..23b555f
--- /dev/null
+++ b/docs/generated/storage-test-gap-matrix.md
@@ -0,0 +1,71 @@
+# ماتریس شکاف تست و سنجش پایداری استوریج (Storage Test Gap Matrix)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-TESTS-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت سنجش:** ممیزی شده (IN_REVIEW)  
+
+این مستند شکاف‌های موجود در پوشش تست‌های کیفیت و پایداری (Test Coverage Gaps) سیستم ذخیره‌سازی را به همراه طرح سناریوهای تستی معتبر توصیف می‌کند.
+
+---
+
+## ۱. ارزیابی پوشش تست‌های موجود (Current Testing Coverage Audit)
+
+- **تست‌های واحد (Unit Tests) در ماژول استوریج:** غیرفعال یا ناقص.
+- **تست‌های یکپارچگی (Integration Tests) در ماژول استوریج:** هیچ تست یکپارچگی خودکاری بین کلاینت MinIO و دیتابیس وجود ندارد.
+- **تست‌های انتها به انتها (E2E Tests):** فاقد پوشش تست خودکار روی بارگذاری بایت‌ها به صورت Multipart.
+- **پوشش تست‌های تلاقی دامنه‌ای (Cross-Domain Test Coverage):** پردازش اسناد کارخانه دانش بدون تست‌های ارزیابی اتصالات استوریج پیاده‌سازی شده که منجر به نادیده گرفتن باگ کرش امضای متدها شده است.
+
+---
+
+## ۲. سناریوهای تست پیشنهادی جهت پوشش کامل شکاف‌ها (Required Test Cases)
+
+جهت تضمین پایداری معماری هدف، سناریوهای تستی زیر باید به طور کامل پیاده‌سازی شوند:
+
+### ۲.۱. مجموعه تست‌های واحد (Unit Test Cases)
+
+#### سناریو UT-01: بررسی اعتبارسنجی حجم مجاز و سهمیه مستأجر (Storage Quota Validation)
+- **شرح تست:** ارسال درخواستی با حجمی بزرگتر از سهمیه باقی‌مانده Workspace.
+- **شرایط تست (Mock Requirements):** شبیه‌سازی خروجی متد `getTotalSize` در مخزن داده دیتابیس.
+- **معیار پذیرش (Assertion):** سیستم باید با پرتاب خطای `ForbiddenException` یا `BadRequestException` آپلود را مسدود کرده و داده‌ای ذخیره نکند.
+
+#### سناریو UT-02: بررسی تخصیص خودکار باکت بر اساس Mime-Type
+- **شرح تست:** اعتبارسنجی خروجی متد خصوصی تشخیص باکت بر اساس پسوند و نوع رسانه.
+- **معیار پذیرش (Assertion):** پسوند `pdf` به باکت `documents`، پسوند `png` به باکت `public` و پسوندهای ناشناس مهندسی به باکت `engineering` ارجاع یابند.
+
+---
+
+### ۲.۲. مجموعه تست‌های یکپارچگی (Integration Test Cases)
+
+#### سناریو IT-01: تعامل با درایور شبیه‌سازی‌شده MinIO (MinIO Service Integration)
+- **شرح تست:** تست متدهای اصلی `uploadBuffer`، `getObject` و `deleteObject` روی سرویس واقعی یا کانتینر موقت MinIO (تست داکر کامپوز ایزوله).
+- **معیار پذیرش (Assertion):**
+  - ایجاد موفقیت‌آمیز باکت‌ها در زمان استارت‌آپ.
+  - همگام‌سازی کامل بایت‌های ورودی با هش فایل نهایی ذخیره شده.
+
+#### سناریو IT-02: تست اتصال کارخانه دانش و پلتفرم ذخیره‌سازی مرکزی
+- **شرح تست:** پردازش سند در کلاس `DocumentIntakeService` با استفاده از اینترفیس بازنویسی شده `'IStorageService'`.
+- **معیار پذیرش (Assertion):** عدم وقوع هرگونه خطای نوع داده یا تضاد آرگومان در متد `upload` و تایید ذخیره فایل در باکت استاندارد `documents` دیتابیس.
+
+---
+
+### ۲.۳. مجموعه تست‌های امنیتی و چندمستأجری (Security & Multi-Tenancy Tests)
+
+#### سناریو ST-01: جلوگیری از دسترسی بین‌مستأجری به فایل‌های خصوصی
+- **شرح تست:** تلاش برای دانلود یا دسترسی به فایل با شناسه معتبر متعلق به Workspace A توسط کاربری که توکن معتبر و شناسه Workspace B را دارد.
+- **معیار پذیرش (Assertion):** سیستم باید فورا خطای `ForbiddenException` یا `NotFoundException` صادر کرده و اجازه دانلود فایل را ندهد.
+
+#### سناریو ST-02: بررسی دسترسی ایمن بدون لاگین به باکت عمومی
+- **شرح تست:** دانلود لوگوی سازمان از آدرس فاقد گارد `/storage/public/:id`.
+- **معیار پذیرش (Assertion):** لود موفقیت‌آمیز فایل بدون نیاز به توکن JWT؛ در حالی که برای تلاش به همان فایل با باکت تغییریافته به `private` خطای ۴۰۱ صادر شود.
+
+---
+
+## ۳. شاخص عبور از گیت کیفیت تست (Quality Gate Metrics)
+
+پیاده‌سازی نهایی فاز یک پلتفرم زمانی به وضعیت **COMPLETED** تغییر می‌یابد که:
+1. پوشش تست‌های خطوط جدید کد پلتفرم استوریج به **حداقل ۸۵٪** برسد.
+2. تمام آزمون‌های امنیتی و چندمستأجری (چهار سناریوی ST-01 و ST-02 به همراه آزمون‌های یکپارچگی هوش مصنوعی) با موفقیت پاس شوند (Green Test Suites).
+3. تست‌های نهایی در فرآیند گیت‌هاب اکشنز (CI/CD Pipeline) ثبت شده و به درستی اجرا شوند.
diff --git a/docs/implementation/phase-one-engineering-order.md b/docs/implementation/phase-one-engineering-order.md
new file mode 100755
index 0000000..113e681
--- /dev/null
+++ b/docs/implementation/phase-one-engineering-order.md
@@ -0,0 +1,64 @@
+# دستور مهندسی فاز یک: ارتقا و همگرایی پلتفرم ذخیره‌سازی زنیک (Phase One Engineering Order)
+========================================================================
+
+**شناسه دستور:** XENNIC-ENG-ORDER-STORAGE-001  
+**تاریخ صدور:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**صادرکننده:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**گیرندگان:** تیم توسعه بک‌اند (NestJS)، تیم هوش مصنوعی (AI/Python) و توسعه‌دهندگان OpenCode  
+**وضعیت:** ابلاغ شده (PLANNED)  
+
+---
+
+## ۱. هدف و دامنه تغییرات (Purpose & Scope)
+
+این سند به عنوان **دستور فنی رسمی فاز یک (Engineering Order)** جهت پایان دادن به جزایر موازی ذخیره‌سازی، برطرف کردن باگ‌های حاد زمان اجرا و پیاده‌سازی پلتفرم همگرای فایل صادر می‌گردد. تمام تیم‌های توسعه و توسعه‌دهندگان جریان OpenCode موظفند این دستورالعمل را به عنوان معیار اصلی پذیرش تغییرات قرار دهند.
+
+تغییرات باید **منحصرا** در حوزه‌های زیر اعمال شوند:
+- ماژول ذخیره‌سازی بک‌اند (`apps/api/src/modules/storage`)
+- اتصالات ماژول کارخانه دانش (`apps/api/src/modules/knowledge-factory`)
+- کلاینت ذخیره‌سازی هوش مصنوعی پایتون (`workspace/services/ai-service`)
+- جداول مربوطه در فایل شمای پریزما (`prisma/schema.prisma`)
+
+---
+
+## ۲. وظایف فنی و مأموریت‌های اجرایی (Technical Action Items)
+
+تیم‌های فنی موظفند اقدامات زیر را با هماهنگی کامل و بدون تغییر در کدهای پروداکشن عملیاتی بدون تایید قبلی گیت‌های کیفیت، پیش ببرند:
+
+### مأموریت ۲.۱: بازنویسی امضاهای متدها و رفع باگ Intake کارخانه دانش
+1. **اصلاح کلاس `StorageService` بک‌اند:** پیاده‌سازی کامل متد آپلود با یک کانتکست شیء پارامتریک برای سازگاری کامل با کارهای فراخوانی دامنه‌های جانبی.
+2. **برطرف کردن تداخل `'IStorageService'`:** به روزرسانی امضای متد ثبت اسناد فنی در `DocumentIntakeService` برای همخوانی با هسته و جلوگیری از وقوع خطای کرش در زمان ثبت و بخش‌بندی اسناد.
+3. **حذف کدهای مرده:** پاکسازی کلاس بدون استفاده `MinioStorageService` در پوشه کارخانه دانش.
+
+### مأموریت ۲.۲: اصلاح رفتار کلاینت پایتون و لغو ایجاد باکت‌های انفرادی
+1. **بازنویسی `MinIOClient` در پایتون:** غیرفعال کردن کدهای ایجاد باکت اختصاصی برای هر مستأجر به فرمت `{workspace_id}-documents`.
+2. **یکپارچه‌سازی باکت‌ها:** کدهای خواندن و تحلیل اسناد مهندسی به باکت یکتای `documents` هدایت شوند و آدرس فایل‌ها با الگوی `${workspace_id}/documents/${file_id}.ext` لود شود.
+
+### مأموریت ۲.۳: ایجاد دسترسی عمومی ایمن به دارایی‌های وب (Public Route)
+1. **طراحی اندپوینت اختصاصی بدون گارد:** تعریف متد `GET /storage/public/:id` در کلاس کنترلر ذخیره‌سازی.
+2. **گارد بررسی نام باکت:** این اندپوینت فقط و فقط باید به رکوردهایی اجازه دسترسی دهد که مقدار فیلد `bucket` آن‌ها در پایگاه داده برابر با `"public"` باشد. در غیر این صورت فورا خطای عدم دسترسی ۴۰۳ ارسال گردد.
+
+### مأموریت ۲.۴: پیاده‌سازی منطق نسخه‌گذاری و زمان‌بندی پاکسازی
+1. **توسعه متد `uploadNewVersion`:** منطق ذخیره‌سازی نسخه جدید فایل در جدول `file_versions` به همراه همگام‌سازی باکت‌های MinIO توسعه یابد.
+2. **پیاده‌سازی Scheduler کرون جاب روزانه:** نوشتن اسکریپت جاب جهت اسکن و حذف فیزیکی بایت‌های مربوط به فایل‌هایی که بیش از ۳۰ روز از حذف نرم آن‌ها گذشته است.
+
+---
+
+## ۳. قوانین هماهنگی با جریان توسعه OpenCode (OpenCode Rules)
+
+جهت جلوگیری از بروز تداخل (Overlap) در تغییرات کدها در جریان موازی توسعه:
+1. توسعه‌دهندگان OpenCode حق تغییر هیچ فایلی خارج از محدوده تعیین شده در بخش ۱ این سند را ندارند.
+2. هرگونه تحویل خروجی از طرف OpenCode که حاوی تعاریف جدید باکت در پایتون یا پیاده‌سازی مجزای کلاینت S3 باشد، فوراً ریجکت (REJECTED) خواهد شد.
+3. تایید نهایی کدهای پیاده‌سازی شده بر عهده این دستیار بر اساس معیارهای پذیرش (Quality Gates) خواهد بود.
+
+---
+
+## ۴. معیارهای نهایی پذیرش کیفیت (Acceptance Criteria)
+
+تغییرات فاز یک زمانی تایید (APPROVED) قلمداد می‌شوند که:
+1. فرآیند آپلود، رگولارایزیشن، چانکینگ و RAG اسناد فنی در کارخانه دانش بدون بروز خطا و با لود موفق بایت‌ها از باکت مشترک `documents` انجام شود.
+2. اندپوینت دسترسی عمومی تصویر لوگوی وب‌سایت بدون هدر لاگین تایید و لود شود.
+3. هیچ کلید و پسورد پیش‌فرضی به صورت هاردکدشده در فولد کدهای بک‌اند یا پایتون باقی نمانده باشد.
+4. تمام تست‌های امنیتی تداخل مستأجرین و ماتریس تست‌های ST-01 با قبولی صد در صدی پاس شوند.
+5. گزارش نهایی تایید کیفیت توسط این دستیار صادر و تحویل Chief Executive AI گردد.
diff --git a/docs/implementation/storage-gap-registry.md b/docs/implementation/storage-gap-registry.md
new file mode 100644
index 0000000..dc0e87b
--- /dev/null
+++ b/docs/implementation/storage-gap-registry.md
@@ -0,0 +1,92 @@
+# دفتر ثبت شکاف‌ها و بدهی‌های فنی پلتفرم ذخیره‌سازی (Storage Gap Registry)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-GAPS-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**تدوین‌کننده:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**آخرین وضعیت بررسی:** بازبینی شده (IN_REVIEW)  
+
+این سند به عنوان ردیف اصلی مغایرت‌ها، نواقص، چالش‌های فنی و نیازمندی‌های پیاده‌سازی پلتفرم ذخیره‌سازی زنیک عمل می‌کند.
+
+---
+
+## ۱. جدول مرجع شکاف‌ها (Gap Reference Matrix)
+
+| شناسه | عنوان شکاف فنی | شدت (Severity) | لایه تاثیرپذیر | مالک اصلاح (Owner) | وضعیت فعلی |
+| :--- | :--- | :--- | :--- | :--- | :--- |
+| **GAP-01** | خطای کرش امضای متد آپلود کارخانه دانش | **بحرانی** | لایه سرویس (NestJS) | تیم مهندسی بک‌اند | باز (OPEN) |
+| **GAP-02** | مغایرت عمیق باکت‌بندی میکروسرویس پایتون | **بالا** | میکروسرویس AI | تیم هوش مصنوعی / پایتون | باز (OPEN) |
+| **GAP-03** | دسترسی قفل‌شده به دارایی‌های عمومی (Public) | **بالا** | لایه کنترلر و امنیت | تیم امنیت و معماری | باز (OPEN) |
+| **GAP-04** | عدم تطابق ساختار دیتابیس با دامنه‌ها | **متوسط** | پایگاه داده (Prisma) | تیم دیتابیس / بک‌اند | باز (OPEN) |
+| **GAP-05** | نبود مکانیزم اجرای نسخه‌گذاری در کد | **متوسط** | لایه سرویس و دیتابیس | تیم مهندسی بک‌اند | باز (OPEN) |
+| **GAP-06** | نبود سیاست نگهداری و پاک‌سازی خودکار | **متوسط** | لایه زیرساخت و کرون‌جاب | تیم زیرساخت و DevOps | باز (OPEN) |
+| **GAP-07** | نبود کانتینر MinIO در داکر کامپوز بیس | **متوسط** | لایه زیرساخت (Docker) | تیم زیرساخت و DevOps | باز (OPEN) |
+| **GAP-08** | استفاده از متغیرهای ناامن پیش‌فرض (Fallback) | **بالا** | امنیت و زیرساخت | تیم امنیت و DevOps | باز (OPEN) |
+| **GAP-09** | نبود تست‌های یکپارچگی اتصالات استوریج | **متوسط** | تضمین کیفیت (QA) | تیم تست و QA | باز (OPEN) |
+
+---
+
+## ۲. توصیف دقیق و معیارهای پذیرش شکاف‌ها (Detailed Gaps & Acceptance Criteria)
+
+### GAP-01: خطای کرش امضای متد آپلود کارخانه دانش
+- **توضیحات:** انتزاع `'IStorageService'` در `KnowledgeFactoryModule` به کلاس `StorageService` متصل شده است، اما متد آپلود در کارخانه دانش به صورت سه آرگومانی فراخوانی می‌شود در حالی که سرویس اصلی فقط یک شیء پیکربندی می‌پذیرد. این ناهمگونی بلافاصله منجر به کرش ناشی از `TypeError` می‌شود.
+- **اثر معماری:** عدم کارکرد کامل زنجیره ورود اطلاعات به هوش مصنوعی (Document Intake Pipeline).
+- **اثر بیزینسی:** غیرفعال شدن ویژگی بارگذاری اسناد مهندسی و RAG.
+- **معیار پذیرش اصلاح:**
+  - یکسان‌سازی همه‌جانبه امضای متدها بر اساس استانداردهای جدید.
+  - فرآیند بارگذاری اسناد به درستی فایل را بدون خطا در باکت `documents` آپلود کرده و رکورد متادیتای آن را ثبت کند.
+
+### GAP-02: مغایرت عمیق باکت‌بندی میکروسرویس پایتون
+- **توضیحات:** کلاینت پایتونی `MinIOClient` در سرویس هوش مصنوعی اقدام به ایجاد باکت‌های مستقل برای هر Workspace به فرمت `{workspace_id}-documents` کرده که کاملاً با ساختار ۶ باکتی مرکزی بک‌اند ناهمخوان است.
+- **اثر معماری:** از بین رفتن معماری یکپارچه ذخیره‌سازی، تولید باکت‌های نامحدود و عدم امکان به اشتراک‌گذاری بهینه منابع.
+- **اثر بیزینسی:** خطای ۴۰۴ در خواندن اسناد آپلود شده توسط کاربران وب در هوش مصنوعی.
+- **معیار پذیرش اصلاح:**
+  - کلاینت پایتون بازنویسی شود تا فایل‌ها را با الگوی استاندارد بک‌اند از باکت `documents` و پیشوند `${workspaceId}/` بخواند و ذخیره کند.
+  - فرآیند تحلیل اسناد به صورت مستقیم فایل را از دایرکتوری استاندارد دریافت کند.
+
+### GAP-03: دسترسی قفل‌شده به دارایی‌های عمومی (Public)
+- **توضیحات:** متدهای کنترلر ذخیره‌سازی کاملاً پشت گاردهای احراز هویت قفل شده‌اند؛ در نتیجه مرورگر برای نمایش دارایی‌های عمومی مثل لوگوی مستأجر یا آواتار کاربر مجبور به ارسال هدر احراز هویت است که این کار برای کاربران غیرلاگین ناممکن است.
+- **اثر امنیتی و بیزینسی:** از کار افتادن قابلیت‌های مربوط به برندینگ عمومی و تصاویر آواتار در صفحات فرود و عمومی برنامه.
+- **معیار پذیرش اصلاح:**
+  - ایجاد اندپوینت اختصاصی `/storage/public/:id` بدون نیاز به توکن JWT.
+  - این اندپوینت فقط اجازه خواندن فایل‌هایی را می‌دهد که مقدار فیلد `bucket` آن‌ها برابر با `"public"` باشد. در غیر این صورت باید خطای `Forbidden` یا `404` صادر شود.
+
+### GAP-04: عدم تطابق ساختار دیتابیس با دامنه‌ها
+- **توضیحات:** فیلدهای لوگو در Workspace، آواتار در User، فایل‌های ورودی محاسبات مهندسی در Calculations و اسناد در Knowledge Factory ارتباط کلید خارجی با جدول `files` ندارند و به صورت متون خام ثبت شده‌اند.
+- **اثر معماری:** آسیب‌پذیری در تمامیت داده‌ها (Referential Integrity)، خطر وجود دیتای یتیم و از هم گسیخته در استوریج و دیتابیس.
+- **معیار پذیرش اصلاح:**
+  - اصلاح مدل Prisma و معرفی ارجاعات صریح کلید خارجی به مدل `files` برای تمام موجودیت‌های مرجع.
+  - اجرای موفق Migrationها بدون اختلال در سوابق کاری دیتابیس.
+
+### GAP-05: نبود مکانیزم اجرای نسخه‌گذاری در کد
+- **توضیحات:** جدول `file_versions` در پایگاه داده ایجاد شده ولی سرویس ذخیره‌سازی هیچ منطقی برای ذخیره یا بازخوانی نسخه‌های فایل ندارد.
+- **اثر معماری:** گسست شدید در پیاده‌سازی سناریوهای پایداری نسخه‌ها و عدم تطابق شدید کد با داکیومنت‌های پروژه.
+- **معیار پذیرش اصلاح:**
+  - افزودن تابع `uploadNewVersion` به کلاس سرویس اصلی.
+  - قابلیت دانلود نسخه مشخص با فیلتر اختیاری `?v=version_number` در اندپوینت دانلود مستقیم.
+
+### GAP-06: نبود سیاست نگهداری و پاک‌سازی خودکار (Retention)
+- **توضیحات:** در حال حاضر با پاک کردن فایل توسط کاربر، تنها ردیف آن در جدول دیتابیس Soft-Delete می‌شود و فایل فیزیکی برای همیشه بدون برنامه پاک‌سازی در MinIO باقی می‌ماند. همچنین نسخه‌های قدیمی فایل‌ها هرگز مدیریت حجم نمی‌شوند.
+- **اثر زیرساختی:** پر شدن سریع دیسک استوریج و هزینه‌های مازاد نگهداری آبجکت‌های مرده.
+- **معیار پذیرش اصلاح:**
+  - پیاده‌سازی یک کرون جاب با زمان‌بندی روزانه جهت پاک کردن فایل‌های فیزیکی که بیش از ۳۰ روز از حذف نرم آن‌ها گذشته است.
+  - تعریف سقف نگهداری ۵ نسخه تاریخی برای هر فایل و پاک کردن نسخه‌های اضافی پس از آن.
+
+### GAP-07: نبود کانتینر MinIO در داکر کامپوز بیس
+- **توضیحات:** در فایل داکر کامپوز محیط لوکال پروژه، سرویس MinIO نادیده گرفته شده است و اعضای تیم توسعه برای اجرای پروژه‌های ذخیره‌سازی ناچار به نصب محلی و دستی این برنامه هستند.
+- **اثر عملیاتی:** کندی در Onboarding نیروهای جدید و گسستگی در تست‌های لوکال توسعه‌دهندگان.
+- **معیار پذیرش اصلاح:**
+  - اضافه شدن کامل تگ سرویس `minio` به همراه پورت‌های `9000` و `9001` به فایل اصلی Docker Compose محیط توسعه.
+
+### GAP-08: استفاده از متغیرهای ناامن پیش‌فرض (Fallback)
+- **توضیحات:** در کلاس‌های سرویس MinIO بک‌اند و کلاینت پایتون، مقادیر پیش‌فرض هاردکدشده برای پسوردها و کلیدهای دسترسی وجود دارد که در صورت خرابی بارگذاری متغیرهای محیطی، برنامه بدون پرتاب خطا لود می‌شود.
+- **اثر امنیتی:** ریسک بسیار بالا در افشای پسوردها در محیط عملیاتی و رخنه به مخزن فایل‌های حساس پروژه.
+- **معیار پذیرش اصلاح:**
+  - حذف کامل رشته‌های ناامن پیش‌فرض از کد.
+  - برنامه باید در زمان راه‌اندازی (Bootstrap) در صورت عدم وجود هر یک از کلیدهای حیاتی باکت و استوریج با پرتاب خطا متوقف شود (Fail-Fast Behavior).
+
+### GAP-09: نبود تست‌های یکپارچگی اتصالات استوریج
+- **توضیحات:** اتصالات بین لایه ذخیره‌سازی، پایگاه داده و درایور MinIO در جریان تست‌های اتوماسیون جاری پروژه فاقد سناریوهای ممیزی کیفی دقیق و منظم است.
+- **اثر تضمین کیفیت:** نشت خطاهای امنیتی یا خطاهای تخصیص سهمیه به نسخه نهایی کدهای پروداکشن.
+- **معیار پذیرش اصلاح:**
+  - توسعه تست‌های آزمایشی یکپارچگی (Integration Tests) در جریان اجرای فاز یک مهندسی جهت تست و صحه‌گذاری جریان‌های گوناگون آپلود و دانلود در سطح ماژول‌ها.
diff --git a/scripts/apply-storage-docs.sh b/scripts/apply-storage-docs.sh
new file mode 100644
index 0000000..f9e1aa6
--- /dev/null
+++ b/scripts/apply-storage-docs.sh
@@ -0,0 +1,1519 @@
+#!/bin/bash
+# ==============================================================================
+# XENNIC STORAGE ARCHITECTURE AND GOVERNANCE DOCUMENTS AUTO-GENERATOR
+# ID: XENNIC-STORAGE-ARCH-001
+# Description: Generates all 13 required Phase 0 Audit & Architecture deliverables
+# ==============================================================================
+
+set -e
+
+echo "Initializing directories..."
+mkdir -p docs/audit docs/architecture docs/adr docs/implementation docs/generated
+
+# ── 1. CURRENT STATE AUDIT ───────────────────────────────────────────────────
+echo "Generating: docs/audit/storage-current-state-audit.md"
+cat << 'EOF' > docs/audit/storage-current-state-audit.md
+# ممیزی وضعیت فعلی پلتفرم ذخیره‌سازی زنیک (Storage Current-State Audit)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-AUDIT-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول ممیزی:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**مخاطب:** Chief Executive AI (CEAI) و تیم توسعه معماری  
+**وضعیت ممیزی:** تکمیل شده (COMPLETED)  
+
+---
+
+## ۱. خلاصه مدیریتی ممیزی (Executive Summary)
+
+این گزارش حاوی نتایج ممیزی همه‌جانبه سیستم‌ها، فرآیندها، ساختار داده‌ها و انتزاع‌های مرتبط با پلتفرم ذخیره‌سازی فایل، سند و دارایی (Asset) در پروژه **زنیک (Xennic)** است. 
+
+در حال حاضر، پروژه فاقد یک ساختار یکپارچه و بهینه برای مدیریت همگرا و قانون‌مند فایل‌هاست. بزرگ‌ترین یافته‌های ممیزی نشان‌دهنده وجود **سه لایه موازی، ناسازگار و مستقل از یکدیگر برای تعامل با فضای ذخیره‌سازی شیء (Object Storage - MinIO)** است که منجر به هدررفت منابع، عدم امنیت داده‌ها، پیچیدگی کدنویسی و بروز خطاهای بحرانی زمان اجرا (Runtime Crashes) شده است.
+
+### شاخص‌های کلیدی ممیزی (Audit KPIs):
+- **تعداد مکانیزم‌های ذخیره‌سازی مستقل:** ۳ مورد (هسته Storage، لایه Knowledge Factory، سرویس پایتونی AI Service).
+- **درجه تطابق کد با مستندات فعلی:** کمتر از ۴۰٪ (انحراف و گسست شدید در پیاده‌سازی نسخه فایل‌ها و انتزاع‌های اینترفیس).
+- **خطای بحرانی کشف‌شده (Critical Bug):** ناسازگاری ۱۰۰ درصدی امضای متد `upload` بین `StorageService` هسته و اینترفیس `IStorageService` در Knowledge Factory که باعث کرش قطعی فرآیند ثبت و پردازش اسناد می‌شود.
+- **ایزوله‌سازی مستأجرین (Tenant Isolation):** در سطح پایگاه داده با شناسه Workspace اعمال شده، اما در لایه دسترسی مستقیم به آبجکت‌ها و باکت‌های MinIO دچار تناقض‌های ساختاری و رفتاری است.
+
+---
+
+## ۲. بررسی لایه‌های ذخیره‌سازی موجود (Storage Architecture Audit)
+
+پروژه زنیک در حال حاضر دارای سه پیاده‌سازی کاملاً مجزا برای تعامل با MinIO است که در زیر تشریح می‌شوند:
+
+```
+                  ┌──────────────────────────────────────────────┐
+                  │                 XENNIC APPS                  │
+                  └──────────────────────┬───────────────────────┘
+                                         │
+         ┌───────────────────────────────┼───────────────────────────────┐
+         ▼                               ▼                               ▼
+┌──────────────────┐           ┌──────────────────┐            ┌──────────────────┐
+│   Core Storage   │           │Knowledge Factory │            │    AI Service    │
+│     (NestJS)     │           │ (NestJS Module)  │            │ (Python/FastAPI) │
+└────────┬─────────┘           └────────┬─────────┘            └────────┬─────────┘
+         │                              │                               │
+  [Uses 6 Buckets]                [Uses IStorage]                [Uses Workspace]
+  [Prefix: ws/path]              [Currently Broken]              [Custom Buckets]
+         │                              │                               │
+         ▼                              ▼                               ▼
+┌─────────────────────────────────────────────────────────────────────────────────┐
+│                               MinIO Object Storage                              │
+└─────────────────────────────────────────────────────────────────────────────────┘
+```
+
+### الف) لایه اول: ماژول هسته ذخیره‌سازی (`apps/api/src/modules/storage`)
+این لایه به عنوان ماژول رسمی و مرکزی ذخیره‌سازی تعریف شده است.
+- **فناوری:** استفاده از کلاینت رسمی `@nestjs` و پکیج `minio` (نسخه `8.0.7`).
+- **مدیریت باکت‌ها (Buckets):** تعهد به ایجاد ۶ باکت سراسری و پیش‌فرض در شروع برنامه (`ensureAllBuckets`):
+  1. `public`: برای دارایی‌های عمومی رابط کاربری و تصاویر پروفایل.
+  2. `private`: برای فایل‌های حساس کاری کاربران.
+  3. `reports`: برای خروجی‌های محاسباتی و فایل‌های اکسل.
+  4. `documents`: برای اسناد متنی و فنی.
+  5. `engineering`: برای فایل‌های محاسباتی و نقشه‌ها.
+  6. `ai`: برای مدل‌ها و داده‌های پردازش هوش مصنوعی.
+- **ایزوله‌سازی داده‌ها:** تعبیه شناسه Workspace به عنوان پیشوند (Prefix) در ساختار کلید آبجکت: `${workspaceId}/${path}`.
+- **محدودیت‌ها و ایرادات:**
+  - ساختار `file_versions` در دیتابیس وجود دارد اما **هیچ خط کدی** در برنامه برای مدیریت نسخه‌های فایل (Versioning) نوشته نشده است.
+  - مکانیزم تشخیص باکت به صورت هاردکد (Hardcoded) بر اساس Mime-Type انجام می‌شود؛ به طوری که باکت‌های `engineering` و `ai` علی‌رغم ایجاد، هرگز به صورت خودکار مقداردهی نمی‌شوند.
+
+### ب) لایه دوم: ماژول کارخانه دانش (`apps/api/src/modules/knowledge-factory`)
+این لایه وظیفه پردازش، بخش‌بندی (Chunking) و نمایه‌سازی اسناد فنی را برعهده دارد.
+- **تضاد معماری:** این ماژول اینترفیس خاص خود را به نام `IStorageService` تعریف کرده است:
+  ```typescript
+  export interface IStorageService {
+    upload(buffer: Buffer, path: string, contentType: string): Promise<string>;
+    download(path: string): Promise<Buffer>;
+    delete(path: string): Promise<void>;
+    exists(path: string): Promise<boolean>;
+  }
+  ```
+- **کرش بحرانی (Critical Runtime Crash):** در فایل `knowledge-factory.module.ts` ثبت ماژول به صورت زیر صورت گرفته است:
+  ```typescript
+  { provide: 'IStorageService', useExisting: StorageService }
+  ```
+  اما کلاس `StorageService` هسته، امضایی کاملاً متفاوت برای آپلود دارد:
+  ```typescript
+  async upload(data: {
+    workspaceId: string;
+    uploadedBy: string;
+    buffer: Buffer;
+    originalName: string;
+    mimeType: string;
+    bucket?: FileBucket;
+  }): Promise<FileEntity>
+  ```
+  هنگامی که فرآیند پردازش اسناد (`DocumentIntakeService.registerDocument`) تلاش می‌کند فایل را با متد زیر آپلود کند:
+  ```typescript
+  await this.storageService.upload(data.buffer, path, data.contentType);
+  ```
+  آرگومان اول به عنوان `data` در هسته دریافت می‌شود که یک شیء ساده نیست بلکه Buffer خام است. در نتیجه تلاش برای دسترسی به `data.buffer` با خطای قطعی `Cannot read properties of undefined (reading 'length')` مواجه شده و فرآیند پردازش اسناد کرش می‌کند.
+- **بخش بدون استفاده (Dead Code):** یک کلاس به نام `MinioStorageService` در پوشه اینفرای Knowledge Factory قرار دارد که کاملاً رها شده و در هیچ جایی نمونه‌سازی یا رجیستر نشده است.
+
+### ج) لایه سوم: سرویس هوش مصنوعی پایتون (`workspace/services/ai-service`)
+این میکروسرویس برای تحلیل اسناد، استخراج جداول و تعامل با مدل‌های زبان بزرگ (LLM) طراحی شده است.
+- **کلاینت مستقل:** پکیج `minio==7.2.16` پایتون در این ماژول نصب است و یک کلاینت اختصاصی به نام `MinIOClient` در `workspace/services/ai-service/app/tools/minio_client.py` پیاده‌سازی شده است.
+- **انحراف عمیق در استراتژی باکت‌ها (Bucket Collision Strategy):**
+  - این کلاینت پایتونی بر خلاف هسته (که ۶ باکت ثابت دارد و فایل‌ها را با پیشوند Workspace جدا می‌کند)، اقدام به **ایجاد یک باکت اختصاصی برای هر Workspace** می‌کند! فرمول نام‌گذاری آن عبارت است از:
+    `{workspace_id}-documents` (مثلاً: `019f75f0-xennic-documents`).
+  - این یعنی اگر یک فایل از طریق وب‌آپلود شود، در باکت `documents` و پیشوند `${workspaceId}/` ذخیره می‌شود. اما میکروسرویس پایتون انتظار دارد آن را در باکت مجزای مستأجر پیدا کند، که منجر به خطای عدم دسترسی و عدم امکان پردازش همگام اسناد خواهد شد.
+  - در حال حاضر این کلاینت در کدهای هسته سرویس پایتونی نمونه‌سازی می‌شود اما در فرآیند نهایی RAG، فایل از سیستم محلی موقت (Temp) به ابزارهای پردازش پاس داده شده و مستقیماً در Qdrant ذخیره می‌شود؛ بنابراین این ناهماهنگی در حال حاضر به صورت پنهان باقی مانده است.
+
+---
+
+## ۳. ممیزی مدل‌های داده و دیتابیس (Database Schema Audit)
+
+در پایگاه داده پروژه، دو جدول اصلی برای مدیریت فایل‌ها تعبیه شده‌اند:
+1. **جدول `files`:**
+   - ذخیره مشخصات فراداده فایل‌ها (نام اصلی، پسوند، نوع فایل، سایز، آدرس ذخیره‌سازی، وضعیت حذف نرم و بارگذارنده).
+   - مشکل اساسی: ستون `workspace_id` وجود دارد، اما هیچ ارتباط ساختاری یا کلید خارجی (Foreign Key) با دامنه‌های دیگر پروژه وجود ندارد.
+2. **جدول `file_versions`:**
+   - جدول تاریخچه نسخه‌ها که شامل `file_id`، فیلد `version` (شماره نسخه)، مسیر ذخیره‌سازی و زمان ایجاد است.
+   - مشکل اساسی: این جدول در سطح کد هیچ کاربردی ندارد و رها شده است.
+
+### نبود ارتباطات بین‌دامنه‌ای (Missing Domain Relationships):
+جداول زیر در پایگاه داده وجود دارند، اما هیچ ارتباطی با پلتفرم ذخیره‌سازی فایل ندارند و پیوندهای آدرسی آن‌ها به صورت رشته‌های خام (String/Text) یا بدون ارجاع ذخیره می‌شود:
+- `knowledge_documents` (کارخانه دانش): دارای ستون `storage_path` به عنوان آدرس متنی در باکت نامعلوم است و فاقد کلید خارجی به جدول `files` است.
+- `invoices` (قبوض مالی): بدون پیوند ساختاری با فایل‌های PDF قبوض صادر شده.
+- `calculations` (محاسبات مهندسی): فاقد ارتباط با فایل‌های ورودی یا خروجی محاسبات.
+- `workspaces` (تنظیمات برندینگ): فاقد ارتباط با فایل‌های لوگو، آیکون و تصاویر هدر.
+- `users` (آواتار کاربران): فاقد ارجاع به جدول فایل‌ها.
+
+---
+
+## ۴. ممیزی امنیت و مجوزها (Security & Permission Audit)
+
+### الف) ایزوله‌سازی مستأجرین (Tenant Isolation)
+- **وضعیت کنونی:** ایزوله‌سازی در دیتابیس از طریق فیلتر کردن پرس‌وجوها بر اساس `workspace_id` (توسط `WorkspaceGuard`) پیاده‌سازی شده است.
+- **ریسک امنیتی آبجکت استوریج:** هیچ مکانیزم امنیتی داینامیکی در سطح MinIO (مانند S3 Bucket Policies یا IAM Roles داینامیک) پیاده‌سازی نشده است. اگر یک کاربر به شناسه فایل کاربر دیگری دست یابد، گرچه پایگاه داده به دلیل فیلترِ هدرِ Workspace درخواست را ریجکت می‌کند، اما متد دانلود مستقیم با ایجاد لینک Presigned در صورت دستکاری دستی یا دور زدن گارد، پتانسیل نشت اطلاعات را دارد.
+
+### ب) تناقض جدی فایل‌های عمومی و خصوصی (Public Asset Exposure Leak)
+- **ایراد اساسی طراحی:** طبق تعریف، دارایی‌هایی مانند لوگوی سازمان، آواتار کاربر، یا Hero Image باید بدون نیاز به احراز هویت (Unauthenticated) در مرورگر لود شوند.
+- اما در پیاده‌سازی فعلی:
+  - تمام اندپوینت‌های کنترلر ذخیره‌سازی تحت گارد `JwtAuthGuard` و `WorkspaceGuard` هستند.
+  - حتی اگر فایلی در باکت `public` آپلود شود، برای گرفتن URL یا دانلود مستقیم آن، کاربر حتماً باید توکن معتبر ارسال کند و عضو آن Workspace باشد!
+  - این کار باعث می‌شود نمایش لوگو یا آواتار در بخش عمومی فرانت‌اند عملاً غیرممکن شود یا نیاز به پیاده‌سازی مکانیزم‌های ناسالم دور زدن گاردها داشته باشد.
+
+---
+
+## ۵. ممیزی زیرساخت، داکر و ریکاوری (Infrastructure, Docker & Recovery Audit)
+
+- **زیرساخت داکر:** 
+  - در فایل داکر کامپوز محیط توسعه (`workspace/docker-compose.yml`) و محیط پایه (`infrastructure/docker/compose/base/docker-compose.yml`) **هیچ سرویس MinIO تعریف نشده است!**
+  - برنامه در کدهای تستی انتظار دارد سرویس روی پورت `9000` یا `9001` در دسترس باشد، اما راه‌اندازی پیش‌فرض توسعه، فاقد این کانتینر است.
+- **امنیت دسترسی در کد (Placeholder Credentials Risk):**
+  - در فایل `minio.service.ts` و کلاینت پایتون، مقدار پیش‌فرض اعتبارنامه MinIO عبارت است از: `"MINIO_CREDENTIALS_FROM_ENV"`.
+  - این متغیر به عنوان مقدار پیش‌فرض در صورت نبود متغیرهای محیطی در نظر گرفته شده است. برنامه در صورت عدم وجود این متغیرها به جای پرتاب خطا در استارت‌آپ (Fail Fast)، با مقادیر هاردکدشده بالا لود می‌شود که یک ریسک امنیتی و عملیاتی بسیار بالاست.
+- **تست‌ها و ریکاوری:**
+  - مستندات سناریو ریکاوری (`dr-runbook.md`) وجود دارند اما ابزار تست یکپارچگی اتصالات و تست خودکار بازیابی فاقد فرآیند صحه‌گذاری عمیق بر روی باکت‌ها و متادیتای مرتبط با پایگاه داده است.
+
+---
+
+## ۶. ممیزی نیازمندی‌های دارایی‌های فرانت‌اند (Frontend Assets Requirement Audit)
+
+ماژول فرانت‌اند در فایل `apps/web/src/features/storage/components/storage-client.tsx` پیاده‌سازی شده است.
+- **مشاهدات لایه فرانت‌اند:**
+  - فرانت‌اند به درستی باکت‌های مختلف از جمله `public`، `private`， `reports`، `documents`، `engineering` و `ai` را متمایز کرده است.
+  - فرآیند آپلود فرانت‌اند به دلیل استفاده از Fastify در بک‌اند به شکل Multipart است که کاملاً مستقل عمل می‌کند.
+  - فرانت‌اند آیکون‌های متفاوتی را بر اساس Mime-Type رندر می‌کند.
+- **مشکلات کلیدی تجربه کاربری و ارتباطی:**
+  - به دلیل عدم پشتیبانی واقعی بک‌اند از نسخه‌گذاری (Versioning)، فرانت‌اند امکان بازیابی یا انتخاب نسخه‌های قبلی یک سند را به کاربر ارائه نمی‌دهد.
+  - به دلیل محدودیت‌های گارد امنیتی که در بخش ۴ ذکر شد، فرانت‌اند برای دریافت آدرس تصاویر عمومی همواره با چالش عدم احراز هویت روبرو خواهد بود.
+
+---
+
+## ۷. ماتریس انحراف و تناقض‌های مستندات و کد (Documentation Drift Matrix)
+
+| دامنه ممیزی | وضعیت توصیف‌شده در اسناد/طراحی | وضعیت واقعی در پیاده‌سازی کد | سطح ریسک | شرح تضاد و اثرات فنی |
+| :--- | :--- | :--- | :--- | :--- |
+| **نسخه‌گذاری فایل** | پلتفرم دارای قابلیت ثبت تاریخچه نسخه‌ها با ارتباط `file_versions` است. | جدول در دیتابیس خالی است؛ متدهای ذخیره، دانلود و حذف فقط روی نسخه تکی فایل کار می‌کنند. | **متوسط** | پیچیدگی بلااستفاده دیتابیس و انحراف انتظارات توسعه‌دهندگان فرانت‌اند. |
+| **تعامل با کارخانه دانش** | فرآیند پردازش اسناد از سیستم ذخیره‌سازی مرکزی استفاده می‌کند. | تضاد عمیق امضای توابع در `useExisting` که منجر به خطای کرش قطعی فرآیند Intake می‌شود. | **بحرانی** | عدم امکان ثبت هرگونه فایل و سند در لایه هوش مصنوعی و RAG. |
+| **یکپارچگی کلاینت پایتون** | میکروسرویس پایتون از همان بستر برای ذخیره داده‌های پردازش هوش مصنوعی استفاده می‌کند. | ساختار باکت به شکل `ws-documents` پیاده‌سازی شده که با معماری ۶ باکت هسته مغایرت دارد. | **بالا** | ناهماهنگی داده‌ای؛ در صورت خواندن فایل‌های آپلودی وب توسط پایتون، سیستم با خطای ۴۰۴ مواجه می‌شود. |
+| **دسترسی عمومی (Public Assets)** | دارایی‌های برندینگ، تصاویر لوگو و آواتار در باکت `public` ذخیره و نمایش داده می‌شوند. | تمام متدها پشت گارد کاربری و مستأجر هستند؛ هیچ فایل عمومی بدون لاگین قابل مشاهده نیست. | **بالا** | مسدود شدن کامل قابلیت‌های ظاهری برندینگ و آواتارهای کاربری در رابط کاربری بدون لاگین. |
+
+---
+
+## ۸. ثبت بدهی‌های فنی و ریسک‌ها (Technical Debt & Risk Registry)
+
+### بدهی‌های فنی (Technical Debts):
+1. **Raw SQL Queries in Repository:** استفاده از کوئری‌های خام SQL به جای متدهای مستقیم Prisma Client جهت ارتباط با دیتابیس، که خوانایی، تست‌پذیری و انعطاف پایگاه داده را کاهش می‌دهد.
+2. **Hardcoded Bucket Detection:** منطق تکراری و هاردکدشده برای تخصیص باکت‌ها بر اساس پسوند فایل در سرویس اصلی بک‌اند بدون وجود پیکربندی پویا.
+3. **Dead Code in Knowledge Factory:** کلاس `MinioStorageService` در پوشه اینفرای دانش بلااستفاده رها شده است.
+
+### ریسک‌های معماری و عملیاتی (Architectural Risks):
+- **Risk-01 (Crash in Intake Pipeline):** خطای کرش زمان اجرای ماژول کارخانه دانش به دلیل امضاهای ناسازگار آپلود فایل. (**تاثیر: بحرانی | احتمال: ۱۰۰٪ در محیط عملیاتی**)
+- **Risk-02 (Siloed Storage Engines):** تولید باکت‌های موازی و گوناگون توسط پایتون و بک‌اند در لایه ذخیره‌سازی MinIO که منبع واحد حقیقت (Single Source of Truth) را نابود کرده و ردیابی فایل‌ها را ناممکن می‌سازد. (**تاثیر: بالا | احتمال: بالا**)
+- **Risk-03 (Security Key Exposure):** بالا آمدن موفق برنامه با مقادیر ناامن پیش‌فرض در صورت عدم تنظیم متغیرهای محیطی استوریج. (**تاثیر: بالا | احتمال: متوسط**)
+
+---
+
+### گام بعدی:
+طراحی و ارائه **معماری هدف (Target Architecture)** جهت برطرف کردن تک‌تک انحرافات، شکاف‌ها و ریسک‌های فوق‌الذکر در قالب **سند معماری جامع ذخیره‌سازی مرکزی زنیک**.
+EOF
+
+# ── 2. TARGET ARCHITECTURE ───────────────────────────────────────────────────
+echo "Generating: docs/architecture/storage-platform-architecture.md"
+cat << 'EOF' > docs/architecture/storage-platform-architecture.md
+# سند معماری هدف پلتفرم ذخیره‌سازی جامع و یکپارچه زنیک (Target Architecture)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-ARCH-002  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**تدوین‌کننده:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**مخاطب:** Chief Executive AI (CEAI)، معماران ارشد و تیم‌های توسعه  
+**وضعیت معماری:** در انتظار بررسی (IN_REVIEW)  
+
+---
+
+## ۱. بیانیه چشم‌انداز معماری (Architectural Vision)
+
+پلتفرم ذخیره‌سازی مرکزی زنیک باید به عنوان **منبع واحد حقیقت (Single Source of Truth - SSoT)** برای تمام موجودیت‌های غیرساختاریافته (فایل‌ها، اسناد فنی، تصاویر، نقشه‌ها، پیوست‌ها و نسخه‌های دارایی‌ها) عمل کند. این معماری با هدف یکپارچه‌سازی کامل فرآیندهای ذخیره‌سازی بک‌اند NestJS، ماژول هوش مصنوعی (Knowledge Factory) و میکروسرویس پایتون طراحی شده است تا تعاملات استوریج را ساده، فوق‌العاده امن، مقیاس‌پذیر و مستقل از دامنه‌های جانبی سازد.
+
+### اصول کلیدی طراحی (Core Design Principles):
+1. **تمرکزگرایی و تجمیع (Centralization):** هیچ دامنه‌ای مجاز نیست کلاینت مستقل یا استراتژی باکت‌بندی متفاوتی برای تعامل مستقیم با MinIO پیاده‌سازی کند. تمام ارتباطات از طریق ماژول مرکزی `storage` انجام می‌شود.
+2. **یکپارچگی مفهومی (Canonical Modeling):** تعریف تمایز ساختاری دقیق بین File، Document، Asset و Attachment در سطح بیزینس و پایگاه داده.
+3. **امنیت پیش‌فرض و ایزوله‌سازی کامل (Secure by Default & Multi-Tenancy):** جداسازی کامل داده‌های مشتریان (Workspaces) در عین ارائه مکانیزمی امن و کارآمد برای دسترسی بدون نیاز به احراز هویت به دارایی‌های عمومی (مانند آیکون‌ها و لوگوها).
+4. **تطابق کامل کد و مستندات (Zero-Drift Enforcement):** هرگونه تغییر در بستر ذخیره‌سازی باید از طریق مکانیزم‌های بازبینی کیفیت حاکمیتی کنترل و تایید شود.
+
+---
+
+## ۲. تعاریف مفاهیم پایه‌ای پلتفرم (Canonical Taxonomy)
+
+جهت جلوگیری از سردرگمی توسعه‌دهندگان، واژگان و ماهیت موجودیت‌های ذخیره‌سازی به شکل زیر تعریف و تفکیک می‌شوند:
+
+```
+                                  ┌─────────────────┐
+                                  │   FileEntity    │ (پایه تمام موجودیت‌ها)
+                                  └────────┬────────┘
+                                           │
+         ┌────────────────────────┬────────┴────────┬────────────────────────┐
+         ▼                        ▼                 ▼                        ▼
+┌─────────────────┐     ┌─────────────────┐ ┌─────────────────┐     ┌─────────────────┐
+│    Document     │     │      Asset      │ │   Attachment    │     │   FileVersion   │
+├─────────────────┤     ├─────────────────┤ ├─────────────────┤     ├─────────────────┤
+│ اسناد متنی، فنی │     │ دارایی‌های برند  │ │  پیوست‌های موقت │ │   تاریخچه تغییرات  │
+│  نقشه‌ها، PDFها │     │ لوگو، آواکار... │ │ محاسبات، قبوض.. │ │   و ارجاعات فایل   │
+└─────────────────┘     └─────────────────┘ └─────────────────┘     └─────────────────┘
+```
+
+1. **فایل (File):** آبجکت پایه‌ای و خام ذخیره شده در Object Storage که دارای شناسه‌ای منحصربه‌فرد (UUID)، متادیتای پایه (سایز، پسوند، نوع فایل، هش بیزینسی) و متعلق به یک مستأجر (Workspace) مشخص است.
+2. **سند (Document):** موجودیتی با ارزش معنایی بالا (Semantic-Rich File) که وارد فرآیند پردازش کارخانه دانش (Knowledge Factory) یا موتورهای هوش مصنوعی می‌شود. اسناد قابلیت تکه‌تکه شدن (Chunking)، استخراج محتوا (Extraction)، تحلیل مفاهیم (OCR) و نمایه‌سازی برداری (Vector Indexing) را دارا هستند.
+3. **دارایی رابط کاربری (Asset):** فایلی که در دامنه‌های عمومی فرانت‌اند و برندینگ استفاده می‌شود (مانند Favicon، لوگوی شرکت، تصاویر برندینگ سازمان و آواتار کاربران). این دارایی‌ها ویژگی دسترسی عمومی بدون لاگین (Public Access) را دارا می‌باشند.
+4. **پیوست دامنه‌ای (Attachment):** فایلی که به یکی از هویت‌های دامنه‌ای کسب‌وکار متصل است (مانند فایل اکسل ضمیمه‌شده به یک گزارش محاسباتی، تصویر فیش یک قبض پرداختی، یا پیوست‌های متنی تسک‌ها). پیوست‌ها چرخه حیات خود را از موجودیت مرجع خود به ارث می‌برند.
+5. **نسخه فایل (FileVersion):** تاریخچه‌ای از وضعیت فایل در زمان‌های مختلف. پلتفرم ذخیره‌سازی امکان جایگزینی یک فایل با نسخه جدیدتر را بدون تغییر در شناسه مرجع فایل (File ID) ارائه می‌دهد.
+
+---
+
+## ۳. مدل ساختاری و ارتباطات بین‌دامنه‌ای (Domain Relationships)
+
+در معماری هدف، موجودیت مرکزی `files` از طریق جدول‌های رابط یا ارتباطات اختیاری (Optional Relations)، نقش چسب متصل‌کننده تمام دامنه‌ها را به فایل‌ها ایفا می‌کند:
+
+```mermaid
+classDiagram
+    class workspaces {
+        String id
+        String name
+        String logo_file_id
+    }
+    class users {
+        String id
+        String avatar_file_id
+    }
+    class files {
+        String id
+        String workspace_id
+        String bucket
+        String path
+        String filename
+        String original_name
+        String mime_type
+        BigInt size
+        String checksum
+        String uploaded_by
+    }
+    class file_versions {
+        String id
+        String file_id
+        Int version
+        String path
+        String checksum
+    }
+    class knowledge_documents {
+        String id
+        String workspace_id
+        String file_id
+        String status
+    }
+    class calculations {
+        String id
+        String workspace_id
+        String template_id
+        String input_file_id
+        String output_file_id
+    }
+    class invoices {
+        String id
+        String workspace_id
+        String pdf_file_id
+    }
+    
+    files "1" *-- "many" file_versions : has versions
+    workspaces "1" o-- "0..1" files : logo_file
+    users "1" o-- "0..1" files : avatar_file
+    knowledge_documents "1" o-- "1" files : backed_by_file
+    calculations "1" o-- "0..2" files : inputs_and_outputs
+    invoices "1" o-- "0..1" files : invoice_pdf
+    files --> workspaces : belongs_to
+    files --> users : uploaded_by
+```
+
+---
+
+## ۴. استراتژی باکت‌بندی و جداسازی داده‌ها (Unified Bucketing Strategy)
+
+برای یکپارچه‌سازی فرآیند ذخیره‌سازی در کل زیست‌بوم زنیک، تمام کلاینت‌ها (شامل پایتون و کارخانه دانش) ملزم به استفاده از مدل ۶ باکتی توافق‌شده هستند. استراتژی باکت‌بندی یکپارچه به صورت زیر اصلاح و یکپارچه می‌شود:
+
+| نام باکت | طبقه‌بندی دسترسی | کاربردهای تعیین شده | نحوه جداسازی مستأجر (Tenant Isolation) |
+| :--- | :--- | :--- | :--- |
+| **`public`** | کاملاً عمومی (Public-Read) | لوگوی سازمان، آواتار کاربر، دارایی‌های عمومی برندینگ | استفاده از پیشوند آدرس: `public/${workspaceId}/${fileId}` |
+| **`private`** | کاملاً محرمانه (Restricted) | اسناد کاری کاربران، گزارش‌ها، ضمیمه‌های حساس دامنه‌ای | استفاده از پیشوند آدرس: `${workspaceId}/private/${fileId}` |
+| **`reports`** | کاملاً محرمانه (Restricted) | قبوض مالیاتی، گزارش‌های عملکرد خروجی از سیستم | استفاده از پیشوند آدرس: `${workspaceId}/reports/${fileId}` |
+| **`documents`** | کاملاً محرمانه (Restricted) | اسناد ورودی کارخانه دانش برای پردازش RAG و هوش مصنوعی | استفاده از پیشوند آدرس: `${workspaceId}/documents/${fileId}` |
+| **`engineering`**| کاملاً محرمانه (Restricted) | فایل‌های محاسباتی مهندسی، نقشه‌های CAD و محاسبات | استفاده از پیشوند آدرس: `${workspaceId}/engineering/${fileId}` |
+| **`ai`** | کاملاً محرمانه (Restricted) | فایل‌های مدل‌های محلی، وزن‌ها، کانتکست‌های بزرگ چت | استفاده از پیشوند آدرس: `${workspaceId}/ai/${fileId}` |
+
+### حل چالش فایل‌های عمومی (Public vs Private Access Strategy):
+- برای باکت `public`، یک سیاست دسترسی عمومی (Anonymous Read S3 Bucket Policy) روی MinIO تنظیم می‌شود.
+- در بک‌اند NestJS، اندپوینتی ویژه با آدرس `/storage/public/:id` بدون هیچ گارد احراز هویتی تعریف می‌شود. این اندپوینت فقط درخواست‌های مرتبط با باکت `public` را پذیرفته و آبجکت را به صورت مستقیم یا در قالب presigned لود می‌کند.
+- برای سایر باکت‌ها، احراز هویت قوی توکن و بررسی دقیق عضویت در Workspace (`WorkspaceGuard`) اجباری است.
+
+---
+
+## ۵. اینترفیس جامع و همگرای استوریج (Centralized IStorageService)
+
+جهت حل چالش کرش و ناهماهنگی در ماژول‌های دیگر، اینترفیس واحد زیر طراحی شده است. کلاس `StorageService` هسته موظف است این اینترفیس را به طور کامل پیاده‌سازی کند:
+
+```typescript
+export interface IUnifiedStorageService {
+  /**
+   * آپلود فایل به همراه ثبت در پایگاه داده
+   */
+  upload(data: {
+    workspaceId: string;
+    uploadedBy: string;
+    buffer: Buffer;
+    originalName: string;
+    mimeType: string;
+    bucket?: FileBucket;
+    customPath?: string;
+  }): Promise<FileEntity>;
+
+  /**
+   * دانلود مستقیم فایل
+   */
+  download(id: string, workspaceId: string): Promise<{ buffer: Buffer; file: FileEntity }>;
+
+  /**
+   * دریافت آدرس موقت Presigned جهت دانلود کلاینت
+   */
+  getDownloadUrl(id: string, workspaceId: string, expirySeconds?: number): Promise<{ url: string; file: FileEntity }>;
+
+  /**
+   * آپلود یک نسخه جدید از فایل
+   */
+  uploadNewVersion(id: string, workspaceId: string, buffer: Buffer, uploadedBy: string): Promise<FileVersionEntity>;
+
+  /**
+   * حذف نرم (غیرفعال‌سازی در پایگاه داده)
+   */
+  softDelete(id: string, workspaceId: string): Promise<void>;
+
+  /**
+   * حذف سخت (پاکسازی از دیتابیس و MinIO)
+   */
+  hardDelete(id: string, workspaceId: string): Promise<void>;
+
+  /**
+   * بررسی وجود فایل
+   */
+  exists(id: string, workspaceId: string): Promise<boolean>;
+}
+```
+
+---
+
+## ۶. فرآیند همگام‌سازی و رفع باگ کارخانه دانش (Knowledge Factory Integration)
+
+برای برطرف کردن باگ کرش و همسان‌سازی پردازش اسناد با پلتفرم مرکزی:
+1. ارجاع `'IStorageService'` در `KnowledgeFactoryModule` به کلاس اصلاح شده و مطابق با اینترفیس جدید نگاشت داده می‌شود.
+2. موجودیت `KnowledgeDocument` از این پس یک ارجاع مستقیم به شناسه فیزیکی فایل ثبت‌شده در جدول `files` خواهد داشت (`file_id`).
+3. در فرآیند ثبت سند فنی:
+   - ابتدا متد `StorageService.upload` با تعیین باکت `documents` فراخوانی می‌شود.
+   - شناسه فایل بازگردانده شده (`file.id`) در فراداده‌های `knowledge_documents` ذخیره می‌شود.
+   - پردازش اسناد همواره با خواندن متادیتا از جدول `files` مرجع یکتا انجام می‌گیرد.
+
+---
+
+## ۷. فرآیند همگام‌سازی میکروسرویس پایتون (Python AI-Service Alignment)
+
+میکروسرویس پایتون از این پس حق ساخت باکت مستقل به فرمت `{workspace_id}-documents` را ندارد.
+1. کلاینت پایتونی `MinIOClient` ویرایش شده تا ساختار باکت‌های پروژه را منطبق بر نام‌های شش‌گانه هسته (`documents`, `ai`, ...) بشناسد.
+2. برای خواندن فایل‌هایی که هوش مصنوعی باید پردازش کند (مثلاً استخراج جداول)، سرویس پایتون شناسه فایل (`file_id`) و شناسه مستأجر (`workspace_id`) را دریافت می‌کند. سپس فایل را از مسیر استاندارد `${workspace_id}/documents/${file_id}.{ext}` در باکت `documents` فراخوانی می‌نماید.
+3. ارتباطات در لایه API بین NestJS و پایتون همواره بر پایه شناسه‌های یکتای فایل تعبیه شده در جدول دیتابیس مشترک (`files`) صورت می‌پذیرد.
+
+---
+
+## ۸. ماشین‌های وضعیت چرخه حیات موجودیت‌ها (Lifecycle State Machines)
+
+زنیک باید چرخه حیات فایل‌ها را در قالب ماشین وضعیت‌های دقیق مدیریت کند تا همواره ردیابی و ممیزی سیستم شفاف باشد.
+
+### الف) چرخه حیات فایل و نسخه‌ها (File & Version Lifecycle)
+```
+[Uploaded] ───► [Active] ───► [Archived] ───► [Soft-Deleted] ───► [Purged/Hard-Deleted]
+                     ▲                                │
+                     │                                │ (Restore action)
+                     └────────────────────────────────┘
+```
+- **Uploaded:** فایل در MinIO قرار گرفته و متادیتای اولیه ثبت شده است.
+- **Active:** فایل تایید شده و نسخه‌های آن در دسترس برنامه‌ها قرار دارد.
+- **Archived:** فایل به بایگانی منتقل شده (آبجکت MinIO به کلاس ذخیره‌سازی ارزان‌تر مانند Cold Storage انتقال می‌یابد).
+- **Soft-Deleted:** رکورد از دید کاربر مخفی شده اما داده‌ها در سیستم جهت بازگردانی تا بازه زمانی مشخص نگهداری می‌شوند.
+- **Purged:** پاکسازی کامل فیزیکی فایل و تمام نسخه‌های تاریخی آن از دیتابیس و MinIO.
+
+### ب) چرخه حیات اسناد کارخانه دانش (Knowledge Document Lifecycle)
+```
+[Registered] ──► [Intaken/Saved] ──► [Classified] ──► [Parsed] ──► [Chunked & Embedded] ──► [Published]
+                                                                                                  │ (On error)
+                                                                                                  ▼
+                                                                                              [Failed]
+```
+
+---
+
+## ۹. استراتژی مدیریت نسخه‌ها، حذف و نگهداری (Retention & Versioning Strategy)
+
+### الف) مکانیزم نسخه‌گذاری فایل‌ها
+- هر آپلود جدید روی یک شناسه فایل موجود، یک رکورد در جدول `file_versions` ایجاد می‌کند.
+- شماره نسخه به صورت افزایشی (`version = version + 1`) درج می‌شود.
+- در دیتابیس، ارجاع اصلی همواره به آخرین نسخه معتبر نگاشت دارد، اما کلاینت‌ها با پارامتر اختیاری `?v=1` می‌توانند نسخه‌های قدیمی‌تر را دانلود کنند.
+
+### ب) سیاست نگهداری و پاک‌سازی (Retention and Deletion Rules)
+1. **پاکسازی فایل‌های حذف نرم شده (Soft-Delete Retention):** فایل‌هایی که به وضعیت Soft-Deleted تغییر یافته‌اند، پس از گذشت **۳۰ روز** به صورت کاملاً خودکار توسط یک Scheduler جاب (NestJS Cron/BullMQ) از MinIO و دیتابیس به صورت سخت حذف می‌شوند.
+2. **سقف تعداد نسخه‌ها (Max Version Threshold):** هر فایل حداکثر می‌تواند **۵ نسخه تاریخی** داشته باشد. با آپلود نسخه ششم، قدیمی‌ترین نسخه تاریخی به صورت خودکار حذف سخت می‌شود.
+
+### ج) سیاست سهمیه مصرفی مستأجرین (Storage Quota Model)
+- هر Workspace دارای یک سقف فضای ذخیره‌سازی بر اساس طرح اشتراک (Subscription Plan) خود است (مثلاً طرح پایه: ۱ گیگابایت، طرح پیشرفته: ۲۰ گیگابایت).
+- در متد `upload` پیش از شروع دریافت جریان بایت‌ها، مجموع حجم مصرفی مستأجر از جدول `files` استعلام شده و با سقف مجاز مقایسه می‌شود. در صورت تجاوز از سقف، خطای `403 Quota Exceeded` صادر می‌شود.
+
+---
+
+## ۱۰. دیاگرام فرآیندهای بارگذاری و دسترسی (Sequence Diagrams)
+
+### فرآیند بارگذاری امن و موازی اسناد به کارخانه دانش (Unified Document Intake Flow)
+
+```mermaid
+sequenceDiagram
+    autonumber
+    actor Client as کاربر / مرورگر
+    participant Web as فرانت‌اند وب
+    participant API as هسته بک‌اند (NestJS)
+    participant DB as دیتابیس (Postgres)
+    participant MinIO as ذخیره‌ساز شیء (MinIO)
+    participant AI as سرویس پایتون (AI-Service)
+
+    Client->>Web: انتخاب و بارگذاری سند فنی
+    Web->>API: POST /storage/upload (multipart)
+    Note over API: بررسی سایز، نوع فایل و سهمیه مستأجر
+    API->>MinIO: ذخیره بایت‌ها در باکت documents
+    MinIO-->>API: تایید ذخیره و هش فایل
+    API->>DB: درج متادیتا در جدول files
+    DB-->>API: تایید و بازگشت UUID فایل
+    API-->>Web: موفقیت‌آمیز همراه با File UUID
+    Web->>API: POST /knowledge-factory/documents/intake (شناسه فایل)
+    API->>DB: ایجاد رکورد در knowledge_documents با ارجاع به file_id
+    API->>AI: فراخوانی پس‌زمینه جهت تحلیل و استخراج محتوا
+    AI->>MinIO: دانلود فایل از باکت documents/${workspaceId}/${fileId}
+    AI-->>API: بازگشت محتوای استخراج‌شده، جداول و خلاصه‌ها
+    API->>DB: به روزرسانی وضعیت سند و ذخیره Chunks و Embeddings
+```
+---
+
+## ۱۱. تاییدیه گیت‌های کیفیت معماری (Quality Gates Approval)
+
+به عنوان راهبر معماری و حاکمیت استوریج زنیک، اجرای فاز یک توسعه پیاده‌سازی این پلتفرم منوط به پاس کردن گیت‌های کیفی زیر است:
+- [ ] **Architecture Gate:** تایید نهایی این سند توسط Chief Executive AI.
+- [ ] **Database Gate:** اعمال موفق مهاجرت‌های ساختار جدول‌ها در Prisma و دیتابیس بدون از دست رفتن اطلاعات موجود.
+- [ ] **Security Gate:** تست موفق عدم دسترسی به فایل مستأجر الف توسط مستأجر ب در تمام سطوح دیتابیس و MinIO.
+- [ ] **Integration Gate:** راه‌اندازی فرآیند پردازش RAG با کلاینت استوریج همگرا بدون وجود خطاهای ناسازگاری لایه هوش مصنوعی.
+- [ ] **Infrastructure Gate:** اضافه شدن موفق کانتینر MinIO به کدهای بیس Docker Compose پروژه و قبولی در سناریوی Disaster Recovery.
+- [ ] **Test Coverage Gate:** پوشش تست بالای ۸۰ درصد برای ماژول استوریج جدید.
+- [ ] **Tenant Isolation Check:** تایید تفکیک مستأجرین در سطح پایگاه داده و آبجکت استوریج به صورت یکتا.
+- [ ] **Performance Review:** تایید کارکرد سریع لودینگ و دانلود مستقیم فایل‌ها بدون تاثیر منفی بر رم سرور بک‌اند.
+- [ ] **API Registry Approval:** تایید نهایی سوابق و مستندات اتصالات عمومی و محرمانه استوریج.
+EOF
+
+# ── 3. ADR-021 CANONICAL PLATFORM ────────────────────────────────────────────
+echo "Generating: docs/adr/ADR-021-canonical-file-document-asset-platform.md"
+cat << 'EOF' > docs/adr/ADR-021-canonical-file-document-asset-platform.md
+# ADR-021: Canonical and Unified File, Document, Asset, and Attachment Platform
+====================================================================================
+
+**وضعیت:** پذیرفته شده (ACCEPTED)  
+**تاریخ:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نویسنده:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**در ارتباط با:**  
+- [ADR-011: Knowledge Factory](./ADR-011-knowledge-factory.md)  
+- [ADR-020: Architecture Governance Automation](./ADR-020-architecture-governance-automation.md)  
+
+---
+
+## ۱. زمینه و انگیزه تصمیم (Context & Problem Statement)
+
+در فرآیند توسعه بستر زنیک (Xennic)، نیازهای متعددی در بخش‌های مختلف به مدیریت فایل‌ها شکل گرفت. این نیازها شامل فایل‌های مستندات فنی کاربران، قبوض صادرشده مالی، تصاویر پروفایل کاربران، لوگوهای برندینگ مستأجرین (Workspaces)، خروجی‌های محاسبات مهندسی و اسناد پردازشی هوش مصنوعی است.
+
+به دلیل عدم وجود قوانین و حاکمیت یکپارچه بر لایه ذخیره‌سازی، این بخش با انحرافات و مشکلات اساسی زیر مواجه شد:
+1. **ایجاد جزایر موازی ذخیره‌سازی:** کارخانه دانش (Knowledge Factory) و میکروسرویس پایتون اقدام به طراحی کلاینت‌ها و ساختارهای باکت‌بندی متفاوتی در MinIO کردند که به صورت مستقیم با یکدیگر در تضاد بوده و باعث پراکندگی داده‌ها شدند.
+2. **باگ بحرانی زمان اجرا:** ثبت یک اینترفیس غیرتطبیقی با نام `IStorageService` در بخش کارخانه دانش و ارجاع آن به `StorageService` هسته، منجر به عدم انطباق امضای متدها و کرش قطعی فرآیند بارگذاری اسناد به صورت ۱۰۰٪ شد.
+3. **نبود یکپارچگی ارجاعات در پایگاه داده:** دامنه‌های محاسبات مهندسی، قبوض مالیاتی، آواتارها و لوگوها به جای برقراری ارتباط با شناسه یکتای جدول فایل‌ها، اطلاعات را به شکل آدرس‌های متنی خام ذخیره می‌کنند که در نتیجه به راحتی می‌توانند بی‌استفاده رها شده یا منجر به فایل‌های یتیم (Orphaned Files) در استوریج شوند.
+4. **انسداد دسترسی به دارایی‌های عمومی:** به علت اعمال گاردهای سفت و سخت احراز هویت کاربری روی تمام متدهای کنترلر استوریج، نمایش آیکون‌ها، لوگوها و آواتارها در بخش‌های عمومی فرانت‌اند و بدون لاگین عملاً مسدود شده است.
+5. **نبود زمان اجرای نسخه‌گذاری (Versioning Runtime):** جدول نسخه‌گذاری در سطح دیتابیس وجود دارد اما در لایه سرویس هیچ ردی از منطق مدیریت نسخه‌ها پیاده‌سازی نشده است.
+
+---
+
+## ۲. تصمیم‌های معماری (Architectural Decisions)
+
+تصمیمات زیر برای حل ریشه‌ای مشکلات فوق و استقرار پلتفرم یکپارچه اتخاذ گردید:
+
+### الف) پذیرش مدل مفهومی یکپارچه (Canonical Model)
+توسعه تمام بخش‌های سیستم باید مفهوم تمایز داده شده بین ۴ مؤلفه پایه‌ای زیر را رعایت کند:
+- **File:** آبجکت باینری مرجع در استوریج.
+- **Document:** فایل غنی پردازش شده در کارخانه دانش.
+- **Asset:** فایل‌های با دسترسی عمومی برای وب‌سایت و برندینگ.
+- **Attachment:** ضمیمه‌های موقت به تسک‌ها، محاسبات و قبوض.
+
+### ب) همگرایی و انحصار در تعامل با MinIO
+هیچ ماژولی مجاز به تعریف مستقل کلاینت MinIO یا ارتباط مستقیم با این زیرساخت نیست. 
+- ماژول `storage` هسته تنها مالک و هدایت‌کننده جریان تعامل با MinIO است.
+- میکروسرویس پایتون و ماژول Knowledge Factory موظف به هماهنگی با الگوهای آدرس‌دهی و باکت‌های شش‌گانه هسته هستند. ساخت باکت‌های موازی و منفرد توسط کلاینت پایتونی کاملاً ممنوع و مردود است.
+
+### ج) بازنویسی اینترفیس ذخیره‌سازی مرکزی
+متدهای کلاس `StorageService` جهت پوشش کامل سناریوهای آپلود، دانلود، نسخه‌گذاری و بررسی‌های بهینه تطبیق داده شده و اینترفیس `IStorageService` به صورت مرکزی در سطح برنامه اصلاح می‌شود تا به صورت همگن در کارخانه دانش مورد استفاده قرار گیرد.
+
+### د) تعریف مسیر عمومی امن (Public Safe Route)
+یک کنترلر مجزا یا یک متد عمومیِ بدونِ گارد به آدرس `/storage/public/:id` معرفی می‌شود که به صورت انحصاری فقط به فایل‌های ثبت‌شده در باکت `public` دسترسی دارد. این روش امنیت مستأجرین را در باکت‌های خصوصی بدون قفل کردن منابع عمومی برندینگ تضمین می‌کند.
+
+### ه) اعمال یکپارچگی ارجاعات در دیتابیس
+جداول `knowledge_documents`، `calculations`، `invoices`، `workspaces` و `users` باید ارجاع ساختاری به جدول `files` را به صورت کلید خارجی (Foreign Key) پیاده‌سازی نمایند.
+
+---
+
+## ۳. پیامدها و اثرات معماری (Consequences)
+
+### نکات مثبت (Pros):
+- **رفع خطاهای زمان اجرا:** رفع قطعی باگ کرش لوله پردازش اسناد و تضمین سازگاری کامل متدها.
+- **منبع واحد حقیقت (SSoT):** ردیابی دقیق و منسجم بایت‌ها، آدرس‌ها، سایزها و متادیتای تمام فایل‌های فیزیکی سازمان در یک پلتفرم واحد.
+- **شفافیت زیرساخت:** پایان هرج‌ومرج ساخت باکت‌های فرعی در MinIO و ساختاردهی منظم در ۶ باکت توافق‌شده.
+- **امنیت پیشرفته:** حفظ بالاترین سطح ایزوله‌سازی مستأجرین در عین آزادسازی امن دارایی‌های عمومی فرانت‌اند.
+- **عدم تکرار کد:** پاکسازی کدهای مرده (Dead Code) و انتزاع‌های کپی‌پیست شده در کارخانه دانش و پایتون.
+
+### نکات منفی و چالش‌ها (Cons):
+- **نیاز به بازنویسی و مهاجرت داده‌ها:** ضرورت انجام رفکتور در بخش‌هایی از کدهای میکروسرویس پایتون و کنترلر هوش مصنوعی برای تغییر آدرس‌دهی باکت‌ها.
+- **مهاجرت دیتابیس:** اصلاح شمای Prisma و اعمال مهاجرت ساختاری جهت معرفی کلیدهای خارجی بدون گسست داده‌های قبلی.
+EOF
+
+# ── 4. STORAGE GAP REGISTRY ──────────────────────────────────────────────────
+echo "Generating: docs/implementation/storage-gap-registry.md"
+cat << 'EOF' > docs/implementation/storage-gap-registry.md
+# دفتر ثبت شکاف‌ها و بدهی‌های فنی پلتفرم ذخیره‌سازی (Storage Gap Registry)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-GAPS-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**تدوین‌کننده:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**آخرین وضعیت بررسی:** بازبینی شده (IN_REVIEW)  
+
+این سند به عنوان ردیف اصلی مغایرت‌ها، نواقص، چالش‌های فنی و نیازمندی‌های پیاده‌سازی پلتفرم ذخیره‌سازی زنیک عمل می‌کند.
+
+---
+
+## ۱. جدول مرجع شکاف‌ها (Gap Reference Matrix)
+
+| شناسه | عنوان شکاف فنی | شدت (Severity) | لایه تاثیرپذیر | مالک اصلاح (Owner) | وضعیت فعلی |
+| :--- | :--- | :--- | :--- | :--- | :--- |
+| **GAP-01** | خطای کرش امضای متد آپلود کارخانه دانش | **بحرانی** | لایه سرویس (NestJS) | تیم مهندسی بک‌اند | باز (OPEN) |
+| **GAP-02** | مغایرت عمیق باکت‌بندی میکروسرویس پایتون | **بالا** | میکروسرویس AI | تیم هوش مصنوعی / پایتون | باز (OPEN) |
+| **GAP-03** | دسترسی قفل‌شده به دارایی‌های عمومی (Public) | **بالا** | لایه کنترلر و امنیت | تیم امنیت و معماری | باز (OPEN) |
+| **GAP-04** | عدم تطابق ساختار دیتابیس با دامنه‌ها | **متوسط** | پایگاه داده (Prisma) | تیم دیتابیس / بک‌اند | باز (OPEN) |
+| **GAP-05** | نبود مکانیزم اجرای نسخه‌گذاری در کد | **متوسط** | لایه سرویس و دیتابیس | تیم مهندسی بک‌اند | باز (OPEN) |
+| **GAP-06** | نبود سیاست نگهداری و پاک‌سازی خودکار | **متوسط** | لایه زیرساخت و کرون‌جاب | تیم زیرساخت و DevOps | باز (OPEN) |
+| **GAP-07** | نبود کانتینر MinIO در داکر کامپوز بیس | **متوسط** | لایه زیرساخت (Docker) | تیم زیرساخت و DevOps | باز (OPEN) |
+| **GAP-08** | استفاده از متغیرهای ناامن پیش‌فرض (Fallback) | **بالا** | امنیت و زیرساخت | تیم امنیت و DevOps | باز (OPEN) |
+| **GAP-09** | نبود تست‌های یکپارچگی اتصالات استوریج | **متوسط** | تضمین کیفیت (QA) | تیم تست و QA | باز (OPEN) |
+
+---
+
+## ۲. توصیف دقیق و معیارهای پذیرش شکاف‌ها (Detailed Gaps & Acceptance Criteria)
+
+### GAP-01: خطای کرش امضای متد آپلود کارخانه دانش
+- **توضیحات:** انتزاع `'IStorageService'` در `KnowledgeFactoryModule` به کلاس `StorageService` متصل شده است، اما متد آپلود در کارخانه دانش به صورت سه آرگومانی فراخوانی می‌شود در حالی که سرویس اصلی فقط یک شیء پیکربندی می‌پذیرد. این ناهمگونی بلافاصله منجر به کرش ناشی از `TypeError` می‌شود.
+- **اثر معماری:** عدم کارکرد کامل زنجیره ورود اطلاعات به هوش مصنوعی (Document Intake Pipeline).
+- **اثر بیزینسی:** غیرفعال شدن ویژگی بارگذاری اسناد مهندسی و RAG.
+- **معیار پذیرش اصلاح:**
+  - یکسان‌سازی همه‌جانبه امضای متدها بر اساس استانداردهای جدید.
+  - فرآیند بارگذاری اسناد به درستی فایل را بدون خطا در باکت `documents` آپلود کرده و رکورد متادیتای آن را ثبت کند.
+
+### GAP-02: مغایرت عمیق باکت‌بندی میکروسرویس پایتون
+- **توضیحات:** کلاینت پایتونی `MinIOClient` در سرویس هوش مصنوعی اقدام به ایجاد باکت‌های مستقل برای هر Workspace به فرمت `{workspace_id}-documents` کرده که کاملاً با ساختار ۶ باکتی مرکزی بک‌اند ناهمخوان است.
+- **اثر معماری:** از بین رفتن معماری یکپارچه ذخیره‌سازی، تولید باکت‌های نامحدود و عدم امکان به اشتراک‌گذاری بهینه منابع.
+- **اثر بیزینسی:** خطای ۴۰۴ در خواندن اسناد آپلود شده توسط کاربران وب در هوش مصنوعی.
+- **معیار پذیرش اصلاح:**
+  - کلاینت پایتون بازنویسی شود تا فایل‌ها را با الگوی استاندارد بک‌اند از باکت `documents` و پیشوند `${workspaceId}/` بخواند و ذخیره کند.
+  - فرآیند تحلیل اسناد به صورت مستقیم فایل را از دایرکتوری استاندارد دریافت کند.
+
+### GAP-03: دسترسی قفل‌شده به دارایی‌های عمومی (Public)
+- **توضیحات:** متدهای کنترلر ذخیره‌سازی کاملاً پشت گاردهای احراز هویت قفل شده‌اند؛ در نتیجه مرورگر برای نمایش دارایی‌های عمومی مثل لوگوی مستأجر یا آواتار کاربر مجبور به ارسال هدر احراز هویت است که این کار برای کاربران غیرلاگین ناممکن است.
+- **اثر امنیتی و بیزینسی:** از کار افتادن قابلیت‌های مربوط به برندینگ عمومی و تصاویر آواتار در صفحات فرود و عمومی برنامه.
+- **معیار پذیرش اصلاح:**
+  - ایجاد اندپوینت اختصاصی `/storage/public/:id` بدون نیاز به توکن JWT.
+  - این اندپوینت فقط اجازه خواندن فایل‌هایی را می‌دهد که مقدار فیلد `bucket` آن‌ها برابر با `"public"` باشد. در غیر این صورت باید خطای `Forbidden` یا `404` صادر شود.
+
+### GAP-04: عدم تطابق ساختار دیتابیس با دامنه‌ها
+- **توضیحات:** فیلدهای لوگو در Workspace، آواتار در User، فایل‌های ورودی محاسبات مهندسی در Calculations و اسناد در Knowledge Factory ارتباط کلید خارجی با جدول `files` ندارند و به صورت متون خام ثبت شده‌اند.
+- **اثر معماری:** آسیب‌پذیری در تمامیت داده‌ها (Referential Integrity)، خطر وجود دیتای یتیم و از هم گسیخته در استوریج و دیتابیس.
+- **معیار پذیرش اصلاح:**
+  - اصلاح مدل Prisma و معرفی ارجاعات صریح کلید خارجی به مدل `files` برای تمام موجودیت‌های مرجع.
+  - اجرای موفق Migrationها بدون اختلال در سوابق کاری دیتابیس.
+
+### GAP-05: نبود مکانیزم اجرای نسخه‌گذاری در کد
+- **توضیحات:** جدول `file_versions` در پایگاه داده ایجاد شده ولی سرویس ذخیره‌سازی هیچ منطقی برای ذخیره یا بازخوانی نسخه‌های فایل ندارد.
+- **اثر معماری:** گسست شدید در پیاده‌سازی سناریوهای پایداری نسخه‌ها و عدم تطابق شدید کد با داکیومنت‌های پروژه.
+- **معیار پذیرش اصلاح:**
+  - افزودن تابع `uploadNewVersion` به کلاس سرویس اصلی.
+  - قابلیت دانلود نسخه مشخص با فیلتر اختیاری `?v=version_number` در اندپوینت دانلود مستقیم.
+
+### GAP-06: نبود سیاست نگهداری و پاک‌سازی خودکار (Retention)
+- **توضیحات:** در حال حاضر با پاک کردن فایل توسط کاربر، تنها ردیف آن در جدول دیتابیس Soft-Delete می‌شود و فایل فیزیکی برای همیشه بدون برنامه پاک‌سازی در MinIO باقی می‌ماند. همچنین نسخه‌های قدیمی فایل‌ها هرگز مدیریت حجم نمی‌شوند.
+- **اثر زیرساختی:** پر شدن سریع دیسک استوریج و هزینه‌های مازاد نگهداری آبجکت‌های مرده.
+- **معیار پذیرش اصلاح:**
+  - پیاده‌سازی یک کرون جاب با زمان‌بندی روزانه جهت پاک کردن فایل‌های فیزیکی که بیش از ۳۰ روز از حذف نرم آن‌ها گذشته است.
+  - تعریف سقف نگهداری ۵ نسخه تاریخی برای هر فایل و پاک کردن نسخه‌های اضافی پس از آن.
+
+### GAP-07: نبود کانتینر MinIO در داکر کامپوز بیس
+- **توضیحات:** در فایل داکر کامپوز محیط لوکال پروژه، سرویس MinIO نادیده گرفته شده است و اعضای تیم توسعه برای اجرای پروژه‌های ذخیره‌سازی ناچار به نصب محلی و دستی این برنامه هستند.
+- **اثر عملیاتی:** کندی در Onboarding نیروهای جدید و گسستگی در تست‌های لوکال توسعه‌دهندگان.
+- **معیار پذیرش اصلاح:**
+  - اضافه شدن کامل تگ سرویس `minio` به همراه پورت‌های `9000` و `9001` به فایل اصلی Docker Compose محیط توسعه.
+
+### GAP-08: استفاده از متغیرهای ناامن پیش‌فرض (Fallback)
+- **توضیحات:** در کلاس‌های سرویس MinIO بک‌اند و کلاینت پایتون، مقادیر پیش‌فرض هاردکدشده برای پسوردها و کلیدهای دسترسی وجود دارد که در صورت خرابی بارگذاری متغیرهای محیطی، برنامه بدون پرتاب خطا لود می‌شود.
+- **اثر امنیتی:** ریسک بسیار بالا در افشای پسوردها در محیط عملیاتی و رخنه به مخزن فایل‌های حساس پروژه.
+- **معیار پذیرش اصلاح:**
+  - حذف کامل رشته‌های ناامن پیش‌فرض از کد.
+  - برنامه باید در زمان راه‌اندازی (Bootstrap) در صورت عدم وجود هر یک از کلیدهای حیاتی باکت و استوریج با پرتاب خطا متوقف شود (Fail-Fast Behavior).
+
+### GAP-09: نبود تست‌های یکپارچگی اتصالات استوریج
+- **توضیحات:** اتصالات بین لایه ذخیره‌سازی، پایگاه داده و درایور MinIO در جریان تست‌های اتوماسیون جاری پروژه فاقد سناریوهای ممیزی کیفی دقیق و منظم است.
+- **اثر تضمین کیفیت:** نشت خطاهای امنیتی یا خطاهای تخصیص سهمیه به نسخه نهایی کدهای پروداکشن.
+- **معیار پذیرش اصلاح:**
+  - توسعه تست‌های آزمایشی یکپارچگی (Integration Tests) در جریان اجرای فاز یک مهندسی جهت تست و صحه‌گذاری جریان‌های گوناگون آپلود و دانلود در سطح ماژول‌ها.
+EOF
+
+# ── 5. STORAGE API INVENTORY ─────────────────────────────────────────────────
+echo "Generating: docs/generated/storage-api-inventory.md"
+cat << 'EOF' > docs/generated/storage-api-inventory.md
+# مستند رسمی واسط‌های برنامه‌نویسی پلتفرم ذخیره‌سازی (Storage API Inventory)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-API-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه هدف:** 1.1.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت واسط‌ها:** پیاده‌سازی شده / در حال ارتقا  
+
+این سند فهرست جامعی از تمام اندپوینت‌های فعال و پیشنهادی پلتفرم ذخیره‌سازی مرکزی زنیک را شامل متدهای احراز هویت، بدنه ورودی و فرمت خروجی توصیف می‌کند.
+
+---
+
+## ۱. فهرست اندپوینت‌های هسته ذخیره‌سازی (Core Storage APIs)
+
+پایه آدرس روت تمام اندپوینت‌ها: `/api/v1/storage`
+
+### ۱.۱. آپلود فایل جدید (Upload File)
+- **آدرس:** `POST /upload`
+- **فرمت ارسال:** `multipart/form-data`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`, `PermissionsGuard`
+- **مجوز دسترسی (Permission):** `files.upload`
+- **هدرهای اجباری:**
+  - `Authorization: Bearer <token>`
+  - `x-workspace-id: <workspace_uuid>`
+- **بدنه درخواست (Request Body):**
+  - فیلد باینری با نام `file` (حداکثر حجم ۱۰۰ مگابایت)
+- **پاسخ موفق (HTTP 201 Created):**
+  ```json
+  {
+    "success": true,
+    "data": {
+      "id": "1d8487b4-3580-4965-a827-6f77e68db35b",
+      "workspaceId": "019f75f0-xennic-45be-9ab6-267035eb4450",
+      "bucket": "documents",
+      "filename": "73bb1fc7-cf25-46f9-bb83-cb5b3d7c390a.pdf",
+      "originalName": "electrical_standards.pdf",
+      "extension": "pdf",
+      "mimeType": "application/pdf",
+      "size": 1548292,
+      "sizeHuman": "1.5 MB",
+      "checksum": "a5e954c278de...",
+      "uploadedBy": "user-uuid",
+      "createdAt": "2026-07-18T10:14:00.000Z",
+      "downloadUrl": "http://localhost:9000/documents/..."
+    }
+  }
+  ```
+
+### ۱.۲. لیست فایل‌های مستأجر (List Files)
+- **آدرس:** `GET /files`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`, `PermissionsGuard`
+- **مجوز دسترسی (Permission):** `files.read`
+- **پارامترهای جست‌وجو (Query Params):**
+  - `page`: شماره صفحه (پیش‌فرض: 1)
+  - `limit`: تعداد در هر صفحه (پیش‌فرض: 20)
+  - `bucket`: فیلتر بر اساس نام باکت (اختیاری)
+- **پاسخ موفق (HTTP 200 OK):**
+  ```json
+  {
+    "success": true,
+    "data": [
+      {
+        "id": "1d8487b4-...",
+        "workspaceId": "...",
+        "bucket": "documents",
+        "originalName": "electrical_standards.pdf",
+        "sizeHuman": "1.5 MB",
+        "createdAt": "2026-07-18T10:14:00.000Z"
+      }
+    ],
+    "meta": {
+      "page": 1,
+      "limit": 20,
+      "total": 1,
+      "totalPages": 1
+    }
+  }
+  ```
+
+### ۱.۳. دریافت جزئیات فایل و آدرس دانلود موقت (Get File Details & URL)
+- **آدرس:** `GET /files/:id`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`, `PermissionsGuard`
+- **مجوز دسترسی (Permission):** `files.read`
+- **پارامتر مسیر (Param):** `:id` شناسه فایل (UUID)
+- **پاسخ موفق (HTTP 200 OK):** بازگشت جزئیات کامل فایل مشابه آپلود به همراه لینک دانلود مستقیم یک‌ساعته (`downloadUrl`).
+
+### ۱.۴. دانلود مستقیم فایل (Direct Binary Stream Download)
+- **آدرس:** `GET /files/:id/download`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`
+- **مجوز دسترسی (Permission):** `files.read`
+- **پارامتر مسیر (Param):** `:id` شناسه فایل (UUID)
+- **نوع پاسخ:** جریان باینری (Buffer Stream) همراه با هدرهای `Content-Type` و `Content-Disposition`.
+
+### ۱.۵. حذف نرم فایل (Soft Delete)
+- **آدرس:** `DELETE /files/:id`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`, `PermissionsGuard`
+- **مجوز دسترسی (Permission):** `files.delete`
+- **پاسخ موفق:** `HTTP 204 No Content` (تغییر وضعیت رکورد دیتابیس به حذف شده).
+
+### ۱.۶. آمار فضای دیسک مصرفی (Storage Statistics)
+- **آدرس:** `GET /stats`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`
+- **مجوز دسترسی (Permission):** `files.read`
+- **پاسخ موفق (HTTP 200 OK):**
+  ```json
+  {
+    "success": true,
+    "data": {
+      "totalFiles": 142,
+      "totalSizeBytes": 538291048,
+      "totalSizeHuman": "513.3 MB"
+    }
+  }
+  ```
+
+### ۱.۷. بررسی وضعیت اتصال به آبجکت استوریج (Health Check)
+- **آدرس:** `GET /health`
+- **محافظت امنیتی (Guards):** فاقد گارد (عمومی)
+- **پاسخ موفق (HTTP 200 OK):**
+  ```json
+  {
+    "success": true,
+    "data": {
+      "status": "ok",
+      "buckets": ["public", "private", "reports", "documents", "engineering", "ai"]
+    }
+  }
+  ```
+
+---
+
+## ۲. اندپوینت‌های پیشنهادی فاز یک هدف (Proposed APIs)
+
+جهت رفع نواقص کشف‌شده در بخش ممیزی، اندپوینت‌های زیر در فاز یک اضافه خواهند شد:
+
+### ۲.۱. دسترسی بدون فیلتر به دارایی‌های عمومی (Public Asset safe Route)
+- **آدرس:** `GET /public/:id`
+- **محافظت امنیتی (Guards):** **فاقد هرگونه گارد احراز هویت (Unauthenticated)**
+- **پارامتر مسیر (Param):** `:id` شناسه فایل عمومی
+- **شرح عملکرد:** این متد فورا فایل را از باکت عمومی `public` فراخوانی کرده و به مرورگر استریم می‌کند. در صورتی که فایل ارجاع داده شده در باکتی غیر از باکت `public` ذخیره شده باشد، سرویس موظف به بازگرداندن خطای `403 Forbidden` است تا از نشت اطلاعات حساس مستأجرین به خارج جلوگیری شود.
+
+### ۲.۲. آپلود نسخه جدید فایل (Upload New Version)
+- **آدرس:** `POST /files/:id/versions`
+- **فرمت ارسال:** `multipart/form-data`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`, `PermissionsGuard`
+- **مجوز دسترسی (Permission):** `files.upload`
+- **بدنه درخواست (Request Body):** فیلد باینری `file` با سایز مجاز.
+- **پاسخ موفق (HTTP 201 Created):**
+  ```json
+  {
+    "success": true,
+    "data": {
+      "fileId": "1d8487b4-...",
+      "version": 2,
+      "path": "2026/07/version_file_name.pdf",
+      "checksum": "checksum_v2...",
+      "createdAt": "2026-07-18T11:00:00.000Z"
+    }
+  }
+  ```
+
+### ۲.۳. مشاهده تاریخچه نسخه‌ها (List Versions)
+- **آدرس:** `GET /files/:id/versions`
+- **محافظت امنیتی (Guards):** `JwtAuthGuard`, `WorkspaceGuard`
+- **مجوز دسترسی (Permission):** `files.read`
+- **پاسخ موفق (HTTP 200 OK):** لیست تمام نسخه‌های آرشیو شده به همراه شماره نسخه و تاریخ ایجاد جهت بازیابی یا لود نسخه‌های قدیمی‌تر.
+EOF
+
+# ── 6. DATABASE INVENTORY ─────────────────────────────────────────────────────
+echo "Generating: docs/generated/storage-database-inventory.md"
+cat << 'EOF' > docs/generated/storage-database-inventory.md
+# مستند ساختار پایگاه داده پلتفرم ذخیره‌سازی (Storage Database Inventory)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-DB-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت پایگاه داده:** آماده توسعه فاز یک  
+
+این مستند حاوی شرح تفصیلی جداول موجود و اصلاحات ساختاری لازم در سطح لایه داده پروژه زنیک (Prisma ORM) به همراه تحلیل کلیدها، نمایه‌ها و گاردها می‌باشد.
+
+---
+
+## ۱. ساختار جداول موجود در فایل شمای پریزما (`prisma/schema.prisma`)
+
+در حال حاضر دو جدول مستقل برای نگهداری فایل‌ها و نسخه‌ها تعریف شده است:
+
+### ۱.۱. جدول فایل‌ها (`files`)
+- **نام مدل:** `files`
+- **هدف:** نگهداری ردیف‌های فراداده فایل‌های آپلود شده در سیستم.
+
+| نام ستون در دیتابیس | نوع داده در Prisma | نوع داده در PostgreSQL | وضعیت Nullable | توضیحات بیزینسی |
+| :--- | :--- | :--- | :--- | :--- |
+| **`id`** | `String` | `uuid` | خیر (PK) | شناسه منحصربه‌فرد کلید اصلی فایل |
+| **`workspace_id`** | `String` | `text` | خیر (FK) | ارجاع به جدول فضاهای کاری (`workspaces`) |
+| **`bucket`** | `String` | `text` | خیر | نام باکت هدف در MinIO (محدود به ۶ نام استاندارد) |
+| **`path`** | `String` | `text` | خیر | مسیر نسبی فایل در باکت پس از شناسه مستأجر |
+| **`filename`** | `String` | `text` | خیر | نام فیزیکی فایل تولیدشده توسط سیستم (UUID) |
+| **`original_name`**| `String` | `text` | خیر | نام اصلی فایل که توسط کاربر ارسال شده است |
+| **`extension`** | `String` | `text` | خیر | پسوند فایل (مانند pdf, png) به صورت حروف کوچک |
+| **`mime_type`** | `String` | `text` | خیر | نوع استاندارد رسانه‌ای فایل (Mime-Type) |
+| **`size`** | `BigInt` | `bigint` | خیر | سایز فایل بر حسب بایت |
+| **`checksum`** | `String?` | `text` | **بله** | هش تولید شده از روی فایل (مبتنی بر SHA-256) |
+| **`uploaded_by`** | `String` | `text` | خیر (FK) | شناسه کاربر آپلودکننده فایل (`users`) |
+| **`created_at`** | `DateTime` | `timestamp` | خیر | زمان بارگذاری فایل (پیش‌فرض: `now()`) |
+| **`deleted_at`** | `DateTime?` | `timestamp` | **بله** | زمان حذف نرم فایل در صورت مخفی‌سازی |
+
+- **نمایه‌ها (Indexes):**
+  - `@@index([workspace_id])` جهت افزایش سرعت جست‌وجوی فایل‌های متعلق به یک مستأجر.
+  - `@@index([uploaded_by])` جهت فیلتر و گزارش‌گیری فایل‌های آپلودی هر کاربر.
+  - `@@index([mime_type])` برای بهینه‌سازی فیلتر نوع فایل‌ها.
+
+### ۱.۲. جدول نسخه‌ها (`file_versions`)
+- **نام مدل:** `file_versions`
+- **هدف:** ثبت ارجاع‌های نسخه‌های تاریخی تغییریافته فایل‌ها.
+
+| نام ستون در دیتابیس | نوع داده در Prisma | نوع داده در PostgreSQL | وضعیت Nullable | توضیحات بیزینسی |
+| :--- | :--- | :--- | :--- | :--- |
+| **`id`** | `String` | `uuid` | خیر (PK) | کلید اصلی شناسه نسخه |
+| **`file_id`** | `String` | `text` | خیر (FK) | کلید خارجی متصل به شناسه فایل پدر (`files.id`) |
+| **`version`** | `Int` | `integer` | خیر | شماره افزایشی نسخه فایل (شروع از ۱) |
+| **`path`** | `String` | `text` | خیر | مسیر باینری فایل نسخه در باکت |
+| **`checksum`** | `String?` | `text` | **بله** | هش شناسه یکپارچگی فایل نسخه جدید |
+| **`created_at`** | `DateTime` | `timestamp` | خیر | زمان آپلود شدن نسخه جدید |
+
+- **قوانین حذف ردیف (Cascade Delete):**
+  - ارتباط با `files` به صورت `onDelete: Cascade` است؛ به این معنی که با حذف سخت یک فایل اصلی، تمام ردیف‌های مربوط به نسخه‌های قدیمی‌تر آن نیز به صورت فیزیکی از جدول دیتابیس حذف خواهند شد.
+
+---
+
+## ۲. گسست دیتابیسی و نیازهای اصلاح شمای ارتباطی (Database Gap Registry)
+
+برای جاری ساختن معماری جامع پلتفرم، تغییرات ساختاری زیر روی شمای پایگاه داده طراحی و پیشنهاد می‌شود:
+
+```
+  ┌───────────────────────────────────────────────────────────┐
+  │                        workspaces                         │
+  └─────────────────────────────┬─────────────────────────────┘
+                                │ (logo_file_id ──► files.id)
+                                ▼
+  ┌───────────────────────────────────────────────────────────┐
+  │                           files                           │
+  └─────────────────────────────▲─────────────────────────────┘
+                                │ (file_id ──► files.id)
+         ┌──────────────────────┼──────────────────────┐
+         ▼                      ▼                      ▼
+┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
+│   file_versions  │  │knowledge_document│  │   calculations   │
+└──────────────────┘  └──────────────────┘  └──────────────────┘
+```
+
+### الف) اتصال مستقیم کارخانه دانش به جدول فایل‌ها
+در مدل `knowledge_documents` ستون `storage_path` باید به عنوان فیلد فرعی در نظر گرفته شده و فیلد اصلی ارتباطی به صورت زیر تعریف شود:
+```prisma
+model knowledge_documents {
+  // ...
+  file_id      String?           @unique
+  file         files?            @relation(fields: [file_id], references: [id])
+  // ...
+}
+```
+
+### ب) اتصال پیوست‌های محاسبات مهندسی
+در مدل `calculations` ورودی‌ها و خروجی‌های محاسباتی به جدول فایل‌ها لینک داده می‌شوند:
+```prisma
+model calculations {
+  // ...
+  input_file_id   String?
+  output_file_id  String?
+  
+  input_file      files?   @relation("CalculationInputs", fields: [input_file_id], references: [id])
+  output_file     files?   @relation("CalculationOutputs", fields: [output_file_id], references: [id])
+  // ...
+}
+```
+
+### ج) تصویر لوگوی فضاهای کاری و آواتار کاربران
+- **در مدل `workspaces`:**
+  ```prisma
+  logo_file_id    String?   @unique
+  logo_file       files?    @relation("WorkspaceLogo", fields: [logo_file_id], references: [id])
+  ```
+- **در مدل `users`:**
+  ```prisma
+  avatar_file_id  String?   @unique
+  avatar_file     files?    @relation("UserAvatar", fields: [avatar_file_id], references: [id])
+  ```
+
+---
+
+## ۳. سناریوی اجرای پچ مهاجرت دیتابیس (DB Migration Plan)
+
+از آنجایی که سیستم در حال توسعه است، برای اعمال تغییرات بدون گسست یا خرابی داده‌های موجود، مهاجرت‌ها به صورت زیر تایید صلاحیت می‌شوند:
+1. **گام اول:** تولید پچ پریزما جهت ایجاد ارتباطات جدید به شکل فیلدهای اختیاری (`Optional / Nullable`). این اقدام جلوی بروز هرگونه عدم انطباق روی رکوردهای قبلی دیتابیس را می‌گیرد.
+2. **گام دوم:** اجرای اسکریپت بک‌آپ‌گیری از داده‌ها به صورت خودکار قبل از اعمال مهاجرت به وسیله اسکریپت‌های زیرساختی ریکاوری.
+3. **گام سوم:** اعمال مهاجرت ساختاری پایگاه داده از طریق اجرای دستور زیر به کمک فرآیندهای امن توسعه:
+   `pnpm prisma migrate dev --name add_unified_storage_relations`
+EOF
+
+# ── 7. PERMISSION MATRIX ─────────────────────────────────────────────────────
+echo "Generating: docs/generated/storage-permission-matrix.md"
+cat << 'EOF' > docs/generated/storage-permission-matrix.md
+# ماتریس امنیتی و دسترسی‌های پلتفرم ذخیره‌سازی (Storage Permission Matrix)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-PERM-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت:** مصوب (APPROVED)  
+
+این مستند ماتریس دقیق دسترسی‌ها، قوانین امنیتی، نقش‌ها و مرزهای ایزوله‌سازی را در سطح پلتفرم ذخیره‌سازی مرکزی زنیک مشخص می‌کند.
+
+---
+
+## ۱. ماتریس دسترسی به تفکیک نقش‌ها (Role-Based Access Control)
+
+ماتریس زیر نشان‌دهنده دسترسی نقش‌های مختلف سازمان به امکانات و باکت‌های گوناگون پلتفرم است. گارد کنترل دسترسی سازمان (`RbacModule` و `PermissionsGuard`) موظف به کنترل و اعمال دقیق این مجوزهاست.
+
+| قابلیت / عملیات روی فایل | مالک فضای کاری (Owner) | مدیر (Admin) | عضو (Member) | مهمان (Guest) | بازدیدکننده عمومی (Anonymous) | باکت هدف | مجوز مرتبط در RBAC |
+| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :--- |
+| **بارگذاری فایل عمومی** | ✅ | ✅ | ✅ | ❌ | ❌ | `public` | `files.upload` |
+| **بارگذاری فایل خصوصی** | ✅ | ✅ | ✅ | ❌ | ❌ | `private`, `engineering` | `files.upload` |
+| **بارگذاری اسناد متنی** | ✅ | ✅ | ✅ | ❌ | ❌ | `documents` | `files.upload` |
+| **مشاهده دارایی‌های عمومی**| ✅ | ✅ | ✅ | ✅ | ✅ (بدون لاگین) | `public` | فاقد مجوز (سراسری) |
+| **دانلود فایل‌های محرمانه**| ✅ | ✅ | ✅ (فقط دپارتمان) | ❌ | ❌ | `private`, `reports` | `files.read` |
+| **ایجاد نسخه جدید فایل** | ✅ | ✅ | ✅ | ❌ | ❌ | همه باکت‌ها | `files.upload` |
+| **حذف نرم فایل (Soft Delete)**| ✅ | ✅ | ❌ | ❌ | ❌ | همه باکت‌ها | `files.delete` |
+| **حذف سخت فایل (Hard Delete)**| ✅ | ❌ | ❌ | ❌ | ❌ | همه باکت‌ها | `files.admin` |
+| **مشاهده آمار مصرف فضا** | ✅ | ✅ | ✅ | ❌ | ❌ | — | `files.read` |
+| **اجرای دستی پاک‌سازی** | ✅ | ❌ | ❌ | ❌ | ❌ | — | `files.admin` |
+
+*توضیح علامت‌ها:*  
+- ✅ مجاز  
+- ❌ غیرمجاز / مسدود  
+
+---
+
+## ۲. قوانین ساختاری ایزوله‌سازی چندمستأجری (Tenant Isolation Rules)
+
+### ۲.۱. ایزوله‌سازی در دیتابیس (Logical DB Isolation)
+- تمام رکوردهای جدول `files` باید با فیلد `workspace_id` فیلتر شوند.
+- متد‌های مخزن داده (Storage Repository) حق بازگرداندن ردیفی را ندارند که مقدار `workspace_id` آن با شناسه فضای کاری جاری ثبت‌شده در هدر درخواست (`req.workspaceId` استخراج‌شده از گارد Workspace) تطابق ندارد.
+
+### ۲.۲. ایزوله‌سازی در لایه باینری (Physical S3 Isolation)
+- برای تمام فایل‌های محرمانه (باکت‌های `private`، `reports`، `documents`، `engineering`، `ai`)، پیشوند مسیر کلید آبجکت در MinIO الزاما به صورت زیر خواهد بود:
+  `{workspaceId}/{bucketName}/{fileId}.{extension}`
+- این قانون فیزیکی، مانع از تداخل دسترسی و لود فایل‌ها بین مستأجرین می‌شود.
+
+---
+
+## ۳. فرآیند فنی امنیت و اعتبارسنجی (Security Validation Process)
+
+```
+[درخواست کاربر] ──► [تایید احراز هویت توکن] ──► [بررسی شناسه Workspace] ──► [بررسی فیلتر باکت عمومی/خصوصی] ──► [استریم فایل]
+```
+
+1. **شناسایی باکت مقصد در زمان دانلود:**
+   - سرویس اصلی ابتدا رکوردهای پایگاه داده فایل را با فیلتر شناسه مستأجر فرامی‌خواند.
+   - اگر باکت فایل مرجع برابر با `public` باشد، بلافاصله اجازه دسترسی و بازگرداندن لینک صادر می‌شود.
+   - اگر باکت فایل غیرعمومی باشد، گارد بررسی عضویت کاربر در آن مستأجر (`workspace_id` فایل با `req.workspaceId` هماهنگ باشد) فعال شده و پس از اعتبارسنجی سطح دسترسی و مجوز، کلید آبجکت برای تولید آدرس دانلود یا دریافت مستقیم باینری فراخوانی می‌شود.
+2. **بررسی سهمیه‌های امنیتی در زمان آپلود:**
+   - پیش از درج بایت‌ها در MinIO، گارد سیستم حجم کل باکت مستأجر را بررسی می‌کند.
+   - اگر حجم آپلود جدید باعث تجاوز از سقف مجاز مستأجر شود، سیستم با ارسال پیغام خطای ۴۰۳ درخواست را متوقف می‌کند تا از حملات مسدودسازی حافظه (Denial of Storage) پیشگیری نماید.
+3. **محدودسازی دسترسی به متدهای حذف:**
+   - حذف نرم فایل‌ها فقط به نقش‌های سطح بالا (Owners and Admins) محدود می‌شود.
+   - فرآیند حذف فیزیکی و سخت فایل‌ها منحصرا به مالک سازمان (Owner) تعلق داشته و در لایه کنترلر با گارد مجوز `files.admin` محافظت می‌گردد.
+EOF
+
+# ── 8. LIFECYCLE STATE MACHINE ───────────────────────────────────────────────
+echo "Generating: docs/generated/storage-lifecycle-state-machine.md"
+cat << 'EOF' > docs/generated/storage-lifecycle-state-machine.md
+# مستند ماشین وضعیت چرخه حیات فایل و سند (Storage Lifecycle State Machines)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-LIFE-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت:** تایید شده (APPROVED)  
+
+این مستند مدل چرخه حیات (Lifecycle) و نمودار ماشین وضعیت رکوردهای فیزیکی و متادیتای موجودیت‌های پلتفرم استوریج را مشخص می‌کند.
+
+---
+
+## ۱. چرخه حیات فایل و نسخه‌ها (File and Version Lifecycle)
+
+پایه‌ای‌ترین چرخه حیات متعلق به موجودیت فایل خام (`FileEntity`) است. این ماشین وضعیت وظیفه هدایت بایت‌های باینری از زمان ورود به دیسک استوریج تا نابودی کامل را عهده‌دار است.
+
+```mermaid
+stateDiagram-v2
+    [*] --> Uploaded : بارگذاری جریان بایت‌ها
+    Uploaded --> Active : تایید متادیتای دیتابیس
+    Active --> Active : آپلود نسخه جدید (FileVersion)
+    Active --> Archived : انتقال به Cold Storage (پس از ۶ ماه)
+    Active --> SoftDeleted : درخواست حذف نرم توسط کاربر
+    Archived --> SoftDeleted : درخواست حذف نرم توسط کاربر
+    SoftDeleted --> Active : بازیابی فایل (Restore)
+    SoftDeleted --> Purged : گذشت ۳۰ روز (Scheduler)
+    Purged --> [*] : حذف فیزیکی از MinIO و دیتابیس
+```
+
+### شرح جزئیات وضعیت‌ها و انتقال‌ها (Transitions):
+- **Uploaded:** بایت‌ها با موفقیت در باکت استاندارد MinIO قرار گرفته‌اند اما تراکنش دیتابیس هنوز تایید نشده است.
+- **Active:** تراکنش دیتابیس با موفقیت ثبت شده و فایل به عنوان منبع زنده برای سیستم شناخته می‌شود.
+- **Archived:** فایل به کلاس ذخیره‌سازی ابری ارزان‌تر انتقال یافته است. لینک‌های دانلود همچنان معتبر هستند اما لود اولیه ممکن است با کمی تاخیر انجام شود.
+- **SoftDeleted:** فیلد `deleted_at` با تاریخ زمان فعلی مقداردهی می‌شود. فایل از جست‌وجوهای کلاینت حذف گردیده ولی بایت‌های آن در MinIO محفوظ می‌ماند.
+- **Purged (Hard-Deleted):** فرآیند نهایی نابودی فیزیکی. در این مرحله بایت‌های شیء از MinIO حذف سخت شده و رکوردهای دیتابیس به صورت Cascade پاکسازی می‌گردند.
+
+---
+
+## ۲. چرخه حیات اسناد کارخانه دانش (Knowledge Document Lifecycle)
+
+موجودیت سند هوش مصنوعی (`KnowledgeDocument`) دارای چرخه‌ای پیچیده و غنی‌تر است که وضعیت پردازش اسناد متنی را مشخص می‌سازد.
+
+```mermaid
+stateDiagram-v2
+    [*] --> Registered : بارگذاری متادیتای فایل در دیتابیس
+    Registered --> Processing : آغاز فرآیند لوله پردازش اسناد
+    Processing --> Parsed : استخراج محتوای متنی (Text Extraction)
+    Parsed --> Classified : تشخیص دپارتمان و استانداردهای سند
+    Classified --> Chunked : تقسیم محتوا به تکه‌های دانش (Chunking)
+    Chunked --> Embedded : تحلیل برداری و تولید Embeddings
+    Embedded --> Published : انتشار در مخزن دانش زنیک (RAG Ready)
+    
+    Processing --> Failed : خطای پردازش فایل / ناسازگاری پکیج
+    Parsed --> Failed : خطای استخراج متن
+    Classified --> Failed : خطای هوش مصنوعی و عدم تحلیل سند
+    Embedded --> Failed : خطای سرویس تولید بردار
+    
+    Failed --> Processing : تلاش مجدد (Retry)
+    Published --> Deleted : حذف سند توسط مدیر
+    Deleted --> [*] : پاکسازی
+```
+
+### جزئیات گام‌های شکست پردازش (Failed State Handling):
+- در صورت وقوع هرگونه خطای سیستمی (مثلاً قطعی کلاینت پایتون یا اورفلو شدن توکن‌های ال‌ال‌ام)، سند در وضعیت **Failed** قرار می‌گیرد و پیغام خطا در ستون `error_message` ثبت می‌شود.
+- سیستم هوشمند کارخانه دانش دارای مکانیزم تلاش مجدد خودکار (Auto-Retry) تا سقف ۳ مرتبه در پس‌زمینه (توسط صف‌های BullMQ) است.
+
+---
+
+## ۳. چرخه حیات دارایی‌های عمومی برندینگ (Brand Asset Lifecycle)
+
+تصاویری که به عنوان لوگو، آیکون و آواتارها بارگذاری می‌شوند، مستقیماً با لایه نمایش فرانت‌اند در ارتباطند و چرخه‌ای کاملاً ایمن و سبک دارند.
+
+```mermaid
+stateDiagram-v2
+    [*] --> UploadedPublic : آپلود در باکت public
+    UploadedPublic --> VerifiedUI : بهینه‌سازی فرمت تصویر (فرمت WebP)
+    VerifiedUI --> ActiveAsset : تخصیص به عنوان لوگو/آواتار کاربر
+    ActiveAsset --> Replaced : بارگذاری لوگوی جدید توسط مستأجر
+    Replaced --> PurgedAsset : حذف فیزیکی بایت‌ها از باکت عمومی
+    PurgedAsset --> [*]
+```
+
+### جزئیات بهینه‌سازی تصاویر (VerifiedUI):
+- در زمان ورود تصاویر عمومی، سیستم بک‌اند موظف به اجرای یک پردازشگر تصویر سبک جهت بهینه‌سازی حجم و فرمت تصاویر به فرمت‌های وب‌پسند مانند `image/webp` یا `image/png` است تا پهنای باند شبکه بهینه‌سازی شود.
+- با آپلود لوگوی جدید، لوگوی قبلی مستقیما به وضعیت **Replaced** وارد شده و بایت‌های فیزیکی آن پس از ۷ روز برای جلوگیری از گسست کش مرورگرها، به شکل سخت از باکت عمومی پاکسازی می‌گردند.
+EOF
+
+# ── 9. STORAGE RISK REGISTER ─────────────────────────────────────────────────
+echo "Generating: docs/generated/storage-risk-register.md"
+cat << 'EOF' > docs/generated/storage-risk-register.md
+# دفتر ثبت مخاطرات پلتفرم ذخیره‌سازی زنیک (Storage Risk Register)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-RISK-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت:** مصوب (APPROVED)  
+
+این مستند فهرست کاملی از مخاطرات امنیتی، زیرساختی و عملیاتی مرتبط با پلتفرم ذخیره‌سازی مرکزی زنیک را به همراه راهکارهای پیشگیری و برنامه‌های مقابله ثبت کرده است.
+
+---
+
+## ۱. جدول ماتریس ارزیابی مخاطرات (Risk Assessment Matrix)
+
+سطح مخاطره بر اساس فرمول: **شدت (Impact) × احتمال (Probability)** محاسبه می‌شود (بازه ۱ تا ۲۵):
+- ۱ تا ۵: ریسک پایین
+- ۶ to ۱۲: ریسک متوسط
+- ۱۵ to ۲۵: ریسک بحرانی و بالا
+
+| شناسه | عنوان مخاطره | احتمال (1-5) | شدت (1-5) | نمره ریسک (1-25) | سطح ریسک | طبقه‌بندی موضوعی |
+| :--- | :--- | :---: | :---: | :---: | :---: | :--- |
+| **RISK-01** | نشت اطلاعات و نقض ایزوله‌سازی مستأجرین | ۲ | ۵ | **۱۰** | متوسط | امنیت داده‌ها |
+| **RISK-02** | کرش قطعی لوله پردازش به علت تضاد امضای متدها| ۵ | ۵ | **25** | **بحرانی** | پایداری نرم‌افزار |
+| **RISK-03** | هرج‌ومرج و انحراف کلاینت‌ها در ساخت باکت‌های موازی| ۴ | ۴ | **۱۶** | **بحرانی** | انحراف معماری |
+| **RISK-04** | حملات پر کردن دیسک سرور (Denial of Storage) | ۳ | ۴ | **۱۲** | متوسط | پایداری زیرساخت |
+| **RISK-05** | تجمع فایل‌های یتیم فیزیکی (Byte Leak) در MinIO | ۴ | ۳ | **۱۲** | متوسط | هزینه‌های عملیاتی |
+| **RISK-06** | بهره‌برداری نفوذگران از پسوردهای پیش‌فرض کد | ۲ | ۵ | **۱۰** | متوسط | امنیت زیرساخت |
+| **RISK-07** | از دسترس خارج شدن بستر ذخیره‌سازی شیء (MinIO) | ۲ | ۵ | **۱۰** | متوسط | پایداری عملیاتی |
+
+---
+
+## ۲. مشخصات تفصیلی مخاطرات و راهکارهای کاهش (Risk Specifications & Mitigations)
+
+### RISK-01: نشت اطلاعات و نقض ایزوله‌سازی مستأجرین
+- **تاثیر نشت:** دسترسی غیرمجاز کاربر فضای کاری الف به اسناد فنی محرمانه فضای کاری ب.
+- **راهکار پیشگیری (Mitigation):**
+  - اعمال فیلترینگ منطقی در سطح کوئری‌های دیتابیس دسامبر با کمک گارد Workspace.
+  - پیاده‌سازی متد اعتبارسنجی همپوشانی Workspace در تمام سطوح دانلود باینری.
+- **برنامه مقابله (Contingency):** تعلیق دسترسی موقت به اندپوینت‌های دانلود عمومی و بررسی تمام رکوردهای لاگ دسترسی در سیستم مانیتورینگ.
+
+### RISK-02: کرش قطعی لوله پردازش به علت تضاد امضای متدها
+- **تاثیر نشت:** توقف صد درصدی فرآیند بارگذاری اسناد و فیل شدن تراکنش‌های هوش مصنوعی.
+- **راهکار پیشگیری (Mitigation):**
+  - تصحیح امضای متدهای کلاس سرویس ذخیره‌سازی بر اساس اینترفیس یکتا در پچ فاز یک.
+  - ممنوع کردن ثبت‌های مستقیمی که فاقد تست‌های ارزیابی کیفی هستند.
+- **برنامه مقابله (Contingency):** غیرفعال‌سازی موقت ماژول Intake دانش و نمایش پیغام در حال به‌روزرسانی به کاربران.
+
+### RISK-03: هرج‌ومرج و انحراف کلاینت‌ها در ساخت باکت‌های موازی
+- **تاثیر نشت:** پراکندگی اسناد فیزیکی، عدم دسترسی پایتون به بایت‌های آپلودی وب و فیل شدن فرآیندهای RAG.
+- **راهکار پیشگیری (Mitigation):**
+  - لغو کامل کدهای مستقل کلاینت MinIO در سرویس پایتون.
+  - تحمیل کدهای استانداردی آدرس‌دهی و باکت‌های هسته زنیک.
+- **برنامه مقابله (Contingency):** اجرای دستی اسکریپت جابجایی فایل‌ها و همگام‌سازی باکت‌های متداخل.
+
+### RISK-04: حملات پر کردن دیسک سرور (Denial of Storage)
+- **تاثیر نشت:** آپلود فایل‌های پرحجم توسط کاربران متخاصم و تمام شدن فضای کل هاست استوریج.
+- **راهکار پیشگیری (Mitigation):**
+  - تعریف سهمیه (Quota) سخت‌گیرانه برای هر Workspace متناسب با پکیج اشتراک.
+  - اعتبارسنجی حجم قبل از پذیرش و ذخیره‌سازی جریان فایل.
+- **برنامه مقابله (Contingency):** مسدودسازی آنی دسترسی آپلود فضای کاری خاطی و ارتقای فضای هارددیسک به صورت داینامیک.
+
+### RISK-05: تجمع فایل‌های یتیم فیزیکی (Byte Leak) در MinIO
+- **تاثیر نشت:** انباشت ترابایت‌ها فایل بلااستفاده و حذف نرم شده در MinIO بدون پاکسازی فیزیکی.
+- **راهکار پیشگیری (Mitigation):**
+  - توسعه و اجرای منظم زمان‌بندی روزانه (Cron Job) جهت پاکسازی فیزیکی رکوردهایی که بیش از ۳۰ روز از حذف نرم آن‌ها گذشته است.
+- **برنامه مقابله (Contingency):** اجرای دستی دستور پاکسازی با استفاده از ابزار کلاینت MinIO (`mc`).
+
+### RISK-06: بهره‌برداری نفوذگران از پسوردهای پیش‌فرض کد
+- **تاثیر نشت:** دسترسی نفوذگران به اطلاعات حیاتی به دلیل استفاده برنامه از کدهای پیش‌فرض هاردکدشده امنیتی در صورت نبود متغیرهای محیطی.
+- **راهکار پیشگیری (Mitigation):**
+  - حذف کامل فالبک‌های پیش‌فرض امنیتی در کدهای بک‌اند و پایتون.
+  - پیاده‌سازی الگوهای بررسی الزامی متغیرهای محیطی در استارت‌آپ برنامه و Fail-Fast.
+- **برنامه مقابله (Contingency):** تغییر اضطراری کلیدها و پسوردهای MinIO و راه‌اندازی مجدد تمام ران‌تایم‌های پروژه.
+
+### RISK-07: از دسترس خارج شدن بستر ذخیره‌سازی شیء (MinIO)
+- **تاثیر نشت:** اختلال همه‌جانبه در لود تصاویر، آواتارها، خروجی‌های محاسباتی و توقف کارهای پردازش اسناد.
+- **راهکار پیشگیری (Mitigation):**
+  - پیاده‌سازی کلاستر توزیع‌شده MinIO به همراه مکانیزم همگام‌سازی چندمرکزه (Replication).
+- **برنامه مقابله (Contingency):** استفاده از ویژگی ذخیره‌سازی محلی موقت (Local Disk Cache Backing) تا زمان اتصال مجدد آبجکت استوریج.
+EOF
+
+# ── 10. STORAGE TEST GAP MATRIX ──────────────────────────────────────────────
+echo "Generating: docs/generated/storage-test-gap-matrix.md"
+cat << 'EOF' > docs/generated/storage-test-gap-matrix.md
+# ماتریس شکاف تست و سنجش پایداری استوریج (Storage Test Gap Matrix)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-TESTS-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت سنجش:** ممیزی شده (IN_REVIEW)  
+
+این مستند شکاف‌های موجود در پوشش تست‌های کیفیت و پایداری (Test Coverage Gaps) سیستم ذخیره‌سازی را به همراه طرح سناریوهای تستی معتبر توصیف می‌کند.
+
+---
+
+## ۱. ارزیابی پوشش تست‌های موجود (Current Testing Coverage Audit)
+
+- **تست‌های واحد (Unit Tests) در ماژول استوریج:** غیرفعال یا ناقص.
+- **تست‌های یکپارچگی (Integration Tests) در ماژول استوریج:** هیچ تست یکپارچگی خودکاری بین کلاینت MinIO و دیتابیس وجود ندارد.
+- **تست‌های انتها به انتها (E2E Tests):** فاقد پوشش تست خودکار روی بارگذاری بایت‌ها به صورت Multipart.
+- **پوشش تست‌های تلاقی دامنه‌ای (Cross-Domain Test Coverage):** پردازش اسناد کارخانه دانش بدون تست‌های ارزیابی اتصالات استوریج پیاده‌سازی شده که منجر به نادیده گرفتن باگ کرش امضای متدها شده است.
+
+---
+
+## ۲. سناریوهای تست پیشنهادی جهت پوشش کامل شکاف‌ها (Required Test Cases)
+
+جهت تضمین پایداری معماری هدف، سناریوهای تستی زیر باید به طور کامل پیاده‌سازی شوند:
+
+### ۲.۱. مجموعه تست‌های واحد (Unit Test Cases)
+
+#### سناریو UT-01: بررسی اعتبارسنجی حجم مجاز و سهمیه مستأجر (Storage Quota Validation)
+- **شرح تست:** ارسال درخواستی با حجمی بزرگتر از سهمیه باقی‌مانده Workspace.
+- **شرایط تست (Mock Requirements):** شبیه‌سازی خروجی متد `getTotalSize` در مخزن داده دیتابیس.
+- **معیار پذیرش (Assertion):** سیستم باید با پرتاب خطای `ForbiddenException` یا `BadRequestException` آپلود را مسدود کرده و داده‌ای ذخیره نکند.
+
+#### سناریو UT-02: بررسی تخصیص خودکار باکت بر اساس Mime-Type
+- **شرح تست:** اعتبارسنجی خروجی متد خصوصی تشخیص باکت بر اساس پسوند و نوع رسانه.
+- **معیار پذیرش (Assertion):** پسوند `pdf` به باکت `documents`، پسوند `png` به باکت `public` و پسوندهای ناشناس مهندسی به باکت `engineering` ارجاع یابند.
+
+---
+
+### ۲.۲. مجموعه تست‌های یکپارچگی (Integration Test Cases)
+
+#### سناریو IT-01: تعامل با درایور شبیه‌سازی‌شده MinIO (MinIO Service Integration)
+- **شرح تست:** تست متدهای اصلی `uploadBuffer`، `getObject` و `deleteObject` روی سرویس واقعی یا کانتینر موقت MinIO (تست داکر کامپوز ایزوله).
+- **معیار پذیرش (Assertion):**
+  - ایجاد موفقیت‌آمیز باکت‌ها در زمان استارت‌آپ.
+  - همگام‌سازی کامل بایت‌های ورودی با هش فایل نهایی ذخیره شده.
+
+#### سناریو IT-02: تست اتصال کارخانه دانش و پلتفرم ذخیره‌سازی مرکزی
+- **شرح تست:** پردازش سند در کلاس `DocumentIntakeService` با استفاده از اینترفیس بازنویسی شده `'IStorageService'`.
+- **معیار پذیرش (Assertion):** عدم وقوع هرگونه خطای نوع داده یا تضاد آرگومان در متد `upload` و تایید ذخیره فایل در باکت استاندارد `documents` دیتابیس.
+
+---
+
+### ۲.۳. مجموعه تست‌های امنیتی و چندمستأجری (Security & Multi-Tenancy Tests)
+
+#### سناریو ST-01: جلوگیری از دسترسی بین‌مستأجری به فایل‌های خصوصی
+- **شرح تست:** تلاش برای دانلود یا دسترسی به فایل با شناسه معتبر متعلق به Workspace A توسط کاربری که توکن معتبر و شناسه Workspace B را دارد.
+- **معیار پذیرش (Assertion):** سیستم باید فورا خطای `ForbiddenException` یا `NotFoundException` صادر کرده و اجازه دانلود فایل را ندهد.
+
+#### سناریو ST-02: بررسی دسترسی ایمن بدون لاگین به باکت عمومی
+- **شرح تست:** دانلود لوگوی سازمان از آدرس فاقد گارد `/storage/public/:id`.
+- **معیار پذیرش (Assertion):** لود موفقیت‌آمیز فایل بدون نیاز به توکن JWT؛ در حالی که برای تلاش به همان فایل با باکت تغییریافته به `private` خطای ۴۰۱ صادر شود.
+
+---
+
+## ۳. شاخص عبور از گیت کیفیت تست (Quality Gate Metrics)
+
+پیاده‌سازی نهایی فاز یک پلتفرم زمانی به وضعیت **COMPLETED** تغییر می‌یابد که:
+1. پوشش تست‌های خطوط جدید کد پلتفرم استوریج به **حداقل ۸۵٪** برسد.
+2. تمام آزمون‌های امنیتی و چندمستأجری (چهار سناریوی ST-01 و ST-02 به همراه آزمون‌های یکپارچگی هوش مصنوعی) با موفقیت پاس شوند (Green Test Suites).
+3. تست‌های نهایی در فرآیند گیت‌هاب اکشنز (CI/CD Pipeline) ثبت شده و به درستی اجرا شوند.
+EOF
+
+# ── 11. STORAGE MIGRATION STRATEGY ───────────────────────────────────────────
+echo "Generating: docs/generated/storage-migration-strategy.md"
+cat << 'EOF' > docs/generated/storage-migration-strategy.md
+# راهبرد مهاجرت داده‌ها و همگرایی پلتفرم ذخیره‌سازی (Storage Migration Strategy)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-MIGRATE-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول تدوین:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**وضعیت راهبرد:** آماده تایید (IN_REVIEW)  
+
+این مستند راهبرد گام‌به‌گام و همه‌جانبه مهاجرت کدهای قدیمی و انتقال بایت‌های آبجکت استوریج به بستر معماری همگرای هدف را به همراه مکانیزم‌های بازگشت به عقب (Rollback Strategy) تشریح می‌کند.
+
+---
+
+## ۱. مراحل مهاجرت و همگرایی (Migration Stages)
+
+مهاجرت سیستم ذخیره‌سازی زنیک طی ۵ فاز سازمان‌یافته صورت می‌گیرد تا از هرگونه قطعی سرویس یا از دست رفتن اسناد حساس مهندسی پیشگیری شود:
+
+```
+[فاز ۱: بازبینی و طراحی] ──► [فاز ۲: مهاجرت پایگاه داده] ──► [فاز ۳: بازنویسی کدها] ──► [فاز ۴: انتقال فیزیکی بایت‌ها] ──► [فاز ۵: تست نهایی و Cutover]
+```
+
+### فاز ۱: تایید معماری و زیرساخت (Audit & Infrastructure Alignment)
+- **اقدامات:**
+  - تایید و تصویب سند معماری هدف توسط رهبری هوش مصنوعی (CEAI).
+  - افزودن کانتینر MinIO به فایل Docker Compose لوکال جهت هماهنگی محیط‌های لوکال توسعه‌دهندگان.
+  - حذف متغیرهای محیطی ناامن هاردکدشده پیش‌فرض در تمام سرویس‌ها.
+
+### فاز ۲: مهاجرت پایگاه داده (Database Migration)
+- **اقدامات:**
+  - اعمال شمای اصلاح‌شده Prisma و ایجاد فیلدهای ارتباطی کلید خارجی جدید به عنوان ویژگی‌های اختیاری (`Nullable`).
+  - اجرای مهاجرت پایگاه داده بدون تغییرات تخریبی روی فیلدهای قبلی:
+    `pnpm prisma migrate dev --name unified_storage_links`
+  - حفظ ستون قدیمی `storage_path` در جداول هوش مصنوعی تا پایان انتقال کامل داده‌ها.
+
+### فاز ۳: بازنویسی کدهای بک‌اند و پایتون (Code Refactoring)
+- **اقدامات:**
+  - تصحیح امضای تابع آپلود در کلاس `StorageService` و پیاده‌سازی اینترفیس یکتای `'IStorageService'`.
+  - رفکتور در فرآیند ثبت اسناد کارخانه دانش (`DocumentIntakeService.registerDocument`) جهت اتصال به استوریج جدید و برطرف کردن باگ کرش.
+  - بازنویسی کلاینت پایتونی `MinIOClient` در میکروسرویس پایتون جهت خواندن فایل‌ها از باکت مشترک `documents` با پیشوند مستأجر به جای باکت مستقل در هم شکسته قبلی.
+
+### فاز ۴: انتقال فیزیکی بایت‌ها و داده‌های قدیمی (Data Migration Script)
+- **اقدامات:**
+  - طراحی یک اسکریپت سبک مهاجرت داده فیزیکی (مثلاً اسکریپت Node.js یا پایتون مبتنی بر `mc` کلاینت).
+  - **وظیفه اسکریپت:** خواندن فایل‌های فیزیکی ذخیره شده در باکت‌های قدیمی به شکل `{workspace_id}-documents` و بارگذاری مجدد آن‌ها در باکت واحد مرکزی `documents` با الگوی جدید پیشوند مسیر:
+    `${workspaceId}/documents/${fileId}.${ext}`
+  - درج رکوردهای متناظر در جدول `files` پایگاه داده به صورت همزمان برای تضمین تمامیت ارجاعی.
+
+### فاز ۵: آزمون نهایی و تغییر وضعیت (Verification & Cutover)
+- **اقدامات:**
+  - اجرای تست‌های تضمین پایداری و ماتریس تست‌های ST-01 و ST-02.
+  - هدایت ترافیک نهایی فرانت‌اند به کنترلرهای جدید.
+  - پاکسازی فیزیکی کدهای مرده قدیمی و باکت‌های مستأجر منفرد قبلی از روی سرورهای MinIO.
+
+---
+
+## ۲. برنامه بازگشت به عقب در صورت بروز خطا (Rollback Strategy)
+
+در صورتی که در هر یک از مراحل مهاجرت با اختلال بحرانی یا از دست رفتن داده‌ها مواجه شویم، پروتکل بازگشت به عقب (Rollback) به صورت زیر به سرعت فعال می‌گردد:
+
+```mermaid
+graph TD
+    A[وقوع خطا در فرآیند مهاجرت] --> B{آیا خطا در لایه داده فیزیکی است؟}
+    B -- بله --> C[بازیابی بک‌آپ دایرکتوری MinIO با ابزار mc mirror]
+    B -- خیر --> D{آیا خطا در لایه دیتابیس است؟}
+    D -- بله --> E[اجرای مهاجرت معکوس دیتابیس با Rollback Migration]
+    D -- خیر --> F[برگشت کدهای برنامه به تگ گیت قبلی Git Revert]
+```
+
+### ۲.۱. بازیابی بایت‌های پایگاه داده و آبجکت استوریج
+- **دیتابیس:** در صورت بروز مشکل در ساختار دیتابیس، با استفاده از فایلهای پشتیبان تهیه شده قبل از فرآیند مهاجرت (`.sql`)، پایگاه داده به وضعیت پایدار قبلی بازگردانی می‌شود.
+- **استوریج شیء (MinIO):** قبل از شروع فرآیند انتقال فاز ۴، یک همگام‌سازی و کپی پشتیبان کامل از باکت‌های موجود تهیه می‌شود:
+  `mc mirror local/ remote/backups/storage_pre_migration`
+  در صورت خرابی، بایت‌های پشتیبان فورا ری‌استور می‌شوند.
+
+### ۲.۲. بازگشت کدهای برنامه (Application Code Rollback)
+- کدهای محیط عملیاتی بر روی بستر مخزن گیت به آخرین کامیت تگ پایدار قبلی بازمی‌ندازند تا سیستم به صورت فوری لود شده و خطاها در محیط محلی بازسازی و برطرف شوند.
+- این سازوکار تداوم کارکرد ۱۰۰ درصدی بیزینس را بدون ایجاد وقفه طولانی تضمین می‌کند.
+EOF
+
+# ── 12. PHASE ONE ENGINEERING ORDER ──────────────────────────────────────────
+echo "Generating: docs/implementation/phase-one-engineering-order.md"
+cat << 'EOF' > docs/implementation/phase-one-engineering-order.md
+# دستور مهندسی فاز یک: ارتقا و همگرایی پلتفرم ذخیره‌سازی زنیک (Phase One Engineering Order)
+========================================================================
+
+**شناسه دستور:** XENNIC-ENG-ORDER-STORAGE-001  
+**تاریخ صدور:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**صادرکننده:** دستیار معماری و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**گیرندگان:** تیم توسعه بک‌اند (NestJS)، تیم هوش مصنوعی (AI/Python) و توسعه‌دهندگان OpenCode  
+**وضعیت:** ابلاغ شده (PLANNED)  
+
+---
+
+## ۱. هدف و دامنه تغییرات (Purpose & Scope)
+
+این سند به عنوان **دستور فنی رسمی فاز یک (Engineering Order)** جهت پایان دادن به جزایر موازی ذخیره‌سازی، برطرف کردن باگ‌های حاد زمان اجرا و پیاده‌سازی پلتفرم همگرای فایل صادر می‌گردد. تمام تیم‌های توسعه و توسعه‌دهندگان جریان OpenCode موظفند این دستورالعمل را به عنوان معیار اصلی پذیرش تغییرات قرار دهند.
+
+تغییرات باید **منحصرا** در حوزه‌های زیر اعمال شوند:
+- ماژول ذخیره‌سازی بک‌اند (`apps/api/src/modules/storage`)
+- اتصالات ماژول کارخانه دانش (`apps/api/src/modules/knowledge-factory`)
+- کلاینت ذخیره‌سازی هوش مصنوعی پایتون (`workspace/services/ai-service`)
+- جداول مربوطه در فایل شمای پریزما (`prisma/schema.prisma`)
+
+---
+
+## ۲. وظایف فنی و مأموریت‌های اجرایی (Technical Action Items)
+
+تیم‌های فنی موظفند اقدامات زیر را با هماهنگی کامل و بدون تغییر در کدهای پروداکشن عملیاتی بدون تایید قبلی گیت‌های کیفیت، پیش ببرند:
+
+### مأموریت ۲.۱: بازنویسی امضاهای متدها و رفع باگ Intake کارخانه دانش
+1. **اصلاح کلاس `StorageService` بک‌اند:** پیاده‌سازی کامل متد آپلود با یک کانتکست شیء پارامتریک برای سازگاری کامل با کارهای فراخوانی دامنه‌های جانبی.
+2. **برطرف کردن تداخل `'IStorageService'`:** به روزرسانی امضای متد ثبت اسناد فنی در `DocumentIntakeService` برای همخوانی با هسته و جلوگیری از وقوع خطای کرش در زمان ثبت و بخش‌بندی اسناد.
+3. **حذف کدهای مرده:** پاکسازی کلاس بدون استفاده `MinioStorageService` در پوشه کارخانه دانش.
+
+### مأموریت ۲.۲: اصلاح رفتار کلاینت پایتون و لغو ایجاد باکت‌های انفرادی
+1. **بازنویسی `MinIOClient` در پایتون:** غیرفعال کردن کدهای ایجاد باکت اختصاصی برای هر مستأجر به فرمت `{workspace_id}-documents`.
+2. **یکپارچه‌سازی باکت‌ها:** کدهای خواندن و تحلیل اسناد مهندسی به باکت یکتای `documents` هدایت شوند و آدرس فایل‌ها با الگوی `${workspace_id}/documents/${file_id}.ext` لود شود.
+
+### مأموریت ۲.۳: ایجاد دسترسی عمومی ایمن به دارایی‌های وب (Public Route)
+1. **طراحی اندپوینت اختصاصی بدون گارد:** تعریف متد `GET /storage/public/:id` در کلاس کنترلر ذخیره‌سازی.
+2. **گارد بررسی نام باکت:** این اندپوینت فقط و فقط باید به رکوردهایی اجازه دسترسی دهد که مقدار فیلد `bucket` آن‌ها در پایگاه داده برابر با `"public"` باشد. در غیر این صورت فورا خطای عدم دسترسی ۴۰۳ ارسال گردد.
+
+### مأموریت ۲.۴: پیاده‌سازی منطق نسخه‌گذاری و زمان‌بندی پاکسازی
+1. **توسعه متد `uploadNewVersion`:** منطق ذخیره‌سازی نسخه جدید فایل در جدول `file_versions` به همراه همگام‌سازی باکت‌های MinIO توسعه یابد.
+2. **پیاده‌سازی Scheduler کرون جاب روزانه:** نوشتن اسکریپت جاب جهت اسکن و حذف فیزیکی بایت‌های مربوط به فایل‌هایی که بیش از ۳۰ روز از حذف نرم آن‌ها گذشته است.
+
+---
+
+## ۳. قوانین هماهنگی با جریان توسعه OpenCode (OpenCode Rules)
+
+جهت جلوگیری از بروز تداخل (Overlap) در تغییرات کدها در جریان موازی توسعه:
+1. توسعه‌دهندگان OpenCode حق تغییر هیچ فایلی خارج از محدوده تعیین شده در بخش ۱ این سند را ندارند.
+2. هرگونه تحویل خروجی از طرف OpenCode که حاوی تعاریف جدید باکت در پایتون یا پیاده‌سازی مجزای کلاینت S3 باشد، فوراً ریجکت (REJECTED) خواهد شد.
+3. تایید نهایی کدهای پیاده‌سازی شده بر عهده این دستیار بر اساس معیارهای پذیرش (Quality Gates) خواهد بود.
+
+---
+
+## ۴. معیارهای نهایی پذیرش کیفیت (Acceptance Criteria)
+
+تغییرات فاز یک زمانی تایید (APPROVED) قلمداد می‌شوند که:
+1. فرآیند آپلود، رگولارایزیشن، چانکینگ و RAG اسناد فنی در کارخانه دانش بدون بروز خطا و با لود موفق بایت‌ها از باکت مشترک `documents` انجام شود.
+2. اندپوینت دسترسی عمومی تصویر لوگوی وب‌سایت بدون هدر لاگین تایید و لود شود.
+3. هیچ کلید و پسورد پیش‌فرضی به صورت هاردکدشده در فولد کدهای بک‌اند یا پایتون باقی نمانده باشد.
+4. تمام تست‌های امنیتی تداخل مستأجرین و ماتریس تست‌های ST-01 با قبولی صد در صدی پاس شوند.
+5. گزارش نهایی تایید کیفیت توسط این دستیار صادر و تحویل Chief Executive AI گردد.
+EOF
+
+# ── 13. EXECUTIVE SUMMARY ────────────────────────────────────────────────────
+echo "Generating: docs/audit/executive-summary.md"
+cat << 'EOF' > docs/audit/executive-summary.md
+# خلاصه مدیریتی ممیزی و طراحی معماری پلتفرم ذخیره‌سازی (Executive Summary)
+========================================================================
+
+**شناسه سند:** XENNIC-STORAGE-EXEC-001  
+**تاریخ سند:** ۲۸ تیر ۱۴۰۵ (2026-07-18)  
+**نسخه:** 1.0.0  
+**مسئول ممیزی:** دستیار ممیزی و حاکمیت Storage (شناسه: `XENNIC-STORAGE-ARCH-001`)  
+**مخاطب:** Chief Executive AI (CEAI)  
+**وضعیت:** تکمیل شده (COMPLETED)  
+
+---
+
+## ۱. یافته‌های کلیدی ممیزی فاز صفر (Audit Findings)
+
+ممیزی جامع پلتفرم ذخیره‌سازی پروژه زنیک (Xennic) در فاز صفر، سه گسست یا ناهماهنگی اصلی (Structural Discrepancies) را مابین طراحی تئوریک و پیاده‌سازی واقعی کد شناسایی کرده است:
+
+1. **باگ بحرانی پردازش اسناد (Critical Intake Crash):** ارجاع نادرست اینترفیس `'IStorageService'` در ماژول کارخانه دانش به سرویس هسته ذخیره‌سازی منجر به بروز ناسازگاری شدید در امضای متدها شده و فرآیند پردازش RAG اسناد را در زمان اجرا به طور ۱۰۰٪ با کرش مواجه می‌سازد.
+2. **شکل‌گیری جزایر موازی ذخیره‌سازی:** میکروسرویس پایتون (`ai-service`) اقدام به ایجاد باکت‌های کاملاً مجزا برای هر مستأجر به فرمت `{workspace_id}-documents` کرده که با معماری ۶ باکت سراسری هسته زنیک در تضاد فاحش است. این تضاد مانع از تعامل پایدار هوش مصنوعی با فایل‌های آپلود شده کاربران در لایه وب می‌گردد.
+3. **عدم دسترسی به فایل‌های عمومی (Public Access Block):** گارد امنیتی مستأجر و کاربر به طور سراسری روی تمام متدهای کنترلر استوریج کشیده شده است. این امر دسترسی بدون لاگین به تصاویر پروفایل کاربران، لوگوهای سازمان و دارایی‌های عمومی برندینگ را ناممکن ساخته است.
+4. **رها شدن جداول نسخه‌گذاری:** فرآیند ثبت و بازیابی نسخه‌های مختلف سند در دیتابیس بدون هیچ کد عملیاتی در بک‌اند رها شده است.
+
+---
+
+## ۲. توصیف معماری هدف پیشنهادی (Proposed Target Design)
+
+معماری هدف تدوین‌شده در سند `docs/architecture/storage-platform-architecture.md` راهکارهای پایدار زیر را برای حل قطعی چالش‌های فوق اتخاذ کرده است:
+
+- **تمرکزگرایی تعامل با آبجکت استوریج:** تمام بخش‌های برنامه (شامل هوش مصنوعی و پایتون) ملزم به هدایت فایل‌ها به سمت ۶ باکت رسمی هسته (`public`, `private`, `reports`, `documents`, `engineering`, `ai`) با پیشوند مسیر استاندارد مستأجر هستند.
+- **اینترفیس یکتای تعاملی (`IUnifiedStorageService`):** رفع ناسازگاری امضای متدها و اصلاح کامل فرآیند ثبت سند کارخانه دانش بر روی فایل‌های ثبت شده در سیستم مرکزی.
+- **اندپوینت عمومی امن (Public Safe Path):** تعریف آدرس `/storage/public/:id` بدون نیاز به احراز هویت کاربری که منحصراً برای لود بایت‌ها از باکت عمومی `public` بهینه‌سازی شده است.
+- **برقراری تمامیت ارجاعی پایگاه داده:** اصلاح مدل Prisma جهت پیوند ساختاری مستقیم بین فایل‌ها و سایر نهادهای بیزینسی (مانند محاسبات مهندسی، لوگوی فضاها، آواتارها و اسناد چت).
+
+---
+
+## ۳. اسناد و مراجع ثبت شده (Deliverables Checklist)
+
+در چارچوب مأموریت فاز صفر، اسناد کلیدی زیر در بستر مخزن مستندات زنیک مستقر و ایجاد شدند:
+
+1. **گزارش ممیزی وضعیت فعلی:** `docs/audit/storage-current-state-audit.md` (بررسی جامع مشکلات و بدهی‌های فنی).
+2. **سند معماری هدف پلتفرم:** `docs/architecture/storage-platform-architecture.md` (تبیین چارچوب‌ها و دیاگرام‌های جدید).
+3. **ثبت تصمیم معماری رسمی (ADR):** `docs/adr/ADR-021-canonical-file-document-asset-platform.md` (ثبت حاکمیتی تصمیم و اثرات معماری).
+4. **فهرست شکاف‌ها و ریسک‌ها:** `docs/implementation/storage-gap-registry.md` و `docs/generated/storage-risk-register.md`.
+5. **مستند اتصالات API و مدل دیتابیس:** `docs/generated/storage-api-inventory.md` و `docs/generated/storage-database-inventory.md`.
+6. **ماتریس‌های تست، دسترسی و چرخه حیات:** `docs/generated/storage-permission-matrix.md`، `docs/generated/storage-lifecycle-state-machine.md` و `docs/generated/storage-test-gap-matrix.md`.
+7. **دستور مهندسی فاز یک:** `docs/implementation/phase-one-engineering-order.md` (ابلاغیه رسمی کارهای توسعه‌دهندگان).
+8. **سند راهبرد مهاجرت داده‌ها:** `docs/generated/storage-migration-strategy.md` (فرمول انتقال بدون قطعی).
+
+---
+
+## ۴. برنامه و گام‌های آتی (Next Steps)
+
+پس از تایید این سند توسط Chief Executive AI، دستور مهندسی صادر شده به فاز اجرا (Phase 1 — Implementation) هدایت شده و مراحل مهاجرت دیتابیس و همسان‌سازی کدهای پایتون و بک‌اند آغاز خواهد شد. پلتفرم با استقرار این معماری نوین صاحب یک لایه ذخیره‌سازی مقتدر، ایمن و بهینه برای محافظت از Single Source of Truth خواهد شد.
+EOF
+
+echo "All 13 storage platform architecture and governance files have been generated successfully!"
+chmod +x docs/implementation/phase-one-engineering-order.md
