#!/bin/sh
set -e

echo "🔍 Running lint-staged..."
corepack pnpm exec lint-staged

echo "🔍 Running architecture validation for changed API files..."
corepack pnpm exec tsx tools/architecture/validate.ts --light --changed --no-reports

echo "✅ Pre-commit checks passed"
