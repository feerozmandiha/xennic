{
  "name": "lightweight",
  "description": "Minimal load profile for CI/CD validation",
  "vus": 2,
  "duration": "30s",
  "thresholds": {
    "http_req_duration": ["p(95)<5000"],
    "http_req_failed": ["rate<0.1"]
  }
}
