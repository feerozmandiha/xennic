services:

<<<<<<< HEAD
  # ── PostgreSQL ──────────────────────────────────────────────────
  postgres:
    image: postgres:17-alpine
    container_name: xennic-postgres
    restart: unless-stopped
=======
  postgres:
    image: postgres:17-alpine

    container_name: xennic-postgres

    restart: unless-stopped

>>>>>>> 224dcab25526dff14bfe3eb02e4a18e7cb25853a
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
<<<<<<< HEAD
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
=======

    ports:
      - "${POSTGRES_PORT}:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

>>>>>>> 224dcab25526dff14bfe3eb02e4a18e7cb25853a
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
<<<<<<< HEAD
    networks:
      - xennic-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ── PgBouncer (Connection Pooler) ───────────────────────────────
  pgbouncer:
    image: edoburu/pgbouncer:latest
    container_name: xennic-pgbouncer
    restart: unless-stopped
    environment:
      - DB_USER=${POSTGRES_USER}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=${POSTGRES_DB}
      - POOL_MODE=transaction
      - DEFAULT_POOL_SIZE=25
      - MAX_CLIENT_CONN=200
      - MAX_DB_CONNECTIONS=50
    ports:
      - "6432:6432"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - xennic-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h postgres -U ${POSTGRES_USER} -d ${POSTGRES_DB} || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ── Redis ──────────────────────────────────────────────────────
  redis:
    image: redis:8-alpine
    container_name: xennic-redis
    restart: unless-stopped
=======

  redis:
    image: redis:8-alpine

    container_name: xennic-redis

    restart: unless-stopped

>>>>>>> 224dcab25526dff14bfe3eb02e4a18e7cb25853a
    command:
      - redis-server
      - --requirepass
      - ${REDIS_PASSWORD}
<<<<<<< HEAD
      - --appendonly
      - "yes"
    ports:
      - "${REDIS_PORT:-6380}:6379"
    volumes:
      - redis_data:/data
=======

    ports:
      - "${REDIS_PORT}:6379"

    volumes:
      - redis_data:/data

>>>>>>> 224dcab25526dff14bfe3eb02e4a18e7cb25853a
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
<<<<<<< HEAD
    networks:
      - xennic-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ── RabbitMQ ────────────────────────────────────────────────────
  rabbitmq:
    image: rabbitmq:4-management
    container_name: xennic-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
    ports:
      - "${RABBITMQ_PORT:-5672}:5672"
      - "${RABBITMQ_UI_PORT:-15672}:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
=======

  rabbitmq:
    image: rabbitmq:4-management

    container_name: xennic-rabbitmq

    restart: unless-stopped

    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}

    ports:
      - "${RABBITMQ_PORT}:5672"
      - "${RABBITMQ_UI_PORT}:15672"

    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

>>>>>>> 224dcab25526dff14bfe3eb02e4a18e7cb25853a
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 15s
      timeout: 5s
      retries: 5
<<<<<<< HEAD
    networks:
      - xennic-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ── MinIO (S3-Compatible Object Storage) ────────────────────────
  minio:
    image: minio/minio:latest
    container_name: xennic-minio
    command: server /data --console-address ":9001"
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    networks:
      - xennic-network
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ── Qdrant (Vector Database) ────────────────────────────────────
  qdrant:
    image: qdrant/qdrant:v1.13.0
    container_name: xennic-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - xennic-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/healthz"]
      interval: 15s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ── Engineering Service ─────────────────────────────────────────
=======

  # Engineering Service - Electrical Calculation Engine
>>>>>>> 224dcab25526dff14bfe3eb02e4a18e7cb25853a
  engineering-service:
    build:
      context: ../../../../workspace/services/engineering-service
      dockerfile: Dockerfile
    container_name: xennic-engineering-service
    ports:
      - "${ENGINEERING_SERVICE_PORT:-8001}:8001"
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-production}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - xennic-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

<<<<<<< HEAD
  # ── Vision Service (OCR + Image Analysis) ───────────────────────
=======
  # Vision Service - Document & Nameplate Reader
>>>>>>> 224dcab25526dff14bfe3eb02e4a18e7cb25853a
  vision-service:
    build:
      context: ../../../../workspace/services/vision-service
      dockerfile: Dockerfile
    container_name: xennic-vision-service
    ports:
      - "${VISION_SERVICE_PORT:-8003}:8003"
    environment:
      - SERVICE_PORT=8003
      - OCR_ENGINE_MODE=${VISION_OCR_ENGINE_MODE:-hybrid}
      - VISION_LLM_PROVIDER=${VISION_LLM_PROVIDER:-groq}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8003/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          memory: 2G
    networks:
      - xennic-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

<<<<<<< HEAD
  # ── AI Service (LLM Orchestration) ──────────────────────────────
  ai-service:
    build:
      context: ../../../../workspace/services/ai-service
=======
networks:
  xennic-network:
    driver: bridge
    name: xennic-network

  # AI Service - Intelligent Engineering Agents
  ai-service:
    build:
      context: ./services/ai-service
>>>>>>> 224dcab25526dff14bfe3eb02e4a18e7cb25853a
      dockerfile: Dockerfile
    container_name: xennic-ai-service
    ports:
      - "${AI_SERVICE_PORT:-8002}:8002"
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-production}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
      - ENGINEERING_SERVICE_URL=http://engineering-service:8001
    restart: unless-stopped
    depends_on:
      engineering-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - xennic-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
<<<<<<< HEAD
  postgres_data:
  redis_data:
  rabbitmq_data:
  minio_data:
  qdrant_data:

networks:
  xennic-network:
    driver: bridge
    name: xennic-network
=======

  postgres_data:
  redis_data:
  rabbitmq_data:
>>>>>>> 224dcab25526dff14bfe3eb02e4a18e7cb25853a
